# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.Python

# 运行时数据（用户生成的人设/图片/上传，不入库）。
# 只忽略项目根下的 data/（运行时数据卷），不误伤 app/data/（随代码发布的数据文件）。
/data/

# 环境变量与密钥
.env
.env.*
*.key

# 构建产物
*.tar.gz
popop-pipeline-image.tar.gz

# 日志
*.log
nohup.out

# 编辑器/系统
.vscode/
.idea/
.DS_Store

# 临时脚本
_fix*.py
.claude/

# 灵感库派生产物（从 decompositions.json 预处理，体积大；随 Docker 镜像发布，不入 git）
app/data/inspiration_vectors.npy
app/data/inspiration_vectors.ckpt.npy
app/data/inspiration_vectors.done.npy
