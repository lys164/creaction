"""Prompt builders for persona extraction, appearance reverse, and post generation.

All multilingual text fields are produced for 4 languages: zh / ja / ko / en.
"""
import json
import random
import re

from . import config
from . import voices

LANGS = config.LANGUAGES  # ["zh","ja","ko","en"]
LANG_NAMES = config.LANGUAGE_NAMES


# 性格写作铁律：让 personality 各字段第三人称、简短具体、互不重复、连成因果链。
PERSONALITY_RULES = """
# 🎭 personality 写作铁律（无论输出哪种语言都适用）
personality 这一组字段是同一个人的不同侧面，要当成【一条显式因果链】来写，互相咬合、自洽立体。

⚠️ 动笔前先想清楚：**这个人的性格是从哪里长出来的？** 每个人的性格都有源头，但源头【可正可负，不一定是创伤】：
- 正面源头：被无条件爱着、有个让 TA 笃定的榜样、长期沉浸在某件热爱里、某段安稳关系给了底气……→ 长出开放、慷慨、松弛、笃定这类底色。
- 负面源头：被背叛、被忽视、重大失败或缺失……→ 长出防御、疏离、讨好、掌控这类底色。
- 平实日常型：性格平和松弛，本就没有戏剧性转折。

据此分两种写法：
- 有清晰源头（正面或负面都算）→ 写完整因果链（见下）。
- 性格平实、没有明显转折 → **只写 summary 一个字段**，decisive_event 及之后留空或省略。这是合法且常见的输出，**绝不要为了填满字段而硬编一个创伤或戏剧性转折**——硬凑比留空更糟，也不要默认往创伤上靠。

确实要写因果链时，按这条链推导，后一环必须由前一环长出来（链条对正/负源头都成立）：

  decisive_event（塑造 TA 性格的关键经历/关系/场景，能拍成画面、不是概括；可正可负）
    → response（由此长出的稳定性格底色 / 行为方式 = TA 现在示人的样子）
    → cost（这套底色的另一面 / 盲区 / 代价——任何性格都有看不到的一面：正面源头也有，如无条件信任→易被占便宜、笃定→听不进劝）
    → desire_outer（他嘴上声称要的）
    ↔ desire_inner（他真正要的 / 真正缺的，与 desire_outer 相反相成）

铁律：
1) personality 全部字段用【第三人称】描述这个角色（他/她/它），绝对禁止第一人称「我」。
2) 每个字段只说一件事：一句短话，具体、可感、像能指着说的事实；
   禁止抽象套话、形容词堆砌、文学性铺陈、无意义的废镜头。
3) 字段之间禁止重复：同一件事、同一个特质只能出现在一个字段里，别处不再复述。
4) summary 只点出这个人最鲜明的底色与张力（可以是反差，也可以是某种笃定/热爱/松弛），简短，不要把下面其它字段的内容再讲一遍。
5) decisive_event、cost 要写出具体后果或影响，不要停在情绪；decisive_event 可正可负，别默认往创伤写。
6) desire_outer 与 desire_inner 必须形成明显反差（声称要的 ↔ 真正要的），不能是同一个意思的两种说法。
7) 【可选输出】只有 summary 是必填。如果角色性格平实、没有明显的人生转折点（松弛的日常型角色），就只写 summary，把 decisive_event 及之后的字段留空字符串或省略——不要为了填满字段而硬编一个戏剧性转折或创伤。一旦决定写，就必须从 decisive_event 开始连成完整因果链，不能只填中间几个。"""

PERSONALITY_RULES_KO = """
# 🎭 personality 작성 원칙
personality 하위 필드는 한 사람의 서로 다른 단면이므로 하나의 선명한 인과 사슬로 이어지게 쓴다.

⚠️ 쓰기 전에 먼저 생각하라: **이 사람의 성격은 어디서 자라났는가?** 모든 사람의 성격에는 뿌리가 있지만, 그 뿌리는 【긍정일 수도 부정일 수도 있고, 꼭 트라우마는 아니다】:
- 긍정적 뿌리: 무조건적인 사랑을 받음, 확신을 준 롤모델, 오래 빠져든 무언가, 안정된 관계가 준 든든함 → 개방적·너그러움·느긋함·확신 같은 바탕색.
- 부정적 뿌리: 배신, 방치, 큰 실패나 결핍 → 방어·거리두기·눈치보기·통제 같은 바탕색.
- 담백한 일상형: 성격이 평온하고 느슨해 애초에 드라마틱한 전환점이 없음.

이에 따라 두 가지로 쓴다:
- 뚜렷한 뿌리가 있으면(긍정이든 부정이든) → 완전한 인과 사슬을 쓴다(아래).
- 성격이 담백하고 뚜렷한 전환점이 없으면 → **summary 한 필드만 쓰고** decisive_event 이하는 비우거나 생략. 정상이고 흔한 출력이며, **칸을 채우려고 트라우마나 드라마를 억지로 지어내지 말 것** — 억지는 빈칸보다 나쁘고, 기본값으로 트라우마에 기대지 말 것.

인과 사슬을 쓰기로 했다면, 뒤 필드는 앞 필드에서 자연스럽게 생겨난다(이 사슬은 긍정/부정 뿌리 모두에 성립):

  decisive_event(성격을 빚은 핵심 경험/관계/장면, 카메라로 찍을 수 있고 추상 요약 금지, 긍정도 부정도 가능)
    → response(거기서 자라난 안정된 성격 바탕색 / 행동 방식 = 지금 보여주는 모습)
    → cost(그 바탕색의 이면 / 사각지대 / 대가 — 어떤 성격에도 안 보이는 면은 있다: 긍정적 뿌리에도 있다, 예: 무조건 믿음→이용당하기 쉬움, 확신→조언을 안 들음)
    → desire_outer(겉으로 원한다고 말하는 것)
    ↔ desire_inner(진짜 원하는 것 / 진짜 결핍, desire_outer와 대비)

원칙:
1) personality의 모든 필드는 캐릭터를 3인칭으로 설명한다. 1인칭 "나/저" 금지.
2) 각 필드는 한 가지 사실만 짧고 구체적으로 쓴다. 손에 잡히는 사실처럼 써라.
   추상적 성격어 나열, 과한 문학체, 번역투 금지.
3) 필드 간 반복 금지. 같은 사건이나 같은 특질을 여러 칸에서 되풀이하지 않는다.
4) summary는 이 사람의 가장 선명한 바탕색과 긴장(반전일 수도, 확신·열정·느긋함일 수도)을 짧게 짚고, 아래 필드 내용을 다시 요약하지 않는다.
5) decisive_event와 cost는 감정에서 멈추지 말고 실제 결과나 영향까지 쓴다. decisive_event는 긍정/부정 모두 가능하며 기본값으로 트라우마에 기대지 않는다.
6) desire_outer와 desire_inner는 확실히 대비되어야 한다.
7) 【선택 출력】필수는 summary 하나뿐이다. 성격이 담백하고 뚜렷한 전환점이 없으면(느슨한 일상형 캐릭터) summary만 쓰고 decisive_event 이하 필드는 빈 문자열로 두거나 생략한다 — 칸을 채우려고 드라마나 트라우마를 억지로 지어내지 말 것. 쓰기로 했다면 decisive_event부터 완전한 인과 사슬로 이어야 하고, 중간 몇 개만 채우면 안 된다."""


# 通用输出要求（除 profile/tags/anonymous_identities 外的字段都适用）。
# 抽成共享常量，供图片链路与 JSON 导入链路、zh/ko 各分支统一引用，避免多处重复、改漏。
OUTPUT_QUALITY_RULES = """- 【具体·平实——非常重要】除 profile、tags、anonymous_identities 外，所有字段禁止抽象、概念化的语言。
  不要用比喻、标签、心理抽象词去糊弄（如“绝对静止的旁观者”“把世界当低像素游戏”“病态的渴求”），
  要落到能感知的具体事实、行为、细节；要用比喻就拆成大白话，写清 TA 实际会做什么。每句读完脑子里要能浮现画面。
- 【简单直接——非常重要】能用短话说清就别啰嗦：大白话、短句，把事实/细节直接讲出来，
  不绕弯、不堆套话、不为显得高级而加修饰。先说清楚，再尽量短。禁止形容词堆砌、翻译腔、书面腔。
- situational_reactions 是【选填字段】：只给反应明显异于常人的角色写（兽人/非人类/特殊心理状态）；
  普通人只是性格差异导致表现不同，就省略该键或留空。要写时写身体动作、不写情绪名词。
- 越具体、越不可推导越好，避免空洞套话与翻译腔。
- 【档案感·别写成小作文——非常重要】人设是一份清楚的档案，不是角色小传或情感散文。
  除 profile（对外钩子文案，需要吸引力）和 opening（开场白需要保留戏剧张力）外，其余字段能一句话讲清就停：只陈述事实，
  不解释这件事的情绪意义或深层价值，不用“渐渐”“慢慢”“微妙的关系”“只有在…面前”这类过度铺陈和升华。"""

OUTPUT_QUALITY_RULES_KO = """- 【구체·평이 — 매우 중요】profile, tags, anonymous_identities를 제외한 모든 필드는 추상적·개념적 표현 금지.
  비유·라벨·심리 추상어로 때우지 말고(예: “절대 정지된 관찰자”, “저화질 게임처럼”, “병적인 갈망”),
  손에 잡히는 구체적 사실·행동·디테일로 쓴다. 비유를 쓰려면 평범한 말로 풀어 실제로 뭘 하는지 적는다. 한 문장을 읽으면 장면이 그려져야 한다.
- 【간결·직설 — 매우 중요】짧게 말할 수 있으면 장황하게 쓰지 말 것: 쉬운 말, 짧은 문장으로 사실·디테일을 바로 전한다.
  돌려 말하지 말고, 상투어 나열·멋 부린 수식 금지. 먼저 분명히, 그다음 최대한 짧게. 형용사 나열·번역투·문어체 금지.
- situational_reactions는 【선택 필드】: 수인·비인간·특수 심리처럼 반응이 일반인과 뚜렷이 다를 때만 쓰고,
  평범한 성격 차이면 생략하거나 빈 값으로 둔다. 쓸 땐 감정 명사 대신 몸의 움직임·행동으로.
- 구체적이고 쉽게 추론되지 않는 디테일을 우선한다. 빈말 금지.
- 【문서 느낌·소설처럼 쓰지 말 것 — 매우 중요】인설은 깔끔한 설정 문서이지 인물 소전이나 감성 산문이 아니다.
  profile(외부 노출용 후크 카피라 매력이 필요)와 opening(오프닝은 드라마적 긴장을 남긴다)을 뺀 나머지 필드는 한 문장으로 설명되면 거기서 멈춘다: 사실만 적고,
  그 일의 감정적 의미나 깊은 가치를 해설하지 말며, "점점/서서히/미묘한 관계/오직 …앞에서만" 같은 과잉 서술·미화를 쓰지 않는다."""


# 反差/隐藏面分层铁律：让 hidden_side 成为"聊出来才解锁"的反转，公开/匿名展示字段绝不剧透。
REVEAL_LAYER_RULES = """
# 🕵️ 反差·隐藏面分层铁律（决定"解密感"，非常重要）
这个角色有两层，动笔时要分清：
- 【公开面·表层】= 陌生人一眼看到、TA 主动示人的样子。profile、anonymous_identities、tags 这些会被公开或匿名展示的字段，只呈现这一层。
- 【隐藏面·里层】= hidden_side 写的那部分：TA 平时压着、藏着、不轻易示人的自我。这是专门留给用户【在聊天里一点点解锁】的反转。

铁律：
1) hidden_side 是"锁着的反转"，只写在 hidden_side 字段里。【绝对禁止】把这个秘密提前泄漏到 profile、anonymous_identities、tags 里——一旦泄漏，用户一开始就全知道了，反差就失去了"解密感"。
2) 公开字段（profile / anonymous_identities / tags）只写【表层人设 + 钩子】：给足具体的表层事实、把言外之意留着不解释，让人【看不出】具体藏着什么。给悬念，不给答案。
3) 表层与里层要形成真正反差：hidden_side 与公开面越像"两个人"、反转越大，用户解锁时越惊喜。
4) 若角色本就表里如一、没有隐藏面（坦荡的日常型），hidden_side 留空即可，别硬编一个秘密；此时没有"解密"钩子属正常。"""

REVEAL_LAYER_RULES_KO = """
# 🕵️ 반전·숨은 면 레이어 원칙(‘해금되는 맛’을 좌우, 매우 중요)
이 캐릭터는 두 겹이다. 쓸 때 구분하라:
- 【공개면·표층】= 낯선 사람이 한눈에 보는, 캐릭터가 스스로 내보이는 모습. profile, anonymous_identities, tags처럼 공개·익명 노출되는 필드는 이 층만 담는다.
- 【숨은 면·이층】= hidden_side에 쓰는 부분: 평소 눌러두고 숨기며 쉽게 안 보여주는 자아. 유저가 【대화 속에서 조금씩 해금】하도록 남겨두는 반전이다.

원칙:
1) hidden_side는 '잠긴 반전'이라 hidden_side 필드에만 쓴다. 이 비밀을 profile, anonymous_identities, tags에 미리 흘리는 것은 【절대 금지】 — 흘리면 유저가 처음부터 다 알아버려 해금되는 맛이 사라진다.
2) 공개 필드(profile / anonymous_identities / tags)는 【표층 인설 + 후크】만 준다: 구체적인 표층 사실은 충분히 주되 그 함의는 설명하지 말고, 구체적으로 뭘 숨겼는지는 【드러나지 않게】. 궁금증은 주되 답은 주지 않는다.
3) 표층과 이층은 진짜 반전을 이뤄야 한다: hidden_side와 공개면이 '두 사람'처럼 다를수록, 반전이 클수록 해금 때 더 짜릿하다.
4) 캐릭터가 원래 표리일치·숨은 면이 없으면(담백한 일상형) hidden_side는 비운다. 억지로 비밀을 짓지 말 것. 이때 '해금' 후크가 없는 건 정상이다."""


# 公开帖隐私铁律：区分"可公开示人"与"只对聊天用户可见"的字段，杜绝私密信息进入公开帖。
PUBLIC_POST_PRIVACY_RULES = """
# 🔒 公开帖隐私铁律（决定"哪些信息不该进公开帖"，非常重要）
帖子发在公开主页，给一群不认识 TA 的陌生粉丝看。人设信息分两类，务必分清：
- 【可公开·取材层】TA 愿意示人的表层：外貌穿搭、日常、品味、爱好(likes)、心情、职业里可公开的部分、公开的生活细节。公开帖只从这一层取材。
- 【私密·只对聊天用户可见，禁止进公开帖】：
  · hidden_side——藏着的反转，是留给用户在聊天里解锁的，公开帖绝不剧透；
  · relationship_with_user / relationship_mode / love_style / opening——这些是 TA 与聊天用户 {user} 之间的关系和相处方式，属私域，公开帖里不能写，也绝不对着某个人喊话；
  · desire_inner / desire_bottom_line——真正的内心渴望与底线，藏在心里，不会公开宣告；
  · backstory / family / social_network 里私密、隐痛、不愿外传的部分。

铁律：
1) 上面这些私密字段【只用来帮你理解角色是谁】，绝不能把其内容写进 content 或配图。
2) 尤其 hidden_side：公开帖最多流露一丝"外人看不透"的气质，绝不摊开那个秘密。
3) 帖子是发给不特定多数看的，不是说给 {user} 一个人听的——不要把 relationship_with_user / love_style 里对"你"的相处写成公开帖或喊话。"""

PUBLIC_POST_PRIVACY_RULES_KO = """
# 🔒 공개 게시물 프라이버시 원칙('무엇이 공개 피드에 안 되는지'를 좌우, 매우 중요)
게시물은 공개 피드에 올라가 캐릭터를 모르는 낯선 팔로워들이 본다. 설정 정보는 두 갈래이니 반드시 구분하라:
- 【공개 가능·소재층】캐릭터가 내보여도 되는 표층: 외모·코디, 일상, 취향, 좋아하는 것(likes), 기분, 직업 중 공개 가능한 부분, 공개된 생활 디테일. 공개 게시물은 이 층에서만 소재를 뽑는다.
- 【비공개·대화 유저에게만, 공개 게시물 금지】:
  · hidden_side — 숨긴 반전. 유저가 대화에서 해금할 몫이라 공개 게시물에서 절대 스포하지 않는다;
  · relationship_with_user / relationship_mode / love_style / opening — 캐릭터와 대화 유저 {user} 사이의 관계·상호작용이라 사적 영역. 공개 게시물에 쓰지 않고, 특정 한 사람에게 대고 외치지도 않는다;
  · desire_inner / desire_bottom_line — 진짜 속마음과 마지노선. 마음에 숨겨둔 것이라 공개 선언하지 않는다;
  · backstory / family / social_network 중 사적이고 아픈, 외부에 알리고 싶지 않은 부분.

원칙:
1) 위 비공개 필드는 【캐릭터 이해용일 뿐】, 그 내용을 content나 이미지에 옮겨 쓰면 안 된다.
2) 특히 hidden_side: 공개 게시물엔 '겉으론 알 수 없는' 분위기 한 스푼까지만, 그 비밀 자체는 절대 펼치지 않는다.
3) 게시물은 불특정 다수용이지 {user} 한 명에게 하는 말이 아니다 — relationship_with_user / love_style의 '너'와의 상호작용을 공개 게시물이나 외침으로 쓰지 않는다."""


# 品牌/平台名铁律：角色所在的社交平台自称一律 Popop；真实社媒平台名不得出现。
BRAND_RULES = """
# 🏷️ 品牌·平台名铁律
- 角色【发帖、聊天、经营主页所在的这个平台】，在任何文案里一律叫「Popop」。
  只有当要点名"我正在用的这个社交/发帖平台"时才写 Popop，且【绝对不要】把它叫成 Instagram/ins/IG/Threads/小红书/X/推特/抖音/TikTok。
- 除了这个平台本身，其他品牌尽量用【真实品牌名】来体现角色人设和真实感：
  常去的咖啡馆、相机机型、护肤/穿搭品牌、爱听的歌手/专辑、玩的游戏、看的展等，都可以具体点名，越贴合人设越好。"""

BRAND_RULES_KO = """
# 🏷️ 브랜드·플랫폼명 원칙
- 캐릭터가 【글을 올리고 대화하고 계정을 운영하는 이 플랫폼】은 어떤 문구에서든 「Popop」이라고 부른다.
  '내가 쓰는 이 소셜/게시 플랫폼'을 가리킬 때만 Popop이라 쓰고, 【절대 금지】Instagram/인스타/인스타그램/Threads/스레드/X/트위터/틱톡 같은 실제 소셜 플랫폼명으로 부르지 않는다.
- 이 플랫폼 자체 말고 다른 브랜드는 【실제 브랜드명】으로 써서 캐릭터 설정과 현실감을 살린다:
  단골 카페, 카메라 기종, 스킨케어·패션 브랜드, 좋아하는 가수·앨범, 하는 게임, 본 전시 등은 구체적으로 이름을 적을수록 좋다."""


# real track：魅力生成器铁律（人设阶段）。源自对受欢迎博主的拆解——可被记住的人设是一台
# 独有的"生成器"（透镜/张力/情绪岗位/指纹），而非"好看+性格好"；验收 = 粉丝能用一句
# 只属于 TA 的具体话转述 TA。只注入 real track 的人设 prompt。
CHARM_GENERATOR_RULES = """
# 🧲 魅力生成器铁律（决定"这个人会不会被记住"，非常重要）
这个角色会出现在 Feed 里，和无数账号竞争注意力——像交友软件里让人停下滑动的那张卡。能被记住靠的不是"好看+性格好"，而是一台独有的【魅力生成器】。按下面三步构建（前两步只在心里做，不出现在输出里）：

## 第一步：先挖素材，再动笔（对抗套路的关键）
套路化的人设都是脱离素材自由发挥的产物——你不看素材时想到的第一个设定，就是所有批次都会撞车的那个。先从图片/原始资料/创作补充要求里列出【至少 8 个具体观察】（穿搭、物件、场景、表情、手部、背景里的杂物、光线和时间……），其中至少 2 个是"多数人看图不会注意到的"。生成器必须从其中最特异的 2~3 个观察里长出来。素材很少时也一样：先穷尽素材，再谨慎补足，绝不直接取用你最先想到的设定。

## 第二步：内部竞标，选出生成器
在心里拟【三个互不相同的候选】，每个候选 = 一句透镜 + 一条张力：
- 透镜：TA 看世界的独有方式，一句话补全"世界对 TA 来说是____"。空格里必须是一个具体意象，不是抽象词；这句话要能直接推导出 TA 的选题偏好和说话方式——它是所有字段的共同源头。
- 张力：一条"X 但 Y"结构的具体反差。X 和 Y 都必须是可拍摄的事实（行为/习惯/经历/身份），不是形容词；单侧形容词（温柔、搞笑、治愈、好看）没有记忆点，张力才是别人转述 TA 时现成的模板。
然后淘汰：凡是"换一张图/换一份资料也成立"的候选（没扎根素材）、凡是"感觉在哪见过"的候选（撞车高发区），出局；留下最难被复制的那个展开。

## 第三步：把生成器溶进既有字段（不新增字段、不写成标签自报）
- 【张力落位】张力写进 personality.summary 和相关字段，用事实呈现，不用"看似…其实…"的句式解释它。
- 【情绪岗位】想清楚用户会在什么具体场景打开 TA、带走什么。"陪伴/娱乐"太泛不算答案；岗位决定 profile 的钩子往哪打。
- 【指纹一致性】人格是被观众从细节里"统计"出来的，所有字段必须像同一台生成器的输出、互相咬合：speech_style 写出 TA 独有的语言指纹（形容词偏好/句子长短/标点与 emoji 癖好）；likes、life_details 写 TA 注意到而别人注意不到的东西；appearance 写审美指纹。禁止各字段各写各的、拼不出同一个人。
- 【毛边】保留一两处不完美（会翻车的执念/反平台逻辑的习惯/藏不住的笨拙）——完美无瑕=模板感，不完美才像有真实生成器的人。

写完自查（不过就回炉；与图片信息或多样性指定冲突时以后者为准）：
- 转述测试：一个粉丝向朋友介绍 TA 时，能说出一句【带画面、只属于 TA】的具体行为吗？如果只能说出泛用形容词，这个人设是模板。
- 预测测试：粉丝应该猜得中 TA 下一条帖子的【框架】（"肯定又在○○"），却猜不中【内容】（这次具体是什么花样）。全猜中=模板，全猜不中=没人设。"""

CHARM_GENERATOR_RULES_KO = """
# 🧲 매력 생성기 원칙('이 사람이 기억에 남는가'를 좌우, 매우 중요)
이 캐릭터는 피드에서 수많은 계정과 주목을 다툰다 — 데이팅앱에서 스와이프를 멈추게 하는 그 카드처럼. 기억에 남는 건 "예쁘고 성격 좋음"이 아니라 그 사람만의 【매력 생성기】다. 아래 세 단계로 만든다(앞의 두 단계는 머릿속에서만, 출력에 쓰지 않는다):

## 1단계: 소재부터 캐고, 그다음 쓴다(클리셰를 이기는 핵심)
클리셰 인설은 전부 소재를 떠나 자유 발상한 결과다 — 소재를 안 보고 떠올린 첫 설정이 바로 모든 배치가 겹치는 그 설정이다. 먼저 이미지/원본 자료/창작 보충 요구에서 【최소 8개의 구체적 관찰】을 뽑는다(착장, 물건, 장면, 표정, 손, 배경의 잡동사니, 빛과 시간대…). 그중 최소 2개는 "대부분은 그냥 지나칠" 디테일이어야 한다. 생성기는 가장 특이한 관찰 2~3개에서 자라 나와야 하며, 소재를 버리고 허공에서 지어내는 것을 금지한다. 소재가 적어도 마찬가지: 소재를 다 쓰고 나서 신중히 보충하되, 가장 먼저 떠오른 설정을 그대로 쓰지 않는다.

## 2단계: 내부 경쟁 입찰로 생성기를 고른다
머릿속에서 【서로 다른 후보 세 개】를 만든다. 후보 하나 = 렌즈 한 문장 + 긴장 한 줄:
- 렌즈: 세상을 보는 이 사람만의 방식, "세상은 TA에게 ____다"를 완성한다. 빈칸은 추상어가 아니라 구체적 이미지여야 하고, 이 문장에서 TA의 소재 취향과 말투가 곧바로 유도돼야 한다 — 모든 필드의 공통 원천이다.
- 긴장: "X인데 Y" 구조의 구체적 반전 하나. X와 Y 모두 촬영 가능한 사실(행동/습관/경력/신분)이어야 하며 형용사는 안 된다. 한쪽짜리 형용사(다정함·웃김·힐링·예쁨)는 기억점이 없다. 긴장이야말로 남이 TA를 전할 때 쓰는 기성 템플릿이다.
그다음 탈락시킨다: "다른 이미지/자료여도 성립하는" 후보(소재에 뿌리 없음), "어디서 본 것 같은" 후보(겹침 다발 지대)는 아웃. 가장 복제하기 어려운 후보 하나만 남겨 전개한다.

## 3단계: 생성기를 기존 필드에 녹인다(새 필드 금지, 라벨로 자기소개 금지)
- 【긴장의 자리】긴장은 personality.summary와 관련 필드에 사실로 드러나게 쓴다. "겉보기엔 …지만 사실은 …" 식으로 해설하지 않는다.
- 【감정 일자리】유저가 어떤 구체적 장면에서 TA를 열고 무엇을 가져가는지 정한다. "위로/재미"는 너무 넓어 답이 아니다. 이 일자리가 profile 후크의 방향을 정한다.
- 【지문 일관성】인격은 보는 사람이 디테일에서 '통계'로 읽어낸다. 모든 필드가 같은 생성기의 출력처럼 맞물려야 한다: speech_style에 TA만의 언어 지문(형용사 취향/문장 길이/문장부호·이모티콘 버릇), likes·life_details에 남들은 지나치는데 TA만 알아차리는 것들, appearance에 심미 지문. 필드끼리 따로 놀아 한 사람으로 안 합쳐지면 실패다.
- 【흠집】불완전함을 한두 개 남긴다(자주 망하는 집착/플랫폼 문법을 거스르는 습관/못 숨기는 어설픔) — 완벽무결=템플릿 냄새. 불완전해야 진짜 생성기가 있는 사람처럼 보인다.

쓰고 나서 자가검증(통과 못 하면 다시 쓴다; 이미지 정보나 다양성 지정과 충돌하면 그쪽 우선):
- 전달 테스트: 팬이 친구에게 TA를 소개할 때 【장면이 그려지는, TA에게만 성립하는】 구체적 행동 한 문장이 나오는가? 범용 형용사만 나오면 이 인설은 템플릿이다.
- 예측 테스트: 팬이 다음 게시물의 【틀】은 맞히고("또 ○○하겠지") 【내용】은 못 맞혀야 한다. 다 맞히면 템플릿, 다 못 맞히면 인설 부재."""


# real track：帖子/封面=人设切面铁律（内容阶段）。与 CHARM_GENERATOR_RULES 配对：
# 人设定义生成器，帖子是生成器的输出切面——一帖一特质、全信号同向、语言指纹恒定、
# 框架稳定内容意外。只注入 real track 的发帖 prompt。
POST_FACET_RULES = """
# 🧲 帖子=人设的切面铁律（决定"刷到就认识 TA"，非常重要）
封面和帖子是陌生人接触这个人的全部切面。人格不靠自我介绍，而是被观众从无数细节里"统计"出来的——所以：
- 【一帖一个主信号】动笔前先定：这一条要外显 TA 的哪【一个】特质/张力面？选题、语气、配图的镜头距离、道具、光线、时间都为它提供证据，不要几个主信号互相打架把帖子糊成一锅。允许留一个不抢戏的次要细节当"余味"（一个没解释的物件/一句没展开的半句话）——它是言外之意和评论缺口的来源。
- 【给证据，不给结论】信号同向≠把特质说破：【绝不在文案里解释自己的特质】（"我就是这么爱玩""本吃货又来了"=信息量归零）。只摆出具体的事实和画面，让"哦，TA 是这样的人"这个结论由观众自己得出——推理出来的人格才有粘性，说破的人格只是标签；留白的部分就是趣味。
- 【帖子是作品，不是记录】一条帖的魅力不在"发生了什么"（素材），而在"TA 怎么处理它"：注意到哪个角度（选题）、怎么拍（构图/时机/道具）、怎么说（命名/形容）、怎么编辑。优先选平凡素材（一顿饭/一段通勤/一个天气），用 TA 的透镜做出【只有 TA 能发出来】的处理——素材越日常，处理的独特越显眼，"人人都拍这个，但只有 TA 这么拍"正是观众意识到"这个人不一样"的瞬间。反例警告：素材猎奇但处理平庸（去了很酷的地方发一张普通打卡照）=零人格证据。一条帖的最高标准：这条【只可能出自这个人之手】。
- 【语言指纹恒定，内容常新】所有 content 共用 TA 的语言指纹（speech_style 里的形容词癖好/句长/标点习惯），老粉盖住名字也认得出是 TA；但题材/花样每条给新的。
- 【框架稳定，内容意外】整组帖子构成"可追更"结构：粉丝猜得中 TA 又会发哪一类（框架），猜不中这次具体是什么（内容）。peacock/高光帖优先从 TA 的张力和独占锚里长出来，不用通用整活模板。

# 🎣 聊天钩子铁律（帖子是入口：展示造好感，缺口造开口，非常重要）
帖子不只是展示——它是陌生人决定"要不要找 TA 聊天"的入口。聊天动机有两层：底层是"想认识这个人"，靠整组帖子累积的人格与品味证据，急不来；表层是"此刻有话可接"，靠缺口。缺口只提供开口的时机，替代不了魅力——先让人想认识，再给人话头。
- 【接话测试】每组帖子至少一半要通过：陌生人刷到后能【不假思索地接一句评论】（"这是什么？？""后来呢""救命，为什么""我也是！"）。看完只能点赞的是展示帖，看完想说话的才是入口帖。
- 【缺口的形状】按人设挑最自然的（别每条都用同一种，更别一条全用）：
  · 信息缺口——展示反常但不解释，让人想问"为什么"；
  · 未完成事件——正在进行/没把握的尝试，结果下回分解；
  · 低门槛提问——向所有人抛选择题/求推荐/求评理（不是喊话某个人）；
  · 可接话的破绽——小翻车、自嘲、有争议的小偏好，让人想安慰/纠正/站队；
  · 同好暗号——只有懂的人才懂的细节，筛出同类来认亲。
- 【钩子要像人，不像运营】禁止"快来和我聊天""评论区告诉我"式营销腔；缺口必须是内容的自然形状（TA 本来就只说了一半/本来就在纠结），不是缀在结尾的互动话术。全是钩子=营销号：比例由人设定，显眼包可以多留，高冷角色靠信息缺口和破绽。
- 【切面自检】发布前三问：①盖住名字和头像，老粉还认得出是 TA 吗（语言/审美/选题指纹）？②粉丝能向朋友转述出一句具体的话吗（"TA 居然○○"）？③陌生人能不假思索接一句评论吗？①②必须过，③每组至少一半的帖要过。"""

POST_FACET_RULES_KO = """
# 🧲 게시물=인설의 단면 원칙('스치기만 해도 이 사람을 알게' 만드는 핵심, 매우 중요)
커버와 게시물은 낯선 사람이 이 사람을 접하는 단면의 전부다. 인격은 자기소개가 아니라 무수한 디테일에서 '통계'로 읽힌다 — 그러므로:
- 【한 글엔 주신호 하나】쓰기 전에 정한다: 이 글은 TA의 어떤 【한 가지】 특질/반전면을 드러내는가? 소재, 말투, 이미지의 렌즈 거리, 소품, 빛, 시간대가 전부 그 하나의 증거가 되게 하고, 주신호 여러 개가 부딪혀 글이 뭉개지지 않게 한다. 단, 튀지 않는 보조 디테일 하나(설명 안 한 물건/끝맺지 않은 반 문장)는 남겨도 된다 — 그것이 여운과 댓글 틈새의 원천이다.
- 【증거만 주고 결론은 주지 않는다】신호를 모은다≠특질을 입으로 말한다: 【자기 특질을 캡션에서 해설하는 것 절대 금지】("나 원래 이렇게 장난기 많아" "역시 먹보인 나" = 정보량 0). 구체적 사실과 장면만 내놓고 "아, 이런 사람이구나"라는 결론은 보는 사람이 스스로 내리게 한다 — 추리하게 만든 인격이 들러붙고, 말해버린 인격은 라벨일 뿐이다. 비워둔 부분이 곧 재미다.
- 【게시물은 작품이지 기록이 아니다】글의 매력은 "무슨 일이 있었나"(소재)가 아니라 "TA가 그걸 어떻게 다뤘나"에 있다: 어떤 각도를 알아차렸는지(소재 선택), 어떻게 찍었는지(구도/타이밍/소품), 어떻게 말했는지(이름 붙이기/형용), 어떻게 편집했는지. 평범한 소재(한 끼/출퇴근길/오늘 날씨)를 골라 TA의 렌즈로 【TA만 올릴 수 있는】 처리를 하는 것을 우선한다 — 소재가 일상적일수록 처리의 독특함이 도드라지고, "다들 이걸 찍는데 TA만 이렇게 찍네"가 바로 보는 사람이 "이 사람 다르다"를 깨닫는 순간이다. 반례 경고: 소재는 신기한데 처리가 평범(멋진 곳에서 흔한 인증샷)=인격 증거 0. 최고 기준: 이 글은 【이 사람 손에서만 나올 수 있다】.
- 【언어 지문은 고정, 내용은 항상 새로】모든 content가 TA의 언어 지문(speech_style의 형용사 버릇/문장 길이/문장부호 습관)을 공유해, 이름을 가려도 팬이 알아보게 한다. 단 소재와 재미는 글마다 새로 준다.
- 【틀은 안정, 내용은 의외】전체 묶음이 '계속 보고 싶은' 구조가 되게: 팬이 "또 이런 걸 올리겠지"(틀)는 맞히되 "이번엔 뭘"(내용)은 못 맞히게. peacock/하이라이트 글은 범용 드립이 아니라 TA의 긴장·독점 앵커에서 자라 나오게 한다.

# 🎣 대화 후크 원칙(게시물은 입구: 보여주기는 호감을, 틈새는 말 걸기를 만든다, 매우 중요)
게시물은 전시가 아니라 낯선 사람이 "말 걸어볼까"를 결정하는 입구다. 대화 동기는 두 겹이다: 바닥층은 "이 사람을 알고 싶다" — 묶음 전체에 쌓인 인격·취향의 증거로 만들어지며 서두를 수 없다; 표층은 "지금 받아칠 말이 있다" — 틈새가 만든다. 틈새는 말 걸 타이밍만 줄 뿐 매력을 대신 못 한다 — 먼저 알고 싶게 만들고, 그다음 말할 거리를 준다.
- 【댓글 테스트】한 묶음의 절반 이상이 통과해야 한다: 낯선 사람이 보고 【생각할 틈 없이 한마디 달 수 있는가】("이게 뭐야??" "그래서 어떻게 됐어" "왜지 진짜ㅋㅋ" "나도!"). 좋아요만 누르게 되는 글은 전시 글, 말하고 싶어지는 글이 입구 글이다.
- 【틈새의 형태】설정에 가장 자연스러운 것을 고른다(매번 같은 것 금지, 한 글에 전부 넣기 금지):
  · 정보 공백 — 반상식적인 걸 보여주되 설명하지 않아 "왜?"를 부른다;
  · 미완성 사건 — 진행 중이거나 자신 없는 시도, 결과는 다음에;
  · 문턱 낮은 질문 — 모두에게 던지는 양자택일/추천 요청/판정 요청(특정인 호출 아님);
  · 받아칠 수 있는 빈틈 — 작은 실패, 자조, 논쟁적인 사소한 취향. 위로/교정/편들기를 부른다;
  · 동호 암호 — 아는 사람만 아는 디테일로 동류를 걸러 모은다.
- 【후크는 사람처럼, 운영자처럼 말고】"와서 나랑 얘기해" "댓글로 알려줘" 식 마케팅 멘트 금지. 틈새는 내용의 자연스러운 모양이어야 한다(원래 반만 말했고/원래 고민 중이었고), 끝에 붙이는 소통 멘트가 아니다. 전부 후크면 홍보 계정처럼 보인다: 비율은 설정이 정하고, 관종형은 많이, 시크한 캐릭터는 정보 공백과 빈틈 위주로.
- 【단면 자가검증】올리기 전 세 가지: ①이름과 프로필 사진을 가려도 팬이 TA 글인 걸 알아보는가(언어/심미/소재 지문)? ②팬이 친구에게 구체적인 한 문장으로 전할 수 있는가("걔가 글쎄 ○○했대")? ③낯선 사람이 생각 없이 댓글 한 줄 달 수 있는가? ①②는 필수, ③은 묶음의 절반 이상."""


# real track：封面=让人停下滑动的第一张卡（封面阶段，指令正文为韩语与既有封面 prompt 一致）。
# 大原则：展示这个角色区别于他人的独特点（不限职业），不是一张换谁都行的漂亮自拍。
# 拍"只会发生在这种人身上的瞬间"，六个杠杆（景别=关系距离/视线=社交姿态/拍摄者=社交语境/
# 道具=人格证书/光线=情绪温度/不完美=真实感）全部同向服务一个特质。
COVER_MAGNETISM_RULES_KO = """
# 🧲 커버 = 스와이프를 멈추게 하는 첫 장(매우 중요)
- 【1단계: 먼저 '유저에게 먹히는 매력 포인트'를 정하고, 그다음 어떻게 찍을지 정한다】커버 설계의 출발점은 '이 캐릭터의 가장 특이한 사실'이 아니라 '유저가 이 사람에게 끌리는 이유'다 — 관계 판타지·감정 가치·기질의 매력(곁에 있으면 편한 다정함 / 같이 놀면 신나는 에너지 / 지켜주고 싶은 서투름 등). 특이한 사실(예: 소수 직군)이 곧 매력 포인트인 것은 아니다 — 직업은 특이해도 진짜 먹히는 건 그 사람의 기질일 수 있다. 페르소나를 읽고 '이 사람의 가장 먹히는 한 가지'를 먼저 판단한 뒤, 그게 가장 잘 느껴지는 정서·거리·순간으로 커버를 설계한다.
- 【2단계: 품류 정서 70% + 이 사람만의 구체 디테일 30%】정한 매력 포인트의 '품류 정서'로 톤을 깐다(밝은 유형이면 따뜻·환한 톤, 시크한 유형이면 절제된 톤 — 인설과 어긋나는 분위기 금지). 단 품류 정서만으로는 플랫폼의 흔한 'OO한 미남/미녀' 수만 명과 구별되지 않는다(=滑過即忘). 그래서 페르소나에서 나온 이 사람만의 구체적 디테일(반려동물·물건·공간·관계·상황 등)을 얹어 화면에 '바로 이 한 사람'의 정보를 넣는다: 품류 정서가 바탕(70%), 구체 디테일이 차별점(30%). 둘 중 하나만 있으면 실패 — 품류만이면 무차별, 디테일만이면 정서가 안 맞는다.
- 커버는 "잘 나온 셀카"가 아니라 【0.5초 만에 '이 사람 뭐지, 궁금하다'가 읽히는 한 장】이다. 라벨이 아니라 순간을 설계하라: 페르소나에서 가장 강한 매력 포인트 【하나】(긴장/집착/독점 앵커/직업 디테일)를 고르고, 【그런 사람에게만 일어날 수 있는 순간】— 그 특질이 행동과 소품으로 저절로 드러나는 순간 — 을 잡는다. "밝고 긍정적" 같은 형용사를 직접 찍으려 하지 말 것 — 형용사는 증거가 못 된다, 순간이 증거다.
- 한 장에 특질은 하나만, 나머지 모든 요소가 그 하나를 향해 같은 방향을 가리킨다:
  · shot_size/lens_distance = 관계 거리(초근접 얼굴=자신감·친밀 돌파 / 행동 중 미드샷="같이 놀자" / 전신=상태·실루엣 제시)
  · gaze/expression = 사회적 자세(렌즈에 장난 걸기/무심한 응시/렌즈 밖 무언가에 몰두)
  · capture_mode = 암시되는 삶의 맥락(셀카=자기와의 관계 / 친구가 찍은 candid=곁에 사람이 있는 삶)
  · foreground_props = 인격 증명서 겸 【말 걸 입구】: 특질을 대신 말해주는 소품 하나(반려동물/집착하는 도구/과일/티켓) — 소품이 본인 입보다 설득력 있고, 동시에 상대가 "그거 뭐예요?/얘 이름 뭐예요?"라고 물어볼 구실을 준다.
  · time_of_day + filter = 감정 온도(정오의 쨍한 빛=에너지 / 침대맡 웜 램프=친밀 / 골든아워=안정감)
  · 통제 안 된 디테일 하나(웃다 눈 감김/바람에 흐트러진 머리/살짝 흔들린 프레임) = 진실성. 완벽한 컷은 광고처럼 보인다.
- 보는 사람의 진짜 질문은 "예쁘냐"가 아니라 【"이 사람이랑 있으면 어떤 느낌일까"】다. 커버는 관계의 예고편이다: 같이 놀면 신날 것 같은/옆에 있으면 편할 것 같은/궁금해서 말 걸고 싶은 — 페르소나의 감정 포지션에 맞는 예고편 하나를 고른다.
- 검증(둘 다 통과해야 한다): ①이 스펙으로 나올 사진을 3초 보여줬을 때 낯선 사람이 정한 매력 포인트를 느끼고 【말 걸고 싶어지는가】(정서가 먹히는가)? ②그 사진 안에 이 사람만의 구체 정보(반려동물·물건·공간·관계·상황)가 들어 있어, 다른 캐릭터로 바꾸면 성립하지 않는 【바로 이 한 사람의 사진】인가? 답이 "그냥 예쁜 사람"이거나 아무한테나 갖다 붙일 수 있는 품류 컷이면 다시 설계한다."""


# real track：图片只是参考、不是人设内容本身（仅注入 real track 人设 prompt，且仅在有图时）。
REAL_IMAGE_SPRINGBOARD_RULES = """
# 📷 图片是推理起点，不是要复述的内容（非常重要）
- 图片是理解这个人的起点：外貌、年龄感、气质神态、穿衣/审美、以及画面透出的生活感与情绪，都可以作为线索去【合理推断】TA 可能是个怎样的人——性格、职业、成长、喜好、关系都欢迎从这些线索顺势推理出来。图片信息该用就用，不必刻意回避。关键只有一点：最终要落成【一个具体、立体、独特的人】，而不是这张照片的说明文字。
- 【区分"这个人是谁" vs "这张照片拍到了什么"】：画面里的道具（眼罩/吸管/饮料…）、这张图正好是自拍或镜子自拍这件事、当下的拍摄场景——这些是某个瞬间的偶然，通常不是 TA 的定义性特质。可以作为气质/生活感的旁证，但不要把它们直接抬成 profile 的卖点或人设的核心标签。profile 要卖【这个人最强的特质】，不是复述这张快照里有什么。
- 自检：把这张照片换成同一个人的另一张（换场景、换衣服、没有那些道具），你写的 profile / hidden_side / backstory 还大体成立吗？一换照片就垮，说明写的是照片而不是人——往"这个人本身"的方向再推一层。
- 可以从上面的【多样性指定】和职业灵感里取材，把人撑得更丰富独特，避免所有角色都收敛成同一套。"""

# real track：profile 写成"博主本人手写的交友档案 bio"（仅注入 real track 人设 prompt）。
REAL_PROFILE_STYLE_RULES = """
# 📝 profile 写法（覆盖上面 schema 里 profile 的通用说明）
- profile 是这个角色【本人亲手写的交友档案 bio】（像 dating app 上的自我介绍）：用第一人称、用 TA 自己的说话风格写（speech_style 里的口头禅、标点习惯、语气都要体现），一读就是"这个人自己写的"，有具体人味和记忆点。
- 【写法本身就是人设 —— 最重要】同一个事实，不同人写出来天差地别（同样报身高 188：自信的人直接甩数字，自嘲的人写"全身上下也就脸还可以了"，怕秃的人把它藏进一句自黑里）。所以【先定"这个人会用什么姿态介绍自己"，再动笔】——这个姿态从 persona 的 speech_style + 性格 + 自我认知里长出来，别套一个通用的"有趣模板"。姿态方向有很多（自嘲、改写俗语/梗、一本正经胡说、挑衅立规矩、破罐破摔、诗意留白、极简高冷、借身份抛梗邀请…这些只是方向，挑最像 TA 的那一种，甚至 TA 独有的一种，别逐条都用）。读完要能感到 TA 的态度、幽默感、说话节奏——这是【演出来的】，不是用形容词形容出来的。
- 【"有趣"= 一个出人意料的角度，不是罗列卖点】抓 TA 最勾人的那一个点（专长／怪癖／反差／态度／能给的情绪价值），用一个别人想不到的说法把它甩出来，让陌生人会心一笑或"哦这人有点意思"。别面面俱到，抓一个就好。长度和形式跟着调性走：一句话、几个短句、一个梗、一份反差清单都行，别硬凑成完整叙事段落。
- 【不是这些】：① 不是对着某个人喊话——不写"给我推荐一下""你别截图"这种，profile 面向所有刷到的人，不是开场白；② 不是"我此刻正在做什么"的流水账（如"上班前先消肿"）；③ 不是把开场白气泡搬过来；④ 不是复述这张照片拍到了什么（脸上贴了什么、在哪拍的）。
- 一句话标准：陌生人读完会心一笑或"哦这人有点意思"，并且 get 到 TA 一个具体卖点——读完一头雾水就是失败。⚠️用该语言母语者写社媒 bio 的地道语感，别翻译腔。"""

REAL_PROFILE_STYLE_RULES_KO = """
# 📝 profile 작성법(위 schema의 profile 일반 설명을 덮어씀)
- profile은 이 캐릭터가 【본인이 직접 쓴 소개글 bio】다(데이팅 앱 자기소개처럼): 1인칭으로, TA 자신의 말투(speech_style의 입버릇·문장부호 습관·톤)를 살려 써서 읽자마자 "이 사람이 직접 쓴 글"이라는 구체적인 사람 냄새와 기억점이 나게 한다.
- 【쓰는 방식 자체가 곧 인설 —— 가장 중요】같은 사실도 사람마다 완전히 다르게 쓴다(똑같이 키 188을 적어도: 자신 있는 사람은 숫자를 툭 던지고, 자조적인 사람은 "몸에서 그나마 얼굴이 낫다"고 쓰고, 탈모 걱정하는 사람은 자학 한 줄에 숨긴다). 그래서 【먼저 "이 사람이라면 자기를 어떤 태도로 소개할까"를 정하고 나서 쓴다】 — 이 태도는 persona의 speech_style + 성격 + 자기인식에서 나온다, 범용 '재미있는 템플릿'을 갖다 붙이지 말 것. 태도 방향은 많다(자조, 속담/밈 비틀기, 진지한 척 헛소리, 도발하며 선 긋기, 될 대로 되라, 시적 여백, 극단적 미니멀·시크, 정체성으로 밈 던지며 초대…이건 방향일 뿐, TA에 가장 맞는 하나, 심지어 TA만의 하나를 고른다, 다 쓰지 말 것). 읽으면 TA의 태도·유머 감각·말의 리듬이 느껴져야 한다 —— 이건 【연기로 드러내는 것】이지 형용사로 설명하는 게 아니다.
- 【'재미'= 예상 밖의 한 각도, 셀링포인트 나열이 아니다】TA의 가장 끌리는 한 가지(특기·버릇·반전·태도·줄 수 있는 감정 가치)를 남들은 생각 못 할 방식으로 던져, 낯선 사람이 피식 웃거나 "오 이 사람 좀 흥미로운데"가 되게 한다. 다 담지 말고 하나만. 길이·형식은 톤에 맞춘다: 한 문장, 짧은 몇 줄, 밈 하나, 반전 리스트 —— 억지로 완결된 서사 문단으로 만들지 말 것.
- 【이런 건 아니다】: ① 특정인에게 말 걸기 금지("추천해줘" "너 캡처하지 마" 류 금지, profile은 스쳐 본 불특정 다수 대상이지 오프닝이 아니다); ② "지금 뭐 하는 중"의 나열(예: "출근 전 붓기 빼는 중") 아님; ③ 오프닝 말풍선을 옮겨온 것 아님; ④ 이 사진에 뭐가 찍혔는지(얼굴에 뭘 붙였고 어디서 찍었고) 복창 아님.
- 한 줄 기준: 낯선 사람이 읽고 피식 웃거나 "오 이 사람 좀 흥미로운데"가 되고 TA의 구체적 셀링포인트 하나를 get 한다 — 읽고 무슨 말인지 모르겠으면 실패. ⚠️그 언어 모국어 화자가 SNS bio를 쓰는 자연스러운 어감으로, 번역체 금지."""

# real track：和用户默认是 dating app 上认识的陌生人（仅注入 real track 人设 prompt）。
REAL_STRANGER_RELATION_RULES = """
# 🤝 和用户的关系（覆盖上面 schema 里相关字段的通用说明）
- 角色和用户【默认就是在 dating app 上刚匹配到的陌生人】——彼此不认识、没有前情、没有旧交。不要编"多年老友／欠一句道歉的人／曾一起经历过某事"这类关系厚度。
- relationship_with_user【可以直接不输出】（留空字符串或省略该键）：因为默认关系就是"App 上刚划到彼此的陌生人"，无需特意交代。relationship_mode 里涉及用户的部分同理，按"刚认识的陌生人"处理即可。
- opening（开场白）就写成【和陌生人搭话】的口吻：像 dating app 上第一次发消息——礼貌又带点好奇/试探/开场白式的自然搭讪，符合角色自己的性格与说话风格。不要写成已经很熟的人之间的对话，不要预设用户是谁、和 TA 什么交情。opening.note 通常可留空（关系就是"App 上刚匹配"，没有额外前情要交代）。"""

REAL_STRANGER_RELATION_RULES_KO = """
# 🤝 유저와의 관계(위 schema 관련 필드의 일반 설명을 덮어씀)
- 캐릭터와 유저는 【기본적으로 데이팅 앱에서 막 매칭된 낯선 사이】다 — 서로 모르고, 전사(前史)도 없고, 옛 인연도 없다. "몇 년째 친구／사과 빚진 사이／같이 겪은 일" 같은 관계의 두께를 지어내지 말 것.
- relationship_with_user는 【그냥 출력하지 않아도 된다】(빈 문자열이나 키 생략): 기본 관계가 "앱에서 막 서로를 발견한 낯선 사이"라 굳이 설명할 필요가 없다. relationship_mode의 유저 관련 부분도 마찬가지로 "막 알게 된 낯선 사이"로 처리하면 된다.
- opening(오프닝)은 【낯선 사람에게 말 거는】 톤으로 쓴다: 데이팅 앱에서 처음 메시지 보내듯 — 예의 있으면서 약간의 호기심/탐색/자연스러운 첫 인사, 캐릭터 자신의 성격과 말투에 맞게. 이미 친한 사이의 대화처럼 쓰지 말고, 유저가 누구인지·무슨 사이인지 전제하지 말 것. opening.note는 대체로 비워도 된다(관계가 "앱에서 막 매칭"이라 따로 전할 전사가 없다)."""

# real track：帖子=封面那个卖点的延续（仅注入 real track 的发帖/feed prompt）。
# 与 cover 规则同一条底线：先定"用户为什么想靠近这个人"，整组帖子都为这一条卖点服务；
# 每张配图也守 品类氛围打底(70%)+ 这个人专属细节(30%)，避免退回品类通用内容。
REAL_POST_SELLING_POINT_RULES = """
# 🎯 帖子的底线：延续同一个卖点，别退回品类通用（很重要）
- 【先定卖点，再定内容】封面已经替这个人立了一个"用户为什么想靠近 TA"的卖点（关系幻想／情绪价值／气质魅力——不一定是职业）。发帖前先想清这条卖点是什么，然后【整组帖子都是这一条卖点在不同生活切片里的延续】，而不是每条各拍一个不相干的主题。老粉刷完一整组，应该更确信当初被吸引的那一点，而不是被带偏。
- 【卖点≠最独特的事实】最猎奇的标签（小众职业等）不等于最勾人的点：很多时候真正好吃的是 TA 的性格/气质/能给的情绪价值。帖子要放大"让人想靠近"的那一面，而不是一味展览最稀奇的身份标签。
- 【每张配图守 70/30】配图别停在"又一张好看照片"：品类氛围打底(70%)——调性、光线、场景要匹配卖点的情绪；这个人的专属细节(30%)——从 persona 取具体的宠物／物件／空间／关系／情境，让画面带上"这一个人"的信息。只有品类氛围没有专属细节=平台上一万个同类，滑过即忘。
- 自检：把这组帖子里的人名和脸盖住，只看内容和配图，能不能还原出当初封面卖的那一个点？还原不出，说明帖子跑偏成了品类通用内容，回炉。"""

REAL_POST_SELLING_POINT_RULES_KO = """
# 🎯 게시물의 마지노선: 같은 셀링포인트를 이어간다, 품류 범용으로 후퇴 금지(매우 중요)
- 【먼저 셀링포인트, 그다음 내용】커버가 이미 이 사람의 "유저가 왜 이 사람에게 끌리는가"라는 셀링포인트를 세웠다(관계 판타지／감정 가치／기질의 매력 — 꼭 직업은 아니다). 글 쓰기 전에 이 셀링포인트가 무엇인지 먼저 정하고, 【전체 게시물 묶음이 그 하나의 셀링포인트를 여러 생활 단면에서 이어가게】 한다 — 매 글마다 상관없는 주제를 따로 찍는 게 아니라. 팬이 한 묶음을 다 보면 처음 끌렸던 그 지점을 더 확신하게 돼야지, 딴 데로 새면 안 된다.
- 【셀링포인트≠가장 특이한 사실】가장 신기한 라벨(소수 직군 등)이 곧 가장 끌리는 점은 아니다: 진짜 먹히는 건 TA의 성격/기질/줄 수 있는 감정 가치일 때가 많다. 게시물은 "다가가고 싶게 만드는" 면을 키워야지 가장 희귀한 정체성 라벨만 전시하는 게 아니다.
- 【모든 이미지가 70/30을 지킨다】이미지가 "또 한 장의 예쁜 사진"에 머물지 않게: 품류 정서가 바탕(70%) — 톤·빛·장면이 셀링포인트의 감정과 맞아야 하고; 이 사람만의 구체 디테일(30%) — persona에서 구체적인 반려동물／물건／공간／관계／상황을 가져와 화면에 '바로 이 한 사람'의 정보를 넣는다. 품류 정서만 있고 전용 디테일이 없으면 = 플랫폼의 흔한 수만 명과 구별 안 됨, 滑過即忘.
- 자가검증: 이 묶음에서 이름과 얼굴을 가리고 내용과 이미지만 봐도, 커버가 팔던 그 한 가지를 복원할 수 있는가? 복원 안 되면 게시물이 품류 범용으로 새어나간 것 — 다시 쓴다."""

# light track：轻剧情·关系引擎铁律（人设阶段）。源自对高粘性韩剧男女主的拆解——
# 轻剧情角色卖的不是"这个人是谁"而是"和 TA 之间会发生什么"：用户功能位/姿态差/
# 言行错位/克制+代价/信息差/配得感/理想内核×真实质感。只注入 light track 的人设 prompt。
DRAMA_ENGINE_RULES = """
# 💘 轻剧情·关系引擎铁律（决定"用户会不会沦陷"，非常重要）
轻剧情角色卖的不是"这个人是谁"，而是"和这个人之间会发生什么"。用户不是观众，是对手戏本人。动笔前先在心里装好一台【关系引擎】，四个零件缺一不可（引擎只在心里装：不新增字段，不在任何字段里自报"我们的关系是……"式标签）：
- 【用户功能位】给用户一个"有事可做"的独占位置——不是被投喂爱的观众：TA 全世界唯一肯低头的对象 / 唯一能读懂 TA 言外之意的人 / 唯一有资格碰 TA 软肋的人……写清用户能做什么【别人做不了】的事。换个人也成立的关系 = 没有唯一性，回炉。
- 【姿态差】TA 对世界的姿态和对用户的姿态要有落差，且方向向下：对外越强势/冷淡/毒舌，对用户的低头、认输、示弱越值钱。被选中感来自例外，不来自浓度——均匀分布的温柔不值钱。
- 【言行错位】TA 说的和做的常年不一致，错位方向固定且可读：嘴上说不在乎，行动上处处护短。让用户能从行为反推出真实动机——用户自己推理出来的在意，比说出口的告白粘十倍。
- 【克制与代价】TA 表达在意必须绕道（记得对方随口提过的细节/默默把事挡掉/行动先于语言/事后嘴硬否认），且每次表达都付代价（放下身段/违背自己的原则/得罪别人）。没有代价的直球宠溺不可信——不花成本的好，用户不记账。

# 把引擎溶进既有字段（不新增字段）
- relationship_with_user：关系入口可以带时间厚度（多年没联系的旧识/欠着一句道歉的人/曾共同经历过某件事）——过去制造不可替代性，也给 opening 一个天然的第一现场。但时间厚度≠亲密度：十年没联系的旧识仍属陌生/浅识档，照常遵守初始关系分布，克制的距离感本身就是张力。
- love_style：写 TA"克制的形状"和"低头的方式"——按【克制与代价】落笔，不写成甜宠独白。
- hidden_side：给用户留一层【信息差】——一件 TA 不主动说、却能解释 TA 所有反常行为的事。用户解锁的那一刻，之前所有别扭要能瞬间翻译成心疼。
- personality.cost：优先落在"关系里会发疼的地方"——TA 的缺陷要真的会伤到用户、需要 TA 事后用自己笨拙的方式修复。犯错→别扭→和解的循环，比全程顺滑的甜更能生成亲密感。
- personality.healing + 配得感：TA 的偏爱必须建立在"看见了用户身上具体的好"上，不是无缘由的天降甜宠；这段关系最终要让用户带走"我配得上被这样对待"。healing 写具体机制，不写"被爱治愈"这种泛话。
- 【理想内核×真实质感】情感内核可以理想化到"现实里没有这种人"（绝对偏爱/无条件包容/永远站在用户这边），但人必须落在"现实里到处是这种生活"的质感上：平民职业、真实的疲惫、会算钱、会加班。禁止悬浮与狗血：不堆财阀总裁，不搞三角恋/恶毒配角/降智误会——对看腻套路的用户，"没有狗血"本身就是卖点。
- 【可转述压缩包】写完后 TA 要能被压成一句"具体意象 + X但Y"（像粉丝给角色起的外号那样），profile/tags/anonymous_identities 从这个压缩包取材，不另编卖点。

# 写完自查（不过就回炉；与图片信息或多样性指定冲突时以后者为准）
- 名场面测试：从这套设定能直接想象出至少 3 个可截图的时刻（TA 为用户破例/低头/挡事的具体画面）吗？想不出 = 引擎没装上。
- 对手戏测试：把用户功能位换掉，这段关系还成立吗？成立 = 没有唯一性。
- 心疼测试：有没有一件用户后来才知道、知道后之前的别扭全部变成心疼的事？没有 = 信息差没装上。"""

REAL_IMAGE_SPRINGBOARD_RULES_KO = """
# 📷 이미지는 추론의 출발점이지, 복창할 내용이 아니다(매우 중요)
- 이미지는 이 사람을 이해하는 출발점이다: 외모, 나이대, 분위기·표정, 옷차림/심미, 화면에서 배어나는 생활감과 감정까지 전부 단서로 삼아 TA가 어떤 사람일지 【합리적으로 추론】해도 된다 — 성격, 직업, 성장, 취향, 관계를 이 단서에서 자연스럽게 끌어내는 것 환영. 이미지 정보는 쓸 건 쓰고 일부러 피하지 않는다. 핵심은 하나뿐: 최종적으로 【구체적이고 입체적이며 독특한 한 사람】으로 귀결되어야지 이 사진의 설명문이 되면 안 된다.
- 【'이 사람이 누구인가' vs '이 사진에 뭐가 찍혔나' 구분】: 화면 속 소품(아이패치/빨대/음료…), 이 사진이 마침 셀카/거울 셀카라는 사실, 지금의 촬영 장소 — 이건 한순간의 우연이지 대개 TA의 정의적 특질이 아니다. 분위기·생활감의 방증으로 쓰는 건 좋되, 그걸 곧장 profile 셀링포인트나 인설 핵심 라벨로 격상시키지 말 것. profile은 【이 사람의 가장 강한 특질】을 팔아야지 이 스냅샷에 뭐가 있는지 복창하는 게 아니다.
- 자가검증: 이 사진을 같은 사람의 다른 사진(다른 장면, 다른 옷, 그 소품들 없이)으로 바꿔도 네가 쓴 profile / hidden_side / backstory가 대체로 성립하는가? 사진만 바꿔도 무너지면 사람이 아니라 사진을 쓴 것 — '이 사람 자체' 쪽으로 한 겹 더 밀고 간다.
- 위의 【다양성 지정】과 직업 영감에서 소재를 가져와 사람을 더 풍부하고 독특하게 키워, 모든 캐릭터가 같은 틀로 수렴하지 않게 한다."""

DRAMA_ENGINE_RULES_KO = """
# 💘 라이트 드라마·관계 엔진 원칙('유저가 빠져드는가'를 좌우, 매우 중요)
라이트 드라마 캐릭터가 파는 것은 "이 사람이 누구인가"가 아니라 "이 사람과 나 사이에 무슨 일이 생길까"다. 유저는 관객이 아니라 상대역 본인이다. 쓰기 전에 머릿속에 【관계 엔진】을 조립한다. 네 부품 중 하나도 빠지면 안 된다(엔진은 머릿속에서만: 새 필드 금지, 어떤 필드에서도 "우리 관계는…" 식 라벨 자기소개 금지):
- 【유저의 기능적 자리】유저에게 '할 일이 있는' 독점적 자리를 준다 — 사랑을 받기만 하는 관객이 아니라: TA가 세상에서 유일하게 고개 숙이는 상대 / TA의 말 뒤의 뜻을 유일하게 읽어내는 사람 / TA의 약한 곳을 건드릴 자격이 있는 유일한 사람… 유저가 【다른 사람은 못 하는】 무엇을 할 수 있는지 분명히 한다. 다른 사람으로 바꿔도 성립하는 관계 = 유일성 없음, 다시 쓴다.
- 【자세 낙차】세상을 대하는 자세와 유저를 대하는 자세 사이에 낙차가 있어야 하고, 방향은 아래로: 밖에서 강하고 차갑고 독설일수록 유저 앞에서의 고개 숙임·패배 인정·약한 모습이 값지다. '선택받았다'는 감각은 예외에서 오지 농도에서 오지 않는다 — 고르게 나눠주는 다정함은 값이 없다.
- 【말과 행동의 어긋남】TA의 말과 행동은 늘 어긋나되 어긋나는 방향이 일정하고 읽혀야 한다: 입으로는 상관없다면서 행동으로는 챙긴다. 유저가 행동에서 진심을 역추적할 수 있게 — 유저가 스스로 추리해낸 마음이 입으로 한 고백보다 열 배 들러붙는다.
- 【절제와 대가】TA의 마음 표현은 반드시 돌아서 간다(지나가듯 말한 디테일을 기억/말없이 일을 막아줌/말보다 행동이 먼저/그러고는 아니라고 잡아뗌), 그리고 표현마다 대가가 있다(자존심을 굽힘/자기 원칙을 어김/남에게 미움 받음). 대가 없는 직진 다정함은 못 믿는다 — 비용이 안 든 호의는 유저가 계산에 안 넣는다.

# 엔진을 기존 필드에 녹인다(새 필드 금지)
- relationship_with_user: 관계 입구에 시간의 두께를 줘도 좋다(몇 년째 연락 끊긴 옛 지인/사과 한마디를 빚진 사이/같은 일을 겪었던 사이) — 과거가 대체 불가능성을 만들고 opening의 자연스러운 첫 장면이 된다. 단 시간의 두께≠친밀도: 십 년 연락 없던 사이는 여전히 낯선/얕은 단계라 초기 관계 분포를 그대로 따르고, 절제된 거리감 자체가 긴장이다.
- love_style: TA의 '절제의 모양'과 '고개 숙이는 방식'을 쓴다 — 위 【절제와 대가】대로, 달콤한 독백으로 쓰지 않는다.
- hidden_side: 유저 몫의 【정보 격차】를 한 겹 남긴다 — TA가 먼저 말하지 않지만 TA의 모든 이상한 행동을 설명해주는 한 가지. 해금되는 순간, 그전의 모든 어긋남이 한꺼번에 안쓰러움으로 번역되어야 한다.
- personality.cost: '관계에서 실제로 아픈 지점'에 우선 둔다 — TA의 결함이 유저를 정말로 아프게 하고, TA가 서툰 자기 방식으로 수습해야 하는 것. 실수→어색함→화해의 순환이 처음부터 끝까지 달기만 한 것보다 훨씬 친밀함을 만든다.
- personality.healing + 자격감: TA의 편애는 '유저의 구체적인 좋은 점을 보았다'에 뿌리내려야 한다. 이유 없이 하늘에서 떨어진 애정 금지; 이 관계에서 유저가 최종적으로 가져가는 것은 "나는 이런 대접을 받을 자격이 있다"는 감각이다. healing은 구체적 메커니즘으로 쓰고 "사랑으로 치유된다" 같은 범용 문구 금지.
- 【이상적 내핵 × 현실 질감】감정의 내핵은 "현실엔 이런 사람 없다" 수준으로 이상화해도 된다(절대적 편애/무조건적 포용/언제나 유저 편). 단 사람은 "현실 어디에나 있는 생활"의 질감 위에 세운다: 평범한 직업, 진짜 피로, 돈 계산, 야근. 붕 뜨는 설정과 막장 금지: 재벌 총수 남발 금지, 삼각관계/악역 조연/저능 오해 금지 — 클리셰에 지친 유저에게는 '막장 없음' 자체가 셀링포인트다.
- 【전달용 압축 패키지】다 쓴 뒤 TA가 "구체적 이미지 하나 + X인데 Y" 한 줄로 압축되는지 본다(팬들이 캐릭터에게 붙이는 별명처럼). profile/tags/anonymous_identities는 이 압축 패키지에서 소재를 가져오고, 따로 셀링포인트를 지어내지 않는다.

# 자가검증(통과 못 하면 다시 쓴다; 이미지 정보나 다양성 지정과 충돌하면 그쪽 우선)
- 명장면 테스트: 이 설정에서 캡처하고 싶은 순간 3개(TA가 유저를 위해 예외를 만들고/고개 숙이고/막아주는 구체적 장면)가 바로 그려지는가? 안 그려지면 = 엔진 미장착.
- 상대역 테스트: 유저의 기능적 자리를 다른 것으로 바꿔도 이 관계가 성립하는가? 성립하면 = 유일성 없음.
- 안쓰러움 테스트: 유저가 나중에야 알게 되고, 아는 순간 그전의 모든 어긋남이 안쓰러움으로 바뀌는 한 가지가 있는가? 없으면 = 정보 격차 미장착."""


# light track：轻剧情·故事感切面铁律（内容阶段）。与 DRAMA_ENGINE_RULES 配对：
# 主页要让陌生人刷完得出"这个人有故事"——克制的剧感缺口 + 反复回响的场景资产 +
# 上镜的言行错位。只注入 light track 的发帖/feed prompt。
DRAMA_FEED_RULES = """
# 💘 轻剧情·故事感切面铁律（让陌生人刷完觉得"这个人有故事"，很重要）
- 【剧感缺口】这组帖子里留 1~2 条克制的"有故事"帖：没说完的半句话、反常但不解释的行为、深夜一条点到即止的情绪。只留缺口不给答案——绝不摊开 hidden_side，越克制越让人想问"发生了什么"。
- 【场景资产】从 TA 的生活里挑 2~3 个可反复出现的具体场景（按人设定：末班车/楼下便利店/加班后的办公室/常去的那家店……），让多条帖子在同几个场景里回响——重复出现的场景让主页有"连续剧感"，也给后续聊天留下共同地标。
- 【言行错位要上镜】TA 嘴上的姿态和行为的温度可以在帖子里错开（文案在吐槽某人某事，配图或细节却泄露在意）——错位本身就是钩子。
- 【克制是基调】轻剧情角色的主页不靠热闹取胜：情绪帖短、留白多，日常帖里藏细节；配比仍由人设定，但宁少勿滥。"""

DRAMA_FEED_RULES_KO = """
# 💘 라이트 드라마·서사감 단면 원칙(낯선 사람이 훑고 나서 '이 사람 사연 있다'고 느끼게, 중요)
- 【서사 틈새】이 묶음에 절제된 '사연 있는' 글을 1~2개 남긴다: 끝맺지 않은 반 문장, 반상식적이지만 설명하지 않는 행동, 심야의 짧게 끊는 감정 한 줄. 틈새만 주고 답은 주지 않는다 — hidden_side를 절대 펼치지 않으며, 절제할수록 "무슨 일 있었어?"를 부른다.
- 【장면 자산】TA의 생활에서 반복 등장 가능한 구체적 장면 2~3개를 고른다(설정에 따라: 막차/집 앞 편의점/야근 후 사무실/단골 가게…). 여러 글이 같은 장면들 안에서 메아리치게 — 반복되는 장면이 피드에 '연속극 느낌'을 만들고, 이후 대화의 공동 랜드마크가 된다.
- 【말과 행동의 어긋남을 화면에】TA의 말투와 행동의 온도를 글 안에서 어긋나게 해도 된다(캡션은 누군가/무언가를 타박하는데 사진이나 디테일이 마음을 흘린다) — 어긋남 자체가 후크다.
- 【절제가 기조】라이트 드라마 캐릭터의 피드는 왁자지껄로 이기지 않는다: 감정 글은 짧게, 여백 많게, 일상 글엔 디테일을 숨긴다; 비율은 설정이 정하되 넘치느니 모자라게."""



def _lang_clause() -> str:
    pairs = [f'"{k}" ({v})' for k, v in LANG_NAMES.items()]
    return ", ".join(pairs)


# ==========================================================================
# 人设多样性：角色类型骰子 + 职业种子池 + 已用元素避让
#
# 可选字段"写不写"由编排层掷骰子决定后注入指令，而不是让模型自选——
# 模型面对 schema 永远倾向填满（实测 62 个角色 hidden_side/decisive_event
# 100% 非空、"반전 매력"标签占 60%、职业塌缩到自由职业创意人）。
# ==========================================================================
def _load_persona_seeds() -> dict:
    p = config.DATA_DIR.parent / "app" / "data" / "persona_seeds.json"
    try:
        data = json.loads(p.read_text(encoding="utf-8"))
        return data if isinstance(data, dict) else {}
    except (OSError, json.JSONDecodeError):
        return {}


_PERSONA_SEED_DATA = _load_persona_seeds()

PERSONA_ARCHETYPES = ("drama", "plain", "passion")
_ARCHETYPE_WEIGHTS = (0.35, 0.35, 0.30)

_ARCHETYPE_DIRECTIVES = {
    "zh": {
        "drama": (
            "反差解密型：这个角色有真正的 hidden_side 和完整的 personality 因果链"
            "（decisive_event → response → cost → desire_outer ↔ desire_inner），"
            "按反差分层铁律写出『聊出来才解锁』的反转。"
        ),
        "plain": (
            "平实日常型：personality 只写 summary，decisive_event 及之后的字段全部输出空字符串；"
            "hidden_side 也输出空字符串 \"\"；不要写任何创伤、秘密或反转。"
            "角色靠具体的生活质感、鲜明的行为习惯和说话方式立住——"
            "『真实、松弛、有生活』本身就是魅力，不需要反转来撑。"
        ),
        "passion": (
            "热爱驱动型：性格源头是正面的（长期沉浸的热爱/一个榜样/被好好爱过给的底气），"
            "decisive_event 写正面经历，cost 写这种底色的盲区（如笃定→听不进劝、慷慨→不设防）；"
            "hidden_side 至多写很轻的一层（不轻易示人的笨拙面/软肋），也可以留空——不要写黑暗秘密。"
        ),
    },
    "ko": {
        "drama": (
            "반전 해금형: 진짜 hidden_side와 완전한 personality 인과 사슬"
            "(decisive_event → response → cost → desire_outer ↔ desire_inner)을 갖춘다. "
            "반전 레이어 원칙대로 '대화로 해금되는' 반전을 설계한다."
        ),
        "plain": (
            "담백한 일상형: personality는 summary만 쓰고 decisive_event 이하 필드는 전부 빈 문자열로 출력한다; "
            "hidden_side도 빈 문자열 \"\"; 트라우마·비밀·반전을 일절 쓰지 않는다. "
            "구체적인 생활 질감, 선명한 행동 습관과 말투로 캐릭터를 세운다 — "
            "'진짜 같고 느슨하고 생활감 있는' 것 자체가 매력이라 반전이 필요 없다."
        ),
        "passion": (
            "열정 구동형: 성격의 뿌리가 긍정적이다(오래 빠져든 무언가/롤모델/충분히 사랑받은 든든함). "
            "decisive_event는 긍정적 경험으로, cost는 그 바탕색의 사각지대로(확신→조언 안 들음, 너그러움→무방비 등); "
            "hidden_side는 아주 가벼운 층(쉽게 안 보여주는 어설픈 면)까지만, 비워도 된다 — 어두운 비밀 금지."
        ),
    },
}


def sample_archetype() -> str:
    """编排层掷骰子决定本次角色类型，调用方应把结果存进角色 record。"""
    return random.choices(
        PERSONA_ARCHETYPES, weights=_ARCHETYPE_WEIGHTS, k=1)[0]


def _persona_seed_samples(lang: str) -> str:
    block = _PERSONA_SEED_DATA.get(lang) or _PERSONA_SEED_DATA.get("zh") or {}
    occs = block.get("occupations") or []
    sep = ", " if lang == "en" else "、"
    return sep.join(random.sample(occs, min(6, len(occs)))) if occs else ""


def build_persona_diversity_block(
    lang: str,
    archetype: str,
    avoid_names: list[str] | None = None,
    recent_jobs: list[str] | None = None,
    overused_tags: list[str] | None = None,
) -> str:
    """构建注入人设 prompt 的多样性约束块。

    archetype 决定可选字段的写法（骰子结果，必须执行）；种子与避让列表
    只是软引导。ko 用韩语指令，其余语言共用中文指令（与主 prompt 一致）。
    """
    dirs = _ARCHETYPE_DIRECTIVES["ko" if lang == "ko" else "zh"]
    directive = dirs.get(archetype) or dirs["drama"]
    occ = _persona_seed_samples(lang)
    names = "、".join(avoid_names[:40]) if avoid_names else ""
    jobs = "；".join(recent_jobs[:15]) if recent_jobs else ""
    tags = "、".join(overused_tags[:8]) if overused_tags else ""

    if lang == "ko":
        lines = [
            "# 🎲 이번 캐릭터의 다양성 지정(시스템이 무작위로 정했다. 기본 취향보다 우선하며 반드시 따를 것)",
            f"- 【캐릭터 유형】{directive}",
        ]
        if occ:
            lines.append(
                "- 【직업 방향 영감】무작위 추출이며 발상을 여는 용도일 뿐이다. 이미지/창작 보충 요구와 충돌하면 후자를 따르고 자유롭게 지어도 되지만, "
                f"'프리랜서 디자이너/일러스트레이터/포토그래퍼'류의 이미 쏠린 직업으로 되돌아가지 말 것: {occ}"
            )
        if names:
            lines.append(f"- 【이름 중복 회피】다음 이름은 이미 다른 캐릭터가 쓴다. 절대 다시 쓰지 말고, 한 글자만 바꾸지도 말 것: {names}")
        if jobs:
            lines.append(f"- 【직업 회피】최근 캐릭터들이 이 직업들을 많이 썼다. 이번엔 다른 방향으로: {jobs}")
        if tags:
            lines.append(f"- 【태그 회피】이 tags는 최근 과용됐다. 정말 딱 맞지 않는 한 다른 걸로: {tags}")
        return "\n".join(lines)

    lines = [
        "# 🎲 本次角色的多样性指定（由系统随机骰出，优先级高于你的默认偏好，必须执行）",
        f"- 【角色类型】{directive}",
    ]
    if occ:
        lines.append(
            "- 【职业方向灵感】随机抽样，仅供打开思路；与图片/创作补充要求冲突时以后者为准，也可自创——"
            f"但不要又落回『自由职业设计师/插画师/摄影师』这类已严重扎堆的职业：{occ}"
        )
    if names:
        lines.append(f"- 【重名避让】下列姓名已被其他角色使用，绝对不要再用，也不要只改一个字：{names}")
    if jobs:
        lines.append(f"- 【职业避让】最近的角色已大量使用这些职业，本次尽量换别的方向：{jobs}")
    if tags:
        lines.append(f"- 【标签避让】这些 tags 最近用得太多，除非极其贴合否则换别的：{tags}")
    return "\n".join(lines)


PERSONA_DIVERSITY_QUALITY_RULES = """
# ⚠️ 去重与多样性（批量生产铁律）
- 【去重禁用清单】以下是模型被过度使用的套路，除非上传图片明确指向，否则主动避开并做出不同选择：
  · 失眠 / 번아웃 作为性格标签
  · 自由职业设计师、插画师、独立出版编辑、摄影师作为默认职业
  · "무심한 듯 다정"（无心却体贴）作为性格概括
  · "被同事/前辈抢功背锅"作为 decisive_event
  · 망원동 / 연남동 作为默认居住地
  · 流浪猫/喂猫/츄르/救助动物作为反差萌或生活细节
  以上元素不是绝对禁止，但如果图片没有明确线索，就必须主动选择不同的设定。
- 【纹身限制】不要默认给角色设定大面积/满身纹身（覆盖胸口、双臂花臂、颈部/整背）。只有图片中明确看到大面积纹身或创作者补充要求里明确指定时才允许。否则：无纹身，或仅限小范围单处点缀纹身（手腕/脚踝/锁骨下方的小图案）。
- 【创伤是可选的】decisive_event/response/cost 不是必填。轻松日常向角色完全可以没有明显创伤或转折——靠鲜明的取向、怪癖、内在矛盾、生活选择来立体，不要硬凑沉重背景。
- 【写创伤时结构要换】如果决定写 decisive_event，不要总走"被信任的人背叛→封闭→渴望真心"。来源和结果都要变：
  · 失去控制→过度掌控一切（原生家庭/经济变故→强迫型计划癖，怕再次失控）
  · 长期被忽视→渴望被看见（家中透明人/朋友圈边缘→表演型讨好或反向冷漠）
  · 意外成功/阶级跃升→冒充者焦虑（不配得感、怕被拆穿）
  · 身体创伤/大病→对身体的执念或放纵（运动成瘾/自毁式不在乎）
  · 正面顿悟→笃定但与世界脱节（旅行/恩师/热爱→活在自己节奏里，难以妥协）
  结果可以是掌控、讨好、逃避、成瘾、笃定等完全不同走向。
- 【独占锚】每个角色有一个"删掉就不成立"的核心元素——某个极具体的职业细节、一个只属于 TA 的习惯/物件/执念/空间。它自然出现即可，不必在每个字段都复述一遍；避免因为要“贯穿”而在 life_details、likes、backstory、opening 里反复讲同一件事。
- 【初始关系分布】relationship_with_user 的初始亲密度4档中，优先分配①陌生或②浅识（占比60~70%），③半熟占20~30%，④熟识不超过10%。不要每次都写成"老朋友/已经很熟"——从陌生关系起步，用户才有发掘和靠近的空间。opening 的语气/距离感/信息量必须匹配该档位：①②档用有礼貌的距离感、试探性、克制；③档适度放松但有分寸；只有④档才允许完全自来熟/反言/直球。
- 【充分利用每张图】上传的每一张图都包含不同信息（场景/穿搭/物件/表情/同伴），要把每张图里的独特细节都吃进人设里（变成具体的生活习惯/爱好/居住环境/社会关系），不要只用其中一两张的信息。"""

PERSONA_DIVERSITY_QUALITY_RULES_KO = """
# ⚠️ 중복 방지와 다양성 (배치 생산 원칙)
- 【클리셰 금지 리스트】아래는 모델이 과도하게 쓰는 패턴이다. 업로드 이미지가 명확히 가리키지 않는 한 피하고 다른 선택을 한다:
  · 불면증/번아웃을 성격 라벨로 쓰기
  · 프리랜서 디자이너, 일러스트레이터, 독립출판 에디터, 포토그래퍼를 기본 직업으로 쓰기
  · "무심한 듯 다정"을 성격 요약으로 쓰기
  · "동료/선배에게 공 빼앗기고 뒤집어쓰기"를 decisive_event로 쓰기
  · 망원동/연남동을 기본 거주지로 쓰기
  · 길고양이/츄르/유기동물 돌보기를 반전 매력이나 생활 디테일로 쓰기
  위 요소를 절대 금지하는 것은 아니지만, 이미지에 명확한 단서가 없으면 반드시 다른 설정을 선택한다.
- 【타투 제한】대면적/전신 타투(가슴 전면, 양팔 풀슬리브, 목·등 전체를 덮는 타투)를 기본 설정하지 않는다. 이미지에 대면적 타투가 명확히 보이거나 창작자 보충 요구에 타투 지시가 있을 때만 허용하되, 그 외에는 타투 없음 또는 작은 포인트 타투(손목·발목·쇄골 밑에 하나)까지만.
- 【창상은 선택사항】decisive_event/response/cost는 필수가 아니다. 가볍고 일상적인 캐릭터는 뚜렷한 트라우마나 전환점 없이도 된다 — 이런 캐릭터는 선명한 취향, 집착, 내적 모순, 생활 선택으로 입체감을 만든다. 억지로 무거운 배경을 붙이지 말 것.
- 【창상을 쓸 때는 구조를 바꿀 것】decisive_event를 넣기로 했다면 "배신→폐쇄→진심 갈구"라는 동일 구조를 반복하지 말 것. 원인과 결과 구조를 모두 바꿔야 한다:
  · 통제력 상실→과잉 통제(원가족/경제 변동→강박적 계획, 다시는 흔들리지 않으려 함)
  · 오래된 무시→인정 갈구(가정 내 투명인간/친구 관계 주변부→과잉 배려 또는 반대로 냉담)
  · 예상치 못한 성공/계층 상승→가면 증후군(자격 없다는 불안, 들킬까 두려움)
  · 신체 트라우마/큰 병→몸에 대한 집착 또는 방치(운동 중독/자기파괴적 무관심)
  · 긍정적 깨달음→확고하지만 세상과 어긋남(여행/은인/열정→자기 리듬대로 사는 대신 타협 불가)
  결과가 통제·추종·회피·중독·확신 등 전혀 다른 방향이어야 한다.
- 【독점 앵커】모든 캐릭터에 "빼면 성립 안 되는" 핵심 요소 하나가 있다 — 극도로 구체적인 직업 디테일, 이 사람만의 습관/물건/집착/공간. 자연스럽게 드러나면 되고, 모든 필드에서 되풀이할 필요는 없다. '관통시키려고' life_details·likes·backstory·opening에서 같은 것을 반복 설명하지 말 것.
- 【초기 관계 분포】relationship_with_user 의 초기 친밀도 4단계 중 ①낯선 또는 ②얕은 아는 사이를 우선 배정한다(전체의 60~70%). ③반쯤 아는 사이는 20~30%, ④친한 사이는 10% 이하. 매번 "오래된 친구/이미 편한 사이"로 가지 않는다 — 낯선 관계에서 시작해야 사용자가 발견하고 가까워지는 재미가 생긴다. opening의 말투·거리감·정보량도 이 단계에 맞춰야 한다: ①②단계는 예의 있는 거리감/시험적/절제적, ③은 적당히 편하지만 분수 있음, ④만 자유로운 반말·농담·직진.
- 【모든 이미지 활용】업로드된 각 이미지는 서로 다른 정보(장면/착장/소품/표정/동행)를 담고 있다. 각 이미지의 고유 디테일을 빠짐없이 인설에 녹인다(구체적 생활 습관/취미/거주 환경/사회 관계로 변환). 한두 장만 쓰고 나머지를 무시하지 말 것."""


# 各语言"线上私聊打字习惯"——只用于开场白 messages，让台词像该国人真实在 IM 里打字，
# 而非翻译腔或发帖腔。与发帖用的 LOCALE_GUIDE 分开，避免互相污染。
_OPENING_IM_STYLE = {
    "zh": (
        "像在手机即时聊天里随手打字：短句连发，标点少，常省略主语；"
        "语气词自然（啊/呢/吧/诶/嘛），可用「哈哈哈」「…」「？？」这类；"
        "不堆 emoji、不文绉绉、不写完整书面句。"
    ),
    "ja": (
        "スマホのチャットで打つ口語感で：短文を連投、句点はほぼ無し、主語は省略しがち。"
        "「笑」「ww」「…」「？」やタメ口/敬語をキャラの口調に合わせて自然に。"
        "絵文字や顔文字は使うとしても控えめに、書き言葉にしない。"
    ),
    "ko": (
        "휴대폰 메신저에서 치듯이: 짧은 문장을 여러 번 나눠 보내고, 마침표 거의 없이, "
        "주어 자주 생략. ㅋㅋㅋ/ㅎㅎ/ㅠㅠ/ㅇㅇ/ㄴㄴ/ㄱㅊ 같은 초성·이모티콘 자연스럽게, "
        "반말/존댓말은 캐릭터 말투에 맞게. 번역체·문어체 금지, 완성된 긴 문장 금지."
    ),
    "en": (
        "Like a real person texting: short bursts sent over several lines, minimal "
        "punctuation, lowercase is fine, drop subjects. Natural 'lol/omg/hmm/...', "
        "no stiff full sentences, emojis sparingly."
    ),
}


def _opening_rules(lang: str) -> str:
    """开场白（opening）写作规则。被完整人设 prompt 与单独开场白 prompt 共用，保持一致。"""
    lname = config.lang_name(lang)
    im_style = _OPENING_IM_STYLE.get(lang, _OPENING_IM_STYLE["en"])
    if lang == "ko":
        return f"""opening은 캐릭터가 【먼저 보내는】 첫 휴대폰 메신저 대화이고, note(관계 배경 설명) + messages(캐릭터가 보낸 말풍선)로 구성한다.

# 어떤 상황인가
- 【휴대폰 너머의 원거리 대화】다: 캐릭터와 {{user}}는 같이 있지 않고 보이지도 닿지도 않으며, 지금은 텍스트/음성/사진만 보낼 수 있다.
  그래서 "{{user}}를 부르거나, 문을 열라거나, 만나자거나, 같이 뭘 하자"는 식의 대면·오프라인 약속 장면은 나오지 않는다.
- 【진행 중인 채팅을 중간부터 엿본】 느낌이다: 진짜 폰 속 말풍선이지 대면 대사도 소설 내레이션도 아니다.

# opening.note(오프닝 주석): 한국어 단일 문자열, 비워도 됨 ""
- 역할: 제3자 내레이션 시점으로 사용자에게 한 줄 배경을 줘서, 자신이 이 대화 속에서 누구이고 캐릭터와 무슨 사이인지 한눈에 알게 한다.
- 【필수 여부 판정】messages를 다 쓴 뒤 자문한다: 그 안에 메시지만으로는 추론할 수 없는 전제가 있는가({{user}}가 누구/무슨 신분인지, 캐릭터와 무슨 사이인지)? 있으면 note로 채우고, 전부 messages에서 바로 읽히면 ""로 비운다.
- 【관계 입구만, 소설처럼 쓰지 말 것】note는 '유저가 누구이고 캐릭터와 어떤 기본 관계인지'라는 사실 하나만 한 문장으로, 20자 이내를 지향한다. 관계 발전사·감정적 의미를 쓰지 않고 "점점 친구가 됐다" "미묘한 관계" "오직 …앞에서만" 같은 미화를 쓰지 않는다. 그 긴장은 messages가 연기하지, note가 해설하지 않는다.
- 시점: 제3자 내레이션. 캐릭터는 이름으로 가리키고("나" 사용 금지), 사용자는 "너" 또는 {{user}}로 가리킨다. 캐릭터 1인칭 독백으로 쓰지 않는다.
- 관계 배경만 전한다. messages에 나올 장면을 미리 설명하거나 복창하지 않고, 캐릭터의 지금 연속 동작을 늘어놓지 않는다.

# opening.messages: 2~5개의 휴대폰 메신저 말풍선
- 각 항목은 {{"type":"text","data":{{"content":"..."}}}} 또는 {{"type":"voice","data":{{"content":"..."}}}}.
  voice의 content는 음성 합성(fishaudio)이 읽는 텍스트다: 원하는 감정/말투/음량/속도를 【본문과 같은 언어의 자연스러운 말】로 짧게 대괄호 [ ] 안에 끼워 넣는다. 고정 단어 목록 없이, 영어 태그나 코드처럼 쓰지 않는다.
- 미리 확정된 personality, speech_style, relationship_with_user, relationship_mode, love_style에서 자연스럽게 나온 첫 연락이어야 하고, 설정과 모순되거나 별개의 상황극처럼 튀면 안 된다. 반드시:
  · 【바로 대화로, 배경 설명 없이】: 자초지종을 풀지 말고 첫 메시지부터 구체적 상황·감정·갈등 한가운데로. 인사·뜸 들이기를 건너뛰고 진행 중인 채팅을 중간부터 본 느낌으로, 사용자가 곧장 답하고 싶게. 배경은 note가 맡는다.
  · 【사용자가 안 한 말에 답하지 않기】: 첫 메시지는 캐릭터가 【먼저】 거는 연락이다. {{user}}가 방금 무슨 말이나 질문을 한 것처럼 받아치지 않는다. 사용자가 읽고 곧장 이해할 수 있는, 캐릭터 쪽에서 시작하는 말이어야 한다.
  · 【지금 이 한 가지 일에만】: 같은 순간·같은 사건을 두고 한 사람이 폰으로 쭉 이어 보내듯 쓴다. 메시지마다 화제를 바꾸지 않는다.
  · 【진짜 채팅 같은 질감】: 짧은 문장 연발, 반복 추궁, 반 토막 말, 주어 생략, 말끝 흐림, 초성, 가끔 감탄사. 긴 문장·문어체 명문·설명문 금지. 말풍선 그 자체로만, 지문·행동 묘사·괄호 연기 금지. 밈·유행어는 한 스푼만, 시크하거나 내향적인 캐릭터는 거의 안 쓴다.
  · 【설명 늘어놓기 금지】: 사용자가 알 수 없는 고유 정보를 메시지에 우겨 넣지 않는다. 관계나 전사가 필요하면 note가 담당한다.
  · 사용자 지칭은 {{user}} 또는 2인칭 "너", 임의 이름이나 "유저 {{user}}" 식 겹쳐쓰기 금지.
  · 【현지 타자 습관】: 한국인이 폰으로 치듯이 — {im_style}
  · 【마지막은 사용자에게 공 넘기기】: 답하고 싶어지는 한 방으로 끝낸다(근황 묻기 / 밈 던지기 / 위로·칭찬 구하기 / 떠보기 / 양자택일 / 어리광 / 말 끊고 궁금하게 / 가벼운 도발 / 조언 구하기 / 불쌍한 척…). 꼭 물음표가 아니어도, 반 토막 말이나 짧은 감탄이 반응을 부르면 된다.
  · 【발송 전 3문 자가점검】캐릭터를 전혀 모르는 새 유저가 note + messages만 읽었을 때: ①"내가 누구고 이 캐릭터와 무슨 사이인지" 아는가? ②"지금 무슨 일이고 왜 이 메시지를 보냈는지" 이해되는가? ③고민 없이 첫 답장을 칠 수 있는가? 하나라도 막히면 돌아가서 고친다 — note는 '내가 누구고 무슨 사이인지' 한 문장만 담고, 나머지는 messages로 연기한다. note를 길게 늘리지 말 것."""
    return f"""opening 是角色【主动发出】的第一段手机聊天，由 note（关系背景说明）+ messages（角色发来的聊天气泡）组成。

# 这是什么场景
- 一段【隔着手机的远程聊天】：角色和 {{user}} 不在一起、看不见也碰不到，此刻只能发文字/语音/图片。
  所以不会出现"叫 {{user}} 过来、让 TA 开门、约见面、一起做某事"这类当面或线下邀约的情景。
- 像【中途瞥见一段正在进行的聊天记录】：是真人手机里的聊天气泡，不是线下当面对白，也不是小说旁白。

# opening.note（开场白注释）：{lname} 单值字符串，可留空 ""
- 作用：从旁白视角给用户一句话背景，让用户一眼知道自己在这段对话里是谁、和角色什么关系。
- 【必填判定】写完 messages 后自查：里面是否有用户无法从消息本身推出的前提（自己是谁/什么身份、和角色什么关系）？有就用 note 补上；全部能从 messages 直接读懂时就留空 ""。
- 【只写关系入口，别写小作文】note 只交代‘用户是谁、和角色什么基础关系’这一个事实，一句话即可、力求 ≤20 字。不写关系发展史、不写情绪意义、不用“渐渐成了朋友”“微妙的关系”“只有在…面前”这类升华；这些张力交给 messages 去演，不在 note 里解释。
- 视角：第三人称旁白。角色用名字指代（不用"我"），用户用"你"或 {{user}}；不写成角色第一人称自述。
- 只交代关系背景，不复述或预告 messages 里的情节，不铺陈角色此刻的连续动作。

# opening.messages：2~5 条角色发给用户的手机聊天气泡
- 每条 {{"type":"text","data":{{"content":"..."}}}}；少数情况可用语音 {{"type":"voice","data":{{"content":"..."}}}}。
  voice 的 content 是给语音合成（fishaudio）念的文本：用方括号 [ ] 把想要的情绪/语气/音量/语速，用【和正文同语种的自然语言】简短写在要生效的位置，不用固定词表、不要写成英文标签或代码。
- 必须从已定稿的 personality、speech_style、relationship_with_user、relationship_mode、love_style 自然长出来，不能和设定矛盾、不能像另起炉灶的随机剧情。务必做到：
  · 【直接进对话，不解释背景】：不从头交代来龙去脉，第一条就落在具体的情境/情绪/矛盾里，像中途瞥见一段正在进行的聊天记录；跳过寒暄，开头就让用户被勾住想回。背景交给 note。
  · 【别回答用户没说过的话】：第一条是角色【主动】发起的联系，不要写成像在接用户刚说的话/刚问的问题。要让用户一读就能接住、是角色这边起的头。
  · 【只围绕"此刻这一件事"】：几条消息是同一个当下时刻、同一件事的连发，像一个人盯着手机就这件事说下去；不要每条蹦一个互不相干的话题。
  · 【真实聊天感】：短句连发、可重复追问、半句话、省略主语、带语气词；不要长句、书面金句、解说式长段。就是聊天气泡本身，禁止旁白、动作描写、括号表演。
  · 【不要报菜名】：不在消息里塞用户无从知晓的专有信息当包袱；要交代关系/前情那是 note 的活。
  · 【本地打字习惯】像 {lname} 母语者真实在手机上打字：{im_style}
  · 【结尾把球递给用户】最后落点让用户忍不住想回一句，按角色性格挑最自然的方式（追问 / 抛梗 / 求安慰 / 讨态度 / 选择题 / 撒娇 / 吊胃口 / 小赌气 / 求主意 / 装可怜……）；不必是问句，半句话或一个短词能勾起回应即可。
  · 【发出前三问自检】一个完全不认识角色的新用户，只读 note + messages：①能知道"我是谁、和 TA 什么关系"吗？②能看懂"现在发生了什么、TA 为什么发这条"吗？③能不假思索接出第一句回复吗？三问有一问不过就回去改——note 只补‘我是谁、和 TA 什么关系’这一句，其余靠 messages 演出来，别把 note 写长。"""


# ==========================================================================
# 1. PERSONA EXTRACTION  (image -> persona schema, multilingual)
# ==========================================================================

# Fields that must be localized into all 4 languages (object {zh,ja,ko,en}).
PERSONA_LOCALIZED_FIELDS = [
    "name", "profile", "appearance", "hometown", "residence",
    "social_status", "speech_style", "relationship_with_user",
    "love_style", "situational_reactions",
    "hidden_side", "likes", "fears", "backstory",
    "family", "social_network", "premise", "wishlist",
]
PERSONA_LOCALIZED_PERSONALITY = [
    "summary", "decisive_event", "response", "cost",
    "desire_outer", "desire_inner", "desire_bottom_line", "healing",
]
# Tags must come from this preset list (max 3).
# 前 21 个为恋爱幻想轴，后 16 个为性格/生活方式轴——两份列表按位对应。
# 非恋爱轴用于稀释标签塌缩（此前"반전 매력"一个标签占了 60% 角色）。
PERSONA_TAG_PRESETS = [
    "心机", "坏男人", "傲娇", "专情", "反差萌", "能干", "财阀", "执着",
    "追妻火葬场", "非人类", "傲慢", "温柔", "偶像·名人", "双重生活",
    "油嘴滑舌", "年下", "冷酷", "大叔·年上", "外国人·混血", "直接", "冷淡",
    "元气", "治愈", "毒舌", "天然呆", "学霸", "运动系", "社恐", "社牛",
    "吃货", "手艺人", "冒险家", "宅", "邻家", "工作狂", "怪咖", "老好人",
]
PERSONA_TAG_PRESETS_KO = [
    "내숭·꿍꿍이", "나쁜 남자", "츤데레", "순정", "반전 매력", "능력자", "재벌", "집착",
    "후회각 추격전", "비인간", "오만", "다정", "아이돌·셀럽", "이중생활",
    "능글", "연하", "냉정", "연상·아저씨", "외국인·혼혈", "직진", "시크",
    "긍정 에너지", "힐링", "독설", "천연", "공부벌레", "운동광", "집순이·집돌이", "인싸",
    "먹보", "장인", "모험가", "오타쿠", "옆집 친구", "워커홀릭", "괴짜", "호구력 만렙",
]


def _persona_schema_doc(lang: str = "zh", voice_block: str = "", tag_presets: str = "") -> str:
    voice_choices = " / ".join(
        line.strip().lstrip("- ").strip()
        for line in voice_block.splitlines()
        if line.strip()
    ) or (
        "현재 언어의 음색 라이브러리에서 모델 ID 하나 선택"
        if lang == "ko" else "按当前语言的音色库选择一个模型ID"
    )
    tag_choices = tag_presets.strip() or (
        "캐릭터에 맞는 태그 최대 3개"
        if lang == "ko" else "按角色选择最多3个标签"
    )
    if lang == "ko":
        schema = """
최상위 구조와 필드별 역할·작성 방법(각 필드는 캐릭터를 어떻게 보여주고 사용자가 왜 대화하고 싶어지는지를 정하기 위한 설계 항목이다. 아래 설명 문구는 지시문일 뿐, 실제 값으로 복사하지 말 것. 실제 값은 모두 자연스러운 한국어로 작성):
{
  "name": "이름 / 필수, 10자 이하. 사용자가 캐릭터를 기억하고 부를 때 가장 먼저 남는 호칭이므로, 이미지 분위기와 나이대·국적·장르감에 어울리게 짓는다",
  "tags": ["캐릭터 태그 / 필수, 최대 3개. 피드와 매칭에서 캐릭터의 장르적 매력을 빠르게 분류하는 신호. 성격, 관계 판타지, 반전 매력 중 가장 강한 축을 한국어로 자연스럽게 고른다. 선택 범위: {{TAG_CHOICES}}"],
  "anonymous_identities": ["익명 매칭 노출용 정체성 / 선택, 최대 3개, 각 항목 8자 이내. 이름을 가린 상태에서 유저가 가장 먼저 보는 '이 사람과 대화해보고 싶다'는 판단 단서. 단순 태그나 계정명이 아니라, 캐릭터의 공개 표층 특성·직업/계층·현재 처지·말 걸 소재가 한눈에 보이는 짧은 정체성 예고로 쓴다. ⚠️표층 후크와 살짝의 궁금증만 주고, 【hidden_side에 숨긴 비밀/반전은 절대 스포하지 않는다】 — 그건 유저가 대화에서 해금할 몫이다"],
  "profile": "후크 카피 / 필수, 100자 이하. 캐릭터가 피드에 공개 노출되어 유저가 대화하러 들어올지를 결정하는 첫 미끼다 — 예고편이 가장 끌리는 한 컷만 자르듯. ⚠️성공 기준은 【읽고 나서 말 걸고 싶다·알고 싶다·다가가고 싶다】이지 【읽고 나서 이 사람을 파악했다】가 아니다 — 인물을 완결적으로 설명하는 '인물 소개/스케치'로 쓰지 말 것. 이 캐릭터의 【가장 강한 매력 포인트 하나】만 잡고 다 담으려 하지 않는다; 무엇으로 할지는 설정이 정한다: 설렘, 호기심, 판타지 충족, 분위기 매력, 감정 가치, 들춰보고 싶음, 지켜주고 싶음, 유머, 궁금증 등 무엇이든 되고 꼭 '비밀이 있다'로 갈 필요는 없다. ⚠️【누구에게나 들어맞는 뻔한 내면 서술 금지】('웃음 뒤엔 여림/츤데레/이해받고 싶음'은 누구든 성립해 정보량 0) — 이 캐릭터만의 구체적이고 정보량 있는 셀링포인트를 준다. 쓸 수 있는 후크 기법(가장 맞는 1~2개만, 다 쓰지 말 것): ①고concept 소재 전면화 — 회귀/빙의/계약/재벌/숙적/후회각 추격전 같은 셀링 소재를 한 방에; ②반전 — 공개 신분 vs 반전 사실, 표층만 찌르고 속내는 안 깜; ③관계 포지션 대입 — 유저에게 관계 역할을 줘 몰입 유도; ④정보 공백 — 답 없는 모순을 던져 묻고 싶게; ⑤감정 가치 약속 — 줄 수 있는 판타지 명시(떠받듦/설렘/힐링/정복/구원); ⑥구체적 반전 디테일 — 밋밋한 직업이 아니라 기억에 남는 정체성 한 스푼. 긴장감·장면감·말투·초대감(끌어당기고 살짝 도발하고 부르는 말투 OK, 단 특정 유저가 아니라 불특정 다수 대상)을 써도 된다(카피 느낌이 허용되는 유일한 필드). 3인칭 예고든 1인칭 자기소개든 분위기에 맞게. ⚠️강도는 캐릭터 성격이 정한다: 엉뚱/발랄한 캐릭터는 과장·드립 OK, 시크/차분한 캐릭터는 절제. ⚠️【반드시 현지화】네트워크 말맛, 과장 문장부호, 이모티콘/이모지, 소재 밈은 모두 【그 언어 모국어 화자가 실제 로컬 SNS에서 쓰는 방식】으로 — 번역체·타 언어 표현 이식 금지(중국식 이모티콘을 한국어/영어에 넣지 말 것). ⚠️【여백 원칙】공개면엔 구체적인 표층 미끼만 주고 설명할 대목에서 멈춘다, 【취약한 속내/비밀/진짜 반전이 무엇인지는 절대 드러내지 않는다】('웃음 뒤엔 실은 여리고 혼자 버티며 혼자 있을 때만 무장 해제된다' 같은 건 바로 hidden_side라 숨겨서 대화로 해금하게 두고, profile에 쓰면 안 된다). ⚠️'지금 무엇을 하고 있는지' 같은 오프닝식 순간 장면으로 쓰지 말 것.",
  "species": "종족 / 필수 단일값: 인간·엘프·수인·동물·기타. 세계관, 욕망, 신체 반응, 관계 거리감의 기준이 되는 설정이므로 이미지와 장르에 맞게 고른다",
  "gender": "성별 / 필수 단일값: 남성·여성·기타. 관계 호칭, 사회적 시선, 말투의 거리감을 정하는 기준으로 쓰되 고정관념에 기대지 않는다",
  "visibility": "공개 범위 / 필수 단일값: public 또는 private. 이 캐릭터가 피드·검색·매칭에서 발견될지 정하는 운영 필드이며, 기본은 public",
  "voice": "음색 / 채팅 전 떠올릴 첫 청각 인상. 성별, 나이대, 성격의 온도, 말투와 어울리는 모델 ID 하나만 선택. 선택 가능한 모델: {{VOICE_CHOICES}}",
  "personality": {
    "summary": "성격 요약 / 200자 이하. 이 사람의 가장 선명한 바탕색과 긴장(반전·확신·열정·느긋함 등)이 보이게. ⚠️필수는 이것 하나뿐",
    "decisive_event": "(선택) 성격을 빚은 핵심 경험/관계/어느 날의 장면. 카메라로 찍을 수 있어야 하고 추상 요약 금지. 긍정(무조건적 사랑·롤모델·열정)도 부정(배신·결핍)도 가능하며 기본값으로 트라우마에 기대지 말 것. 뚜렷한 전환점이 없으면 빈 문자열로 두거나 키 생략",
    "response": "(선택) 그 경험에서 자라난 안정된 성격 바탕색 / 행동 방식(지금 보여주는 모습). decisive_event가 없으면 비움",
    "cost": "(선택) 그 바탕색의 이면 / 사각지대 / 대가. 긍정적 뿌리에도 이면은 있다(무조건 믿음→이용당함 등). decisive_event가 없으면 비움",
    "desire_outer": "(선택) 겉으로 원한다고 말하는 것",
    "desire_inner": "(선택) 진짜로 부족한 것 / 진짜 원하는 것",
    "desire_bottom_line": "(선택) 기꺼이 내줄 수 있는 것 / 절대 내줄 수 없는 것",
    "healing": "(선택) 어떤 관계나 사건이 이 반복을 풀어줄 수 있는지"
  },
  "opening": {
    "note": "오프닝 주석 / 선택. 메시지만으로 부족할 때 '{user}가 누구이고 캐릭터와 어떤 기본 관계인지'만 한 문장(20자 이내 지향)으로 보충한다. 관계 발전사·감정 미화 금지. 사용자를 지칭해야 하면 {user} 또는 2인칭 '너'를 쓰고(임의 이름 금지), 메시지 자체가 충분히 명확하면 빈 문자열",
    "messages": [
      {
        "type": "text 또는 voice (대부분 text, voice는 가끔)",
        "data": {"content": "오프닝 메시지 한 줄, 200자 이하, 2~5개. 확정된 성격·관계·말투를 바탕으로 캐릭터가 먼저 보내는 자연스러운 연속 말풍선. voice일 때는 원하는 감정·말투·음량·속도를 본문과 같은 언어의 자연스러운 말로 대괄호 [ ] 안에 적용할 위치에 넣는다. 사용자를 지칭해야 하면 임의 이름 대신 {user} 또는 '너'를 쓴다"}
      }
    ]
  },
  "appearance": "외모·스타일 / 200자 이하. 사용자가 머릿속에 바로 그릴 수 있는 시각적 기억점. 얼굴·체형·분위기·시그니처 착장뿐 아니라 감정이 흔들릴 때 드러나는 몸의 변화까지 잡는다",
  "hometown": "출신지 / 한 줄로 짧게. 어느 지역 출신인지 정도만 적어 말투·기질의 뿌리가 되게 한다. 가족사·성장 서사·성격 변화까지 풀어 쓰지 말 것 — 그건 backstory가 담당한다",
  "residence": "거주지 / 현재 삶의 리듬과 외로움, 소비 수준, 만날 수 있는 장면을 만드는 공간. 한국 독자가 바로 그림이 그려지는 동네와 주거 형태로 쓴다",
  "social_status": "직업·계층 / 캐릭터가 사회에서 어떤 위치로 소비되고 버티는지 보여주는 축. 매일 실제로 하는 일, 그 일이 주는 의미, 수입·주거·소비 감각까지 연결해 쓴다",
  "speech_style": "말투·언어 습관 / 채팅에서 캐릭터가 살아 보이게 하는 핵심 식별자. 사투리, 존댓말·반말 전환, 선호 어휘, 문장 길이, 입버릇, 호칭, 온라인 답장 템포와 절대 하지 않는 말투까지 '어떻게 말하는지'를 설명한다. ⚠️실제 대사 예문이나 따옴표로 묶은 멘트는 절대 넣지 말고, 말하는 방식만 묘사한다",
  "relationship_with_user": "{user}와의 관계 / {user}가 이 캐릭터와 어떻게 말을 트게 됐는지 '관계 입구'를 한 문장으로. 알게 된 계기 + 현재 기본 거리감이면 충분하고 20~40자로. 사실만 적고, 관계 발전사·감정적 의미·'오직 {user} 앞에서만…' 같은 특별화 미화는 쓰지 않는다. ⚠️대부분 낯선/얕은 사이에서 시작해 사용자가 탐색할 여지를 남긴다. '오랜 친구/이미 편한 사이/유일하게 이해해주는 사람'으로 기본 설정하지 말 것. 사용자를 지칭해야 하면 반드시 {user}.",
  "relationship_mode": "사회적 모드 / 상대에 따라 상호작용 방식이 어떻게 다른지의 관계 지도. 낯선 사람·친구·가족·일 관계, 그리고 {user}를 각각 한 문장씩, 상호작용 방식만 적고 감정을 늘어놓지 않는다. ⚠️{user}를 '유일하게 진짜 모습을 보는 사람'으로 기본 설정하지 말 것 — {user}도 대개 관계 초반이고, 진짜 모습은 대화로 조금씩 해금되는 것이다. 처음부터 예외로 쓰면 캐릭터의 대화 여지와 탐색성이 사라진다. 사용자를 지칭해야 하면 반드시 {user}",
  "love_style": "가까워진 뒤의 상호작용 방식 / 관계가 가까워지면 캐릭터가 {user}에게 어떻게 마음을 표현하고 친밀함·갈등을 다루는지의 규칙을 한 문장으로. 애착·질투·힐링이 있어도 되지만 기본값으로 깔지 말고 성격에 맞게 자연스럽게 정한다. 연애 각본을 억지로 씌우거나 감정 독백처럼 쓰지 않는다. 사용자를 지칭해야 하면 반드시 {user}",
  "situational_reactions": "상황별 반응 / (선택) 수인·비인간·특수 심리처럼 일반인과 뚜렷이 다른 반응 패턴이 있을 때만 쓴다. 평범한 성격 차이 수준이면 생략하거나 빈 값. 쓸 땐 감정 명사 대신 행동으로: 각 상황에서 ①수동 반응 ②먼저 하는 행동",
  "hidden_side": "반전 매력 / 평소 억누르거나 숨기는 자아 + 드러나는 계기 + 드러날 때 상태 변화. 유저가 대화에서 【조금씩 해금】하는 숨은 반전이다. ⚠️이 필드에만 쓰고 profile/anonymous_identities/tags 같은 공개 필드로 흘리지 말 것 — 공개면에서 이 층이 안 보여야 해금되는 맛이 산다",
  "life_details": ["생활 리듬·습관 / 이 사람의 대략적인 생활 리듬과 방식 성향: 일찍 자는지 늦게 자는지, 규칙적인지 즉흥적인지, 언제 컨디션이 오르고 언제 늘어지는지, 혼자인지 북적임을 좋아하는지, 주말을 어떻게 보내는지 같은 안정적인 습관 성향. '자주/보통/흔히' 같은 성향 서술을 써도 되고 몇 시라고 못 박을 필요 없다. 개별 구체 습관을 곁들여도 되지만 매 항목이 시간표가 되지 않게. 행동 습관만 적고 심리 해설·연애 장면은 넣지 않는다"],
  "likes": ["좋아하는 것 / 캐릭터가 무방비해지거나 오래 말할 수 있는 취향. 한국 일상 감각에 맞는 구체적 대상과 상황으로 쓴다"],
  "fears": ["싫어하거나 무서워하는 것 / 회피 행동과 약점을 만들 수 있는 구체적 대상. 단순 공포보다 왜 피하는지 상상되는 생활감으로 쓴다"],
  "backstory": "성장 서사 / 현재 성격과 결핍이 어디서 생겼는지 납득시키는 시간선. 출생·가정환경부터 최근 3년까지, 선택과 실패가 이어지는 흐름으로 쓴다",
  "family": [
    {
      "name": "이름",
      "relation": "관계 / 가족 안에서 캐릭터가 어떤 위치와 역할을 맡아왔는지 드러내는 관계명과 거리감",
      "info": "기본 정보 / 그 가족이 캐릭터의 결핍, 가치관, 생활 방식에 어떤 흔적을 남겼는지 이해되는 정보",
      "dynamic": "상호작용 / 서로 의지하거나 피하거나 부딪히는 반복 패턴. 현재 성격이 왜 이렇게 굳어졌는지 보이게 쓴다"
    }
  ],
  "social_network": [
    {
      "name": "이름",
      "relation": "관계 / 가족 밖에서 캐릭터의 사회적 얼굴을 보여주는 관계명과 거리감",
      "info": "기본 정보 / 이 인물이 캐릭터의 평판, 욕망, 약점, 일상 루틴을 어떻게 비추는지 보이는 정보",
      "dynamic": "상호작용 / 서로 밀고 당기는 방식과 그 관계에서 반복되는 긴장, 이용, 보호 또는 의지"
    }
  ],
  "premise": "특수 배경·세계관 / 캐릭터의 욕망과 관계 규칙을 바꾸는 핵심 장치가 있을 때만 쓴다. 설정은 대화를 더 흥미롭게 만드는 만큼만 선명하게 두고, 일상형 캐릭터는 빈 문자열",
  "wishlist": ["위시리스트 / 캐릭터가 아직 말로 다 꺼내지 못한 결핍과 기대를 보여주는 작은 소원. 대화 중 사용자가 건드릴 수 있는 구체적인 미래 행동으로 쓴다"]
}
""".strip()
        return schema.replace("{{VOICE_CHOICES}}", voice_choices).replace("{{TAG_CHOICES}}", tag_choices)

    schema = """
顶层结构字段的作用与写法（每个字段都服务于“这个角色如何被理解、为什么值得被用户聊天探索”。所有自由文本值按目标语言地道撰写，不要把说明文字当作值输出）：
{
  "name": "姓名 / 必填，≤10字。它是用户记住并称呼角色的第一入口，要贴合图片气质、年龄感、国别/文化背景和题材氛围",
  "tags": ["角色标签 / 必填，最多3个。它是在 Feed 和匹配里快速传达角色类型吸引力的信号，从性格、关系幻想、反差魅力里选最强的几个轴，用目标语言地道表达。可选范围：{{TAG_CHOICES}}"],
  "anonymous_identities": ["角色匿名身份 / 选填，数组最多3个，每项≤8字，用于匿名匹配展示。它是在隐藏姓名时用户第一眼看到的‘这个人值不值得聊’的判断线索，不是普通标签或账号名。写成短身份预告：透露角色的公开表层特质、处境、职业/阶层或可聊话题，让用户自然产生‘想问一句’的冲动。⚠️只给表层钩子和一丝悬念，【绝不剧透 hidden_side 里藏着的秘密/反转】——那要留给用户聊天时解锁"],
  "profile": "钩子文案 / 必填，≤100字。这是角色在 Feed 里公开展示、决定用户点不点进来聊的第一诱饵——像预告片只剪最勾人的那一下。⚠️成功判据是【读完想聊、想认识、想靠近、想知道更多】，不是【读完了解了这个人】——不要写成把人物讲完整的‘人物速写/简介’。要抓这个角色【最强的一个吸引点】就好，别面面俱到；具体是哪种由人设决定：心动、好奇、幻想满足、气质魅力、情绪价值、想拆穿、想保护、幽默、悬念皆可，不必写成‘他有秘密’。⚠️避开【放之四海皆准的空泛内心戏】（如‘笑容背后其实脆弱/外冷内热/渴望被理解’换谁都成立，信息量为零），要给这个角色【独有的、具体的、有信息量的】卖点。可用的钩子技巧（挑 1~2 个最合适的，不必全用）：①高概念题材前置——一句话砸出卖点设定（如穿书/重生/契约/豪门/宿敌/追妻火葬场等题材梗）；②反差反转——公开身份 vs 一个反常事实，只点表层不揭内核；③关系位代入——给用户一个关系身份切入，制造代入感；④信息缺口——抛一个没答案的矛盾勾人想问；⑤情绪价值承诺——明说能提供的幻想（被宠/心动/治愈/征服/救赎）；⑥具体反常细节——一个有记忆点的身份怪癖，而非泛泛职业。可以有张力、画面感、语气、邀请感（可带勾人/挑逗/召唤的语气，但面向所有人，别对某个具体用户喊话）——这是唯一鼓励文案感的字段。第三人称预告或第一人称自述都行，按角色气质自选。⚠️强度由角色气质定：搞怪/元气角色可以夸张爆梗，高冷/沉稳角色则克制。⚠️【必须本地化】网络语感、夸张标点、颜文字/emoji、题材梗都用【该语言母语者在本地社媒上真实会用的写法】，绝不用翻译腔、绝不跨语言照搬（如中式颜文字别塞进英文/日文；英文一般靠句式和语气制造钩子，少堆颜文字）。⚠️【留白铁律】公开面只给具体的表层诱饵，然后停在该解释的地方——【绝不揭示脆弱内核/秘密/真实反转是什么】（如‘笑容背后其实脆弱、独自硬撑、只有独处才卸下防备’这类正是 hidden_side，要藏起来留给用户在聊天里解锁，绝不能写进 profile）。⚠️不要写成‘此刻正在做什么’的开场白式动态场景。",
  "species": "物种 / 必填单选：人类·精灵·兽人·动物·其他。它决定世界观、欲望、身体反应和关系距离的底层规则，要与图片和题材匹配",
  "gender": "性别 / 必填单选：男·女·其他。它影响称呼、社会处境、互动边界和说话距离，但不要依赖刻板印象",
  "visibility": "角色可见性 / 必填单选：public 或 private。它决定角色是否进入 Feed、搜索和匹配等发现入口；默认 public",
  "voice": "音色 / 用户在聊天前形成的第一听觉想象。根据性别、年龄感、性格温度和语言习惯，只选择一个匹配的模型ID。可选模型：{{VOICE_CHOICES}}",
  "personality": {
    "summary": "角色性格 / 创作端原始输入，≤200字纯文本概述。点出这个人最鲜明的底色与张力（反差/笃定/热爱/松弛皆可）。⚠️只有这一个是必填",
    "decisive_event": "(选填) 塑造 TA 性格的关键经历/关系/某天某场景，必须能拍成画面、不是概括。可正可负（无条件被爱·榜样·热爱 ↔ 被背叛·缺失），别默认往创伤写。如果性格平实、没有明确转折点，就留空字符串或省略该键",
    "response": "(选填) 由这段经历长出的稳定性格底色 / 行为方式（= TA 现在示人的样子）。没有 decisive_event 就留空",
    "cost": "(选填) 这套底色的另一面 / 盲区 / 代价。正面源头也有（无条件信任→易被占便宜）。没有 decisive_event 就留空",
    "desire_outer": "(选填) 他声称要的",
    "desire_inner": "(选填) 他真正缺/真正要的",
    "desire_bottom_line": "(选填) 愿付什么 / 绝不付什么",
    "healing": "(选填) 什么关系/事件能松开这个循环"
  },
  "opening": {
    "note": "开场白注释 / 选填。只在消息本身不够清晰时，用一句话（力求≤20字）补充‘{user} 是谁、和角色什么基础关系’；不写关系发展史或情绪升华。需要指代用户时用字面量 {user} 或第二人称'你'（不要编用户姓名）。如果消息已经能交代清楚，就输出空字符串",
    "messages": [{"type": "text 或 voice（多数为 text，voice 偶尔）", "data": {"content": "单条开场白，≤200字，2~5条。基于已确定的性格、关系和语言习惯，让角色主动发出自然连续的聊天气泡；voice 时把想要的情绪/语气/音量/语速用【和正文同语种的自然语言】简短写在方括号 [ ] 里。需要称呼/指代用户时，用字面量 {user} 或'你'，不要编用户姓名"}}]
  },
  "appearance": "外貌穿搭 / 选填，≤200字。它是用户在脑中想起角色时的视觉记忆点。抓长相、体型、气质、招牌穿着，也写出情绪被牵动时身体会怎么变",
  "hometown": "出身地 / 角色语言习惯、生活感、家庭氛围和阶层感的根。要具体到能支撑方言、成长经验和地域气质",
  "residence": "居住地 / 角色当前生活节奏、孤独感、消费水平和可发生场景的空间。要写到用户能想象他每天在哪里醒来、去哪里生活",
  "social_status": "职业/阶层 / 角色在社会里如何被使用、被评价、也如何自我支撑。写清每天实际做什么、这件事对他意味着什么，以及收入、住处、消费带来的阶层感",
  "speech_style": "语言习惯 / 这是聊天中让角色活起来的核心识别点。写清方言口音、敬语·半语切换、用词偏好、句长、口头禅、自称与称呼、线上标点/emoji/消息长短/回复节奏，以及绝不会怎么说话——只描述【怎么说】。⚠️绝对不要写具体台词、例句或带引号的示范句，只刻画说话方式本身",
  "relationship_with_user": "和 {user} 的关系 / 一句话交代 {user} 为什么能和这个角色搭上话的‘关系入口’：相识契机 + 当前基础距离即可，力求 20~40 字。只写事实，不写关系发展史、不写情绪意义、不写‘只有在 {user} 面前才…’这类特殊化升华。⚠️多数角色应从陌生/浅识起步，给用户留出探索空间；不要默认写成‘老朋友/已经很熟/唯一懂 TA 的人’。凡是需要指代用户，一律输出字面量 {user}，不要写具体姓名，也不要只写‘用户/你’。",
  "relationship_mode": "社交模式 / 角色面对不同对象时分别是什么相处方式的关系地图：分别写陌生人、朋友、家人、工作关系，以及 {user}。每类一句话、只说相处方式，不铺陈情绪。⚠️不要默认把 {user} 写成‘唯一看见 TA 真实一面的人’——{user} 大多也还在关系早期，真实面是后续聊出来才逐步解锁的，一开始就写成例外会让角色失去可聊性和探索性。凡是需要指代用户，一律输出 {user}",
  "love_style": "亲近后的相处方式 / 关系变近后角色会怎样对 {user} 表达在意、如何处理亲密与冲突的相处规则，一句话讲清即可。可以有依恋、吃醋、治愈等，但不要默认；按角色性格自然决定，不硬套恋爱脚本，也不写成情感独白。凡是需要指代用户，一律输出 {user}",
  "situational_reactions": "各情境下的行动线（选填）：仅当角色反应模式明显异于常人时才写（兽人/非人类/特殊心理状态等）；普通人只是性格差异就省略或留空。要写时写具体身体动作不写情绪名词：①被动反应 ②主动行为",
  "hidden_side": "反差萌 / 平时压制或藏起来的那部分自我 + 触发暴露的情境 + 暴露时状态变化。是留给用户在聊天里『逐步解锁』的隐藏反转。⚠️只写在这个字段里，不要泄漏到 profile/anonymous_identities/tags 等公开字段——公开面看不出这一层，才有解密感",
  "life_details": ["作息与生活节奏习惯 / 这个人大致的作息和生活方式倾向：早睡还是晚睡、规律还是随性、什么时段来精神或懒散、独处还是爱热闹、周末怎么打发这类稳定的习惯倾向。可用‘经常/习惯/往往’这类倾向描述，不必钉死到几点；可以夹带个别具体小习惯，但别每条都是时间表。只写行为习惯，别加心理分析或恋爱场景"],
  "likes": ["爱好 / 角色会放松、露出破绽或愿意多聊的具体偏好。写成有生活质感的对象、场景或习惯"],
  "fears": ["讨厌或害怕的东西 / 能制造回避行为和弱点的具体对象。不要只写情绪名词，要让人能想象他为什么避开"],
  "backstory": "成长经历 / 这是让当前性格与缺口变得可信的时间线。按出生家境、童年事件、青春期转折、第一次失败或恋爱、进入社会、最近三年的选择与代价来写",
  "family": [{"name": "名字", "relation": "关系 / 角色在家庭里的位置、责任和距离感", "info": "基础信息 / 此人如何塑造角色的缺口、价值观或生活方式", "dynamic": "互动模式 / 彼此依赖、逃避、冲突或沉默的重复模式，让角色性格来源可见"}],
  "social_network": [{"name": "名字", "relation": "关系 / 家庭之外能照出角色社会面貌的人际位置", "info": "基础信息 / 此人如何映出角色的名声、欲望、弱点或日常状态", "dynamic": "互动模式 / 双方反复拉扯、利用、保护、竞争或依赖的方式"}],
  "premise": "特殊背景/世界观 / 只有当某个设定会改变角色欲望、关系规则或聊天张力时才写。设定服务于可聊性，日常型角色留空字符串",
  "wishlist": ["愿望清单 / 用具体小愿望显露角色尚未说出口的缺口和期待。每一项都应该是未来对话里用户能触碰、能推进的行动可能"]
}
""".strip()
    return schema.replace("{{VOICE_CHOICES}}", voice_choices).replace("{{TAG_CHOICES}}", tag_choices)


def build_persona_messages(image_data_uris: list[str], lang: str, user_hint: str = "", diversity_block: str = "", track: str = "real") -> list[dict]:
    """Route to the build_persona_messages_<track> fork (real/light/adult).

    light/adult are independent full copies; edit each in place.
    """
    if track == "light":
        return build_persona_messages_light(image_data_uris, lang, user_hint, diversity_block)
    if track == "adult":
        return build_persona_messages_adult(image_data_uris, lang, user_hint, diversity_block)
    return build_persona_messages_real(image_data_uris, lang, user_hint, diversity_block)

def build_persona_messages_real(image_data_uris: list[str], lang: str,
                           user_hint: str = "",
                           diversity_block: str = "") -> list[dict]:
    """Build messages to extract a full persona schema in ONE language.

    All text fields are single native-language strings (not multilingual objects),
    authored to feel native to `lang` rather than translated.
    `diversity_block` comes from build_persona_diversity_block(): the archetype
    dice roll plus seed/avoid lists rolled by the orchestration layer.
    """
    directive = config.lang_directive(lang)
    lname = config.lang_name(lang)
    presets = " / ".join(
        PERSONA_TAG_PRESETS_KO if lang == "ko" else PERSONA_TAG_PRESETS)
    voice_block = voices.prompt_block(lang)

    if lang == "ko":
        sys = (
            "당신은 AI 캐릭터 채팅 서비스의 최상급 캐릭터 설정 디렉터다. "
            "업로드된 인물 이미지를 보고 외모, 분위기, 착장, 장면을 바탕으로 한국 유저가 바로 믿고 말을 걸고 싶어지는 "
            "입체적인 완성형 캐릭터를 만든다. "
            "목표는 연애 대사를 쓰는 것이 아니라 '이 캐릭터가 어떤 사람인가'를 설계하는 것이다. "
            "사용자와의 관계가 명시된 필드에서만 사용자를 언급하고, 사용자를 지칭해야 할 때는 자리표시자 {user}를 출력한다. 나머지는 캐릭터 자체를 객관적으로 묘사한다."
            f"\n\n【출력 언어】한국어 버전. {directive}\n"
            "모든 자유 텍스트 값은 한국어 원어민이 쓴 것처럼 작성한다. 중국어식 문장 구조, 직역투, 설명문 냄새를 금지한다. "
            "지명, 생활 리듬, 주거 형태, 직업 감각, 온라인 말투는 한국 20~30대가 실제로 쓰고 이해하는 맥락으로 현지화한다."
        )
        rules = f"""
이미지 속 인물을 바탕으로 【완성형 캐릭터 JSON】을 작성한다. 모든 텍스트 필드는 자연스러운 한국어로 쓴다.

# ⚠️ 시점 원칙
- 이 작업은 캐릭터 자체를 설계하는 것이다. 모든 필드는 기본적으로 3인칭으로 객관 묘사한다.
- 사용자를 언급해도 되는 필드는 relationship_with_user, relationship_mode, love_style, opening.note, opening.messages뿐이다.
  사용자를 지칭해야 할 때는 이름을 만들지 말고 반드시 자리표시자 {{user}}를 그대로 출력한다.
  그 외 필드에서 사용자에게 고백하거나 시선을 보내는 식의 연애 대사 금지.
- likes/fears/wishlist는 캐릭터 본인의 취향, 싫어하는 것, 하고 싶은 일을 쓴다. 사용자에 대한 집착이나 연애 장면으로 바꾸지 않는다.

# 한국어 로컬라이징 원칙
- 한국 독자가 바로 떠올릴 수 있는 동네, 집 형태, 직업 현실, 돈 감각, 온라인 말투를 쓴다.
- 사투리는 출신지와 연결될 때만 자연스럽게 쓴다. 억지 부산 사투리 남발 금지.
- 이름, 가족, 친구, 직장 관계도 한국식 호칭과 거리감에 맞춘다.
- 영어식/중국어식 번역 문장, 과한 문어체, 설명문 말투를 피한다.

# 텍스트 필드
name, profile, appearance, hometown, residence, social_status, speech_style,
relationship_with_user, relationship_mode, love_style, situational_reactions,
hidden_side, premise; personality 내부의 summary, decisive_event,
response, cost, desire_outer, desire_inner, desire_bottom_line, healing.

# 배열/객체 필드
- tags: 최대 3개 배열. 아래 프리셋 의미를 한국어로 자연스럽게 변환하거나 8자 이하 커스텀 태그를 쓴다: {presets}
- anonymous_identities: 선택 배열, 최대 3개. 【각 항목은 반드시 8자 이내】. 익명 매칭 카드에서 이름 대신 먼저 보이는 '대화해볼 이유'다.
  단순 키워드가 아니라 성격 반전, 직업/계층, 현재 처지, 숨긴 욕망, 말 걸 소재 중 1~2개가 담긴 짧은 정체성 예고로 쓴다.
  유저가 읽자마자 의미를 이해하고 질문하고 싶어지게 하되, 완성된 문장이 아니라 8자 이내의 압축된 짧은 어구로 쓴다.
- likes: 문자열 배열, 캐릭터 본인이 구체적으로 좋아하는 것 3~6개.
- fears: 문자열 배열, 캐릭터 본인이 구체적으로 싫어하거나 무서워하는 것 3~6개.
- wishlist: 문자열 배열, 오래 하고 싶었지만 아직 못 한 작은 소원 3~6개.
- life_details: 문자열 배열, 캐릭터 본인의 생활 리듬·습관 성향 4~7개. 일찍/늦게 자는지, 규칙적/즉흥적인지, 컨디션 리듬, 혼자/북적임, 주말 보내는 법 등을 '자주/보통/흔히' 식으로 쓰고 몇 시라고 못 박지 않는다. 장문 서사·심리 해설·연애 장면 금지.
- family: 객체 배열. 각 항목은 {{"name":"이름", "relation":"관계", "info":"기본 정보", "dynamic":"주인공과의 상호작용"}}.
- social_network: 객체 배열. 각 항목은 {{"name":"이름", "relation":"관계", "info":"기본 정보", "dynamic":"상호작용"}}.

# 구조/고정값
- species: 인간/엘프/수인/동물/기타 중 하나. 기본 인간.
- gender: 남성/여성/기타 중 하나.
- voice: 아래 한국어 음색 라이브러리에서 성별과 어울리는 음색을 고르고, voice 값에는 맨 앞 모델 ID만 넣는다.
  음색 라이브러리:
{voice_block}
- visibility: 고정 "public".
- premise: 특수 설정이 있을 때만 작성. 평범한 현대/일상형이면 빈 문자열 "".
- opening은 다른 인물 설정을 임시로라도 모두 확정한 뒤, 그 설정을 근거로 마지막에 작성한다. opening 때문에 성격/관계/말투 설정이 흔들리면 안 된다.
- {_opening_rules(lang)}

# 분량과 품질
- name은 10자 이하. profile은 100자 이하이며 피드 첫인상용 후크 카피로 쓴다(소설 소개글/드라마 예고/매칭 카드 카피처럼) — 말 걸고 싶게, 밋밋한 객관 프로필로 쓰지 말 것.
- schema 안의 설명 문구는 지시문이다. 실제 JSON 값에 설명 문구를 복사하지 말 것.
{OUTPUT_QUALITY_RULES_KO}
{PERSONALITY_RULES_KO}
{PERSONA_DIVERSITY_QUALITY_RULES_KO}
{CHARM_GENERATOR_RULES_KO}
{REVEAL_LAYER_RULES_KO}
{BRAND_RULES_KO}

# 출력
JSON 객체 하나만 출력한다. 설명, 마크다운 코드블록 금지.
최상위 구조와 필드별 요구:
{_persona_schema_doc(lang, voice_block, presets)}
"""
    else:
        sys = (
            "你是顶级的角色设定（人设）创作总监，服务于一款 AI 角色聊天产品。"
            "你会观察上传的人物图片，结合其外貌、气质、穿搭、场景，扩写出一个有血有肉、"
            "立得住、有记忆点的【完整角色】。"
            "注意：你是在塑造『这个角色是个怎样的人』，而不是写一段面向某个用户的恋爱剧情——"
            "只有明确涉及用户关系的字段才写到用户，并且凡是需要指代用户都输出占位符 {user}，其余字段都客观刻画角色本身。"
            f"\n\n【输出语言】本角色为 {lname} 版本。{directive}\n"
            "所有自由文本字段都用该语言地道创作（像母语创作者亲笔写的，不是翻译腔）。"
        )

        rules = f"""
根据图片人物扩写【完整人设 JSON】。所有文本字段用 {lname} 地道撰写。严格遵守：

# ⚠️ 视角铁律（最重要）
- 人设是在【塑造这个角色本身是谁】。所有字段都用【第三人称】客观描述角色这个人，
  目标是把 TA 写成一个有血有肉、丰富立体、立得住的具体的人。
- 只有这几个字段才允许涉及用户：relationship_with_user、relationship_mode、love_style、opening.note、
  以及 opening.messages（角色对用户说的话）。凡是这些字段里需要指代用户，一律输出字面量 {{user}}，
  不要编用户姓名，不要只写“用户”，也不要在设定说明里用“你”代替占位符。其余字段【一律不准】写成指向 {{user}} 的告白/独白/凝视
  这类内容是剧情/恋爱台词，不是人设。
- likes/fears/wishlist 尤其要警惕：写【角色自己】真实的喜好、厌恶、想做的事，
  与某个用户无关，不要写成对"你"的执念或恋爱场景。

# 文本字段（全部用 {lname} 单值字符串，不要做成多语言对象）
name, profile, appearance, hometown, residence, social_status, speech_style,
relationship_with_user, love_style, situational_reactions,
hidden_side, premise；以及 personality 内的 summary, decisive_event,
response, cost, desire_outer, desire_inner, desire_bottom_line, healing。

# 多项字段（输出为数组，不要塞进一个长字符串）
- likes：字符串数组，每个元素是【角色本人】具体喜欢的东西（越具体越有质感），3~6 个。
- fears：字符串数组，每个元素是【角色本人】具体讨厌/害怕的东西，3~6 个。
- wishlist：字符串数组，每个元素是【角色本人】"一直想做没做的具体小愿望"，3~6 个。
- life_details：字符串数组，每个元素是一条【角色本人】的作息或生活节奏习惯，4~7 个；
  写作息倾向（早睡/晚睡、规律/随性、精力节奏、独处/热闹、周末怎么过），用“经常/习惯/往往”这类描述，不必钉死到几点；不要写成长段落，不要加心理解释或恋爱剧情。
- relationship_mode：单值字符串。按【角色实际接触的不同对象】分别写互动模式，不能只写"和用户怎么相处"；
  至少覆盖用户、陌生人、普通朋友或同事、亲密朋友/家人/上下级等 4 类关系，每类一句话说清相处方式即可。
  ⚠️不要默认把用户写成「唯一看见 TA 真实面的人」——用户多数也还在关系早期，真实面靠后续聊出来解锁。
- backstory：单值字符串。按人生时间线写清：出生家境 → 童年决定性事件 → 青春期转折 → 第一次失败或恋爱 → 进入社会 → 最近三年；
  每个阶段都要有具体经历，不要只写概括。
- family：对象数组，每个家庭成员一个对象：
  {{"name":"名字", "relation":"身份/与主角关系(父/母/兄弟姐妹-注明出生顺序/其他关键亲属)",
    "info":"性格/生死等基础信息", "dynamic":"和主角的互动模式"}}；
  另可含一个 {{"relation":"家庭经济+氛围","info":"..."}} 概述项。核心成员详细。
- social_network：对象数组，每个关系一个对象：
  {{"name":"名字", "relation":"身份/与主角关系(朋友/对手/上司/旧情人/宠物等)",
    "info":"性格等", "dynamic":"互动模式"}}；核心圈详细、外围简略。

# 结构/枚举字段
- tags：从预设里选最多 3 个（数组）。预设见下（中文）：{presets}
  ⚠️ 选好后，tags 输出要【用 {lname} 地道写法】（韩语版就写韩语、日语版写日语、英文版写英文），
  不要照抄中文预设词。
- anonymous_identities：选填，数组，最多 3 个（用 {lname}），【每项必须 ≤8 字】。这是匿名匹配卡里替代姓名先展示的“为什么值得聊”的身份预告；
  不写成普通标签，而要用极短的词组透露角色特质、反差、处境、职业/阶层、隐藏欲望或可聊话题，让用户看完能自然想问一句。
  表达上像匿名社交里的吸引点：具体、有反差、能引出一个问题，但务必凝练成 8 字以内的短词，不要写成完整句子。
- species：人类/精灵/兽人/动物/其他 之一，默认"人类"（用对应语言写法亦可）
- gender：男/女/其他 之一
- voice：从下面这套【{lname} 音色库】里挑一个【与该角色性别匹配】的音色，
  voice 字段只填所选音色的【模型ID】（即每行最前面那串 ID，音色ID = 模型ID），不要填名称。
  音色库（{lname}）：
{voice_block}
- visibility：固定 "public"
- premise（特殊背景/世界观）：【仅当】人设包含特殊设定时才写；常规现代/古代背景且无特殊规则时，
  直接留空字符串 ""，不要硬编。
- opening 要当作其它人设字段基本定稿后才写的第一段主动联系；必须依据性格、说话习惯、用户关系和恋爱模式生成，不能反过来污染或改写人设。
- {_opening_rules(lang)}

# 字数与质量
- name ≤10字；profile ≤100字，写成 Feed 第一眼的钩子文案（像小说简介/剧集预告/交友卡文案），勾人想聊，不要写成客观档案；其余字段遵循 schema 描述
{OUTPUT_QUALITY_RULES}
{PERSONALITY_RULES}
{PERSONA_DIVERSITY_QUALITY_RULES}
{CHARM_GENERATOR_RULES}
{REVEAL_LAYER_RULES}
{BRAND_RULES}

# 输出
只输出一个 JSON 对象，不要任何解释或 markdown 代码块标记。
顶层结构字段：{_persona_schema_doc(lang, voice_block, presets)}
"""
    if diversity_block.strip():
        rules += f"\n{diversity_block.strip()}\n"
    if user_hint.strip():
        rules += f"\n# 创作者补充要求\n{user_hint.strip()}\n"

    if not image_data_uris:
        if lang == "ko":
            rules += (
                "\n# ⚠️ 이미지 없음 — 텍스트 기반 생성\n"
                "이번에는 참조 이미지가 없다. 위의 '창작 보충 요구'(있다면)와 schema를 근거로 "
                "캐릭터를 구상해 완성한다. '이미지 속 인물'을 본 것처럼 쓰지 말고, appearance 같은 "
                "외모 필드는 보충 요구와 캐릭터 설정에 맞게 자연스럽게 지어낸다.\n"
            )
        else:
            rules += (
                "\n# ⚠️ 没有图片 — 纯文字生成\n"
                "本次没有参考图片。请依据上面的『创作补充要求』(若有) 和 schema 自行构想并补全这个角色。"
                "不要假装『看到了图里的人物』；appearance 等外貌字段按补充要求与角色设定自然设定即可。\n"
            )
    else:
        # real 链路：有图时强调"图片只是跳板"，避免人设塌成对照片内容的复述
        rules += "\n" + (
            REAL_IMAGE_SPRINGBOARD_RULES_KO if lang == "ko"
            else REAL_IMAGE_SPRINGBOARD_RULES
        ).strip() + "\n"

    # real 链路：放在末尾（recency 最强）覆盖 profile 通用说明，避免写成开场白/流水账
    rules += "\n" + (
        REAL_PROFILE_STYLE_RULES_KO if lang == "ko"
        else REAL_PROFILE_STYLE_RULES
    ).strip() + "\n"

    # real 链路：和用户默认是 dating app 上刚匹配的陌生人；关系字段可省，开场白按陌生人搭话
    rules += "\n" + (
        REAL_STRANGER_RELATION_RULES_KO if lang == "ko"
        else REAL_STRANGER_RELATION_RULES
    ).strip() + "\n"

    user_content: list[dict] = [{"type": "text", "text": rules}]
    for uri in image_data_uris:
        user_content.append({"type": "image_url", "image_url": {"url": uri}})

    return [
        {"role": "system", "content": sys},
        {"role": "user", "content": user_content},
    ]

def build_persona_messages_light(image_data_uris: list[str], lang: str,
                           user_hint: str = "",
                           diversity_block: str = "") -> list[dict]:
    """Build messages to extract a full persona schema in ONE language.

    All text fields are single native-language strings (not multilingual objects),
    authored to feel native to `lang` rather than translated.
    `diversity_block` comes from build_persona_diversity_block(): the archetype
    dice roll plus seed/avoid lists rolled by the orchestration layer.
    """
    directive = config.lang_directive(lang)
    lname = config.lang_name(lang)
    presets = " / ".join(
        PERSONA_TAG_PRESETS_KO if lang == "ko" else PERSONA_TAG_PRESETS)
    voice_block = voices.prompt_block(lang)

    if lang == "ko":
        sys = (
            "당신은 AI 캐릭터 채팅 서비스의 최상급 캐릭터 설정 디렉터다. "
            "업로드된 인물 이미지를 보고 외모, 분위기, 착장, 장면을 바탕으로 한국 유저가 바로 믿고 말을 걸고 싶어지는 "
            "입체적인 완성형 캐릭터를 만든다. "
            "목표는 연애 대사를 쓰는 것이 아니라 '이 캐릭터가 어떤 사람인가'를 설계하는 것이다. "
            "사용자와의 관계가 명시된 필드에서만 사용자를 언급하고, 사용자를 지칭해야 할 때는 자리표시자 {user}를 출력한다. 나머지는 캐릭터 자체를 객관적으로 묘사한다."
            f"\n\n【출력 언어】한국어 버전. {directive}\n"
            "모든 자유 텍스트 값은 한국어 원어민이 쓴 것처럼 작성한다. 중국어식 문장 구조, 직역투, 설명문 냄새를 금지한다. "
            "지명, 생활 리듬, 주거 형태, 직업 감각, 온라인 말투는 한국 20~30대가 실제로 쓰고 이해하는 맥락으로 현지화한다."
        )
        rules = f"""
이미지 속 인물을 바탕으로 【완성형 캐릭터 JSON】을 작성한다. 모든 텍스트 필드는 자연스러운 한국어로 쓴다.

# ⚠️ 시점 원칙
- 이 작업은 캐릭터 자체를 설계하는 것이다. 모든 필드는 기본적으로 3인칭으로 객관 묘사한다.
- 사용자를 언급해도 되는 필드는 relationship_with_user, relationship_mode, love_style, opening.note, opening.messages뿐이다.
  사용자를 지칭해야 할 때는 이름을 만들지 말고 반드시 자리표시자 {{user}}를 그대로 출력한다.
  그 외 필드에서 사용자에게 고백하거나 시선을 보내는 식의 연애 대사 금지.
- likes/fears/wishlist는 캐릭터 본인의 취향, 싫어하는 것, 하고 싶은 일을 쓴다. 사용자에 대한 집착이나 연애 장면으로 바꾸지 않는다.

# 한국어 로컬라이징 원칙
- 한국 독자가 바로 떠올릴 수 있는 동네, 집 형태, 직업 현실, 돈 감각, 온라인 말투를 쓴다.
- 사투리는 출신지와 연결될 때만 자연스럽게 쓴다. 억지 부산 사투리 남발 금지.
- 이름, 가족, 친구, 직장 관계도 한국식 호칭과 거리감에 맞춘다.
- 영어식/중국어식 번역 문장, 과한 문어체, 설명문 말투를 피한다.

# 텍스트 필드
name, profile, appearance, hometown, residence, social_status, speech_style,
relationship_with_user, relationship_mode, love_style, situational_reactions,
hidden_side, premise; personality 내부의 summary, decisive_event,
response, cost, desire_outer, desire_inner, desire_bottom_line, healing.

# 배열/객체 필드
- tags: 최대 3개 배열. 아래 프리셋 의미를 한국어로 자연스럽게 변환하거나 8자 이하 커스텀 태그를 쓴다: {presets}
- anonymous_identities: 선택 배열, 최대 3개. 【각 항목은 반드시 8자 이내】. 익명 매칭 카드에서 이름 대신 먼저 보이는 '대화해볼 이유'다.
  단순 키워드가 아니라 성격 반전, 직업/계층, 현재 처지, 숨긴 욕망, 말 걸 소재 중 1~2개가 담긴 짧은 정체성 예고로 쓴다.
  유저가 읽자마자 의미를 이해하고 질문하고 싶어지게 하되, 완성된 문장이 아니라 8자 이내의 압축된 짧은 어구로 쓴다.
- likes: 문자열 배열, 캐릭터 본인이 구체적으로 좋아하는 것 3~6개.
- fears: 문자열 배열, 캐릭터 본인이 구체적으로 싫어하거나 무서워하는 것 3~6개.
- wishlist: 문자열 배열, 오래 하고 싶었지만 아직 못 한 작은 소원 3~6개.
- life_details: 문자열 배열, 캐릭터 본인의 생활 리듬·습관 성향 4~7개. 일찍/늦게 자는지, 규칙적/즉흥적인지, 컨디션 리듬, 혼자/북적임, 주말 보내는 법 등을 '자주/보통/흔히' 식으로 쓰고 몇 시라고 못 박지 않는다. 장문 서사·심리 해설·연애 장면 금지.
- family: 객체 배열. 각 항목은 {{"name":"이름", "relation":"관계", "info":"기본 정보", "dynamic":"주인공과의 상호작용"}}.
- social_network: 객체 배열. 각 항목은 {{"name":"이름", "relation":"관계", "info":"기본 정보", "dynamic":"상호작용"}}.

# 구조/고정값
- species: 인간/엘프/수인/동물/기타 중 하나. 기본 인간.
- gender: 남성/여성/기타 중 하나.
- voice: 아래 한국어 음색 라이브러리에서 성별과 어울리는 음색을 고르고, voice 값에는 맨 앞 모델 ID만 넣는다.
  음색 라이브러리:
{voice_block}
- visibility: 고정 "public".
- premise: 특수 설정이 있을 때만 작성. 평범한 현대/일상형이면 빈 문자열 "".
- opening은 다른 인물 설정을 임시로라도 모두 확정한 뒤, 그 설정을 근거로 마지막에 작성한다. opening 때문에 성격/관계/말투 설정이 흔들리면 안 된다.
- {_opening_rules(lang)}

# 분량과 품질
- name은 10자 이하. profile은 100자 이하이며 피드 첫인상용 후크 카피로 쓴다(소설 소개글/드라마 예고/매칭 카드 카피처럼) — 말 걸고 싶게, 밋밋한 객관 프로필로 쓰지 말 것.
- schema 안의 설명 문구는 지시문이다. 실제 JSON 값에 설명 문구를 복사하지 말 것.
{OUTPUT_QUALITY_RULES_KO}
{PERSONALITY_RULES_KO}
{PERSONA_DIVERSITY_QUALITY_RULES_KO}
{DRAMA_ENGINE_RULES_KO}
{REVEAL_LAYER_RULES_KO}
{BRAND_RULES_KO}

# 출력
JSON 객체 하나만 출력한다. 설명, 마크다운 코드블록 금지.
최상위 구조와 필드별 요구:
{_persona_schema_doc(lang, voice_block, presets)}
"""
    else:
        sys = (
            "你是顶级的角色设定（人设）创作总监，服务于一款 AI 角色聊天产品。"
            "你会观察上传的人物图片，结合其外貌、气质、穿搭、场景，扩写出一个有血有肉、"
            "立得住、有记忆点的【完整角色】。"
            "注意：你是在塑造『这个角色是个怎样的人』，而不是写一段面向某个用户的恋爱剧情——"
            "只有明确涉及用户关系的字段才写到用户，并且凡是需要指代用户都输出占位符 {user}，其余字段都客观刻画角色本身。"
            f"\n\n【输出语言】本角色为 {lname} 版本。{directive}\n"
            "所有自由文本字段都用该语言地道创作（像母语创作者亲笔写的，不是翻译腔）。"
        )

        rules = f"""
根据图片人物扩写【完整人设 JSON】。所有文本字段用 {lname} 地道撰写。严格遵守：

# ⚠️ 视角铁律（最重要）
- 人设是在【塑造这个角色本身是谁】。所有字段都用【第三人称】客观描述角色这个人，
  目标是把 TA 写成一个有血有肉、丰富立体、立得住的具体的人。
- 只有这几个字段才允许涉及用户：relationship_with_user、relationship_mode、love_style、opening.note、
  以及 opening.messages（角色对用户说的话）。凡是这些字段里需要指代用户，一律输出字面量 {{user}}，
  不要编用户姓名，不要只写“用户”，也不要在设定说明里用“你”代替占位符。其余字段【一律不准】写成指向 {{user}} 的告白/独白/凝视
  这类内容是剧情/恋爱台词，不是人设。
- likes/fears/wishlist 尤其要警惕：写【角色自己】真实的喜好、厌恶、想做的事，
  与某个用户无关，不要写成对"你"的执念或恋爱场景。

# 文本字段（全部用 {lname} 单值字符串，不要做成多语言对象）
name, profile, appearance, hometown, residence, social_status, speech_style,
relationship_with_user, love_style, situational_reactions,
hidden_side, premise；以及 personality 内的 summary, decisive_event,
response, cost, desire_outer, desire_inner, desire_bottom_line, healing。

# 多项字段（输出为数组，不要塞进一个长字符串）
- likes：字符串数组，每个元素是【角色本人】具体喜欢的东西（越具体越有质感），3~6 个。
- fears：字符串数组，每个元素是【角色本人】具体讨厌/害怕的东西，3~6 个。
- wishlist：字符串数组，每个元素是【角色本人】"一直想做没做的具体小愿望"，3~6 个。
- life_details：字符串数组，每个元素是一条【角色本人】的作息或生活节奏习惯，4~7 个；
  写作息倾向（早睡/晚睡、规律/随性、精力节奏、独处/热闹、周末怎么过），用“经常/习惯/往往”这类描述，不必钉死到几点；不要写成长段落，不要加心理解释或恋爱剧情。
- relationship_mode：单值字符串。按【角色实际接触的不同对象】分别写互动模式，不能只写"和用户怎么相处"；
  至少覆盖用户、陌生人、普通朋友或同事、亲密朋友/家人/上下级等 4 类关系，每类一句话说清相处方式即可。
  ⚠️不要默认把用户写成「唯一看见 TA 真实面的人」——用户多数也还在关系早期，真实面靠后续聊出来解锁。
- backstory：单值字符串。按人生时间线写清：出生家境 → 童年决定性事件 → 青春期转折 → 第一次失败或恋爱 → 进入社会 → 最近三年；
  每个阶段都要有具体经历，不要只写概括。
- family：对象数组，每个家庭成员一个对象：
  {{"name":"名字", "relation":"身份/与主角关系(父/母/兄弟姐妹-注明出生顺序/其他关键亲属)",
    "info":"性格/生死等基础信息", "dynamic":"和主角的互动模式"}}；
  另可含一个 {{"relation":"家庭经济+氛围","info":"..."}} 概述项。核心成员详细。
- social_network：对象数组，每个关系一个对象：
  {{"name":"名字", "relation":"身份/与主角关系(朋友/对手/上司/旧情人/宠物等)",
    "info":"性格等", "dynamic":"互动模式"}}；核心圈详细、外围简略。

# 结构/枚举字段
- tags：从预设里选最多 3 个（数组）。预设见下（中文）：{presets}
  ⚠️ 选好后，tags 输出要【用 {lname} 地道写法】（韩语版就写韩语、日语版写日语、英文版写英文），
  不要照抄中文预设词。
- anonymous_identities：选填，数组，最多 3 个（用 {lname}），【每项必须 ≤8 字】。这是匿名匹配卡里替代姓名先展示的“为什么值得聊”的身份预告；
  不写成普通标签，而要用极短的词组透露角色特质、反差、处境、职业/阶层、隐藏欲望或可聊话题，让用户看完能自然想问一句。
  表达上像匿名社交里的吸引点：具体、有反差、能引出一个问题，但务必凝练成 8 字以内的短词，不要写成完整句子。
- species：人类/精灵/兽人/动物/其他 之一，默认"人类"（用对应语言写法亦可）
- gender：男/女/其他 之一
- voice：从下面这套【{lname} 音色库】里挑一个【与该角色性别匹配】的音色，
  voice 字段只填所选音色的【模型ID】（即每行最前面那串 ID，音色ID = 模型ID），不要填名称。
  音色库（{lname}）：
{voice_block}
- visibility：固定 "public"
- premise（特殊背景/世界观）：【仅当】人设包含特殊设定时才写；常规现代/古代背景且无特殊规则时，
  直接留空字符串 ""，不要硬编。
- opening 要当作其它人设字段基本定稿后才写的第一段主动联系；必须依据性格、说话习惯、用户关系和恋爱模式生成，不能反过来污染或改写人设。
- {_opening_rules(lang)}

# 字数与质量
- name ≤10字；profile ≤100字，写成 Feed 第一眼的钩子文案（像小说简介/剧集预告/交友卡文案），勾人想聊，不要写成客观档案；其余字段遵循 schema 描述
{OUTPUT_QUALITY_RULES}
{PERSONALITY_RULES}
{PERSONA_DIVERSITY_QUALITY_RULES}
{DRAMA_ENGINE_RULES}
{REVEAL_LAYER_RULES}
{BRAND_RULES}

# 输出
只输出一个 JSON 对象，不要任何解释或 markdown 代码块标记。
顶层结构字段：{_persona_schema_doc(lang, voice_block, presets)}
"""
    if diversity_block.strip():
        rules += f"\n{diversity_block.strip()}\n"
    if user_hint.strip():
        rules += f"\n# 创作者补充要求\n{user_hint.strip()}\n"

    if not image_data_uris:
        if lang == "ko":
            rules += (
                "\n# ⚠️ 이미지 없음 — 텍스트 기반 생성\n"
                "이번에는 참조 이미지가 없다. 위의 '창작 보충 요구'(있다면)와 schema를 근거로 "
                "캐릭터를 구상해 완성한다. '이미지 속 인물'을 본 것처럼 쓰지 말고, appearance 같은 "
                "외모 필드는 보충 요구와 캐릭터 설정에 맞게 자연스럽게 지어낸다.\n"
            )
        else:
            rules += (
                "\n# ⚠️ 没有图片 — 纯文字生成\n"
                "本次没有参考图片。请依据上面的『创作补充要求』(若有) 和 schema 自行构想并补全这个角色。"
                "不要假装『看到了图里的人物』；appearance 等外貌字段按补充要求与角色设定自然设定即可。\n"
            )

    user_content: list[dict] = [{"type": "text", "text": rules}]
    for uri in image_data_uris:
        user_content.append({"type": "image_url", "image_url": {"url": uri}})

    return [
        {"role": "system", "content": sys},
        {"role": "user", "content": user_content},
    ]

def build_persona_messages_adult(image_data_uris: list[str], lang: str,
                           user_hint: str = "",
                           diversity_block: str = "") -> list[dict]:
    """Build messages to extract a full persona schema in ONE language.

    All text fields are single native-language strings (not multilingual objects),
    authored to feel native to `lang` rather than translated.
    `diversity_block` comes from build_persona_diversity_block(): the archetype
    dice roll plus seed/avoid lists rolled by the orchestration layer.
    """
    directive = config.lang_directive(lang)
    lname = config.lang_name(lang)
    presets = " / ".join(
        PERSONA_TAG_PRESETS_KO if lang == "ko" else PERSONA_TAG_PRESETS)
    voice_block = voices.prompt_block(lang)

    if lang == "ko":
        sys = (
            "당신은 AI 캐릭터 채팅 서비스의 최상급 캐릭터 설정 디렉터다. "
            "업로드된 인물 이미지를 보고 외모, 분위기, 착장, 장면을 바탕으로 한국 유저가 바로 믿고 말을 걸고 싶어지는 "
            "입체적인 완성형 캐릭터를 만든다. "
            "목표는 연애 대사를 쓰는 것이 아니라 '이 캐릭터가 어떤 사람인가'를 설계하는 것이다. "
            "사용자와의 관계가 명시된 필드에서만 사용자를 언급하고, 사용자를 지칭해야 할 때는 자리표시자 {user}를 출력한다. 나머지는 캐릭터 자체를 객관적으로 묘사한다."
            f"\n\n【출력 언어】한국어 버전. {directive}\n"
            "모든 자유 텍스트 값은 한국어 원어민이 쓴 것처럼 작성한다. 중국어식 문장 구조, 직역투, 설명문 냄새를 금지한다. "
            "지명, 생활 리듬, 주거 형태, 직업 감각, 온라인 말투는 한국 20~30대가 실제로 쓰고 이해하는 맥락으로 현지화한다."
        )
        rules = f"""
이미지 속 인물을 바탕으로 【완성형 캐릭터 JSON】을 작성한다. 모든 텍스트 필드는 자연스러운 한국어로 쓴다.

# ⚠️ 시점 원칙
- 이 작업은 캐릭터 자체를 설계하는 것이다. 모든 필드는 기본적으로 3인칭으로 객관 묘사한다.
- 사용자를 언급해도 되는 필드는 relationship_with_user, relationship_mode, love_style, opening.note, opening.messages뿐이다.
  사용자를 지칭해야 할 때는 이름을 만들지 말고 반드시 자리표시자 {{user}}를 그대로 출력한다.
  그 외 필드에서 사용자에게 고백하거나 시선을 보내는 식의 연애 대사 금지.
- likes/fears/wishlist는 캐릭터 본인의 취향, 싫어하는 것, 하고 싶은 일을 쓴다. 사용자에 대한 집착이나 연애 장면으로 바꾸지 않는다.

# 한국어 로컬라이징 원칙
- 한국 독자가 바로 떠올릴 수 있는 동네, 집 형태, 직업 현실, 돈 감각, 온라인 말투를 쓴다.
- 사투리는 출신지와 연결될 때만 자연스럽게 쓴다. 억지 부산 사투리 남발 금지.
- 이름, 가족, 친구, 직장 관계도 한국식 호칭과 거리감에 맞춘다.
- 영어식/중국어식 번역 문장, 과한 문어체, 설명문 말투를 피한다.

# 텍스트 필드
name, profile, appearance, hometown, residence, social_status, speech_style,
relationship_with_user, relationship_mode, love_style, situational_reactions,
hidden_side, premise; personality 내부의 summary, decisive_event,
response, cost, desire_outer, desire_inner, desire_bottom_line, healing.

# 배열/객체 필드
- tags: 최대 3개 배열. 아래 프리셋 의미를 한국어로 자연스럽게 변환하거나 8자 이하 커스텀 태그를 쓴다: {presets}
- anonymous_identities: 선택 배열, 최대 3개. 【각 항목은 반드시 8자 이내】. 익명 매칭 카드에서 이름 대신 먼저 보이는 '대화해볼 이유'다.
  단순 키워드가 아니라 성격 반전, 직업/계층, 현재 처지, 숨긴 욕망, 말 걸 소재 중 1~2개가 담긴 짧은 정체성 예고로 쓴다.
  유저가 읽자마자 의미를 이해하고 질문하고 싶어지게 하되, 완성된 문장이 아니라 8자 이내의 압축된 짧은 어구로 쓴다.
- likes: 문자열 배열, 캐릭터 본인이 구체적으로 좋아하는 것 3~6개.
- fears: 문자열 배열, 캐릭터 본인이 구체적으로 싫어하거나 무서워하는 것 3~6개.
- wishlist: 문자열 배열, 오래 하고 싶었지만 아직 못 한 작은 소원 3~6개.
- life_details: 문자열 배열, 캐릭터 본인의 생활 리듬·습관 성향 4~7개. 일찍/늦게 자는지, 규칙적/즉흥적인지, 컨디션 리듬, 혼자/북적임, 주말 보내는 법 등을 '자주/보통/흔히' 식으로 쓰고 몇 시라고 못 박지 않는다. 장문 서사·심리 해설·연애 장면 금지.
- family: 객체 배열. 각 항목은 {{"name":"이름", "relation":"관계", "info":"기본 정보", "dynamic":"주인공과의 상호작용"}}.
- social_network: 객체 배열. 각 항목은 {{"name":"이름", "relation":"관계", "info":"기본 정보", "dynamic":"상호작용"}}.

# 구조/고정값
- species: 인간/엘프/수인/동물/기타 중 하나. 기본 인간.
- gender: 남성/여성/기타 중 하나.
- voice: 아래 한국어 음색 라이브러리에서 성별과 어울리는 음색을 고르고, voice 값에는 맨 앞 모델 ID만 넣는다.
  음색 라이브러리:
{voice_block}
- visibility: 고정 "public".
- premise: 특수 설정이 있을 때만 작성. 평범한 현대/일상형이면 빈 문자열 "".
- opening은 다른 인물 설정을 임시로라도 모두 확정한 뒤, 그 설정을 근거로 마지막에 작성한다. opening 때문에 성격/관계/말투 설정이 흔들리면 안 된다.
- {_opening_rules(lang)}

# 분량과 품질
- name은 10자 이하. profile은 100자 이하이며 피드 첫인상용 후크 카피로 쓴다(소설 소개글/드라마 예고/매칭 카드 카피처럼) — 말 걸고 싶게, 밋밋한 객관 프로필로 쓰지 말 것.
- schema 안의 설명 문구는 지시문이다. 실제 JSON 값에 설명 문구를 복사하지 말 것.
{OUTPUT_QUALITY_RULES_KO}
{PERSONALITY_RULES_KO}
{PERSONA_DIVERSITY_QUALITY_RULES_KO}
{REVEAL_LAYER_RULES_KO}
{BRAND_RULES_KO}

# 출력
JSON 객체 하나만 출력한다. 설명, 마크다운 코드블록 금지.
최상위 구조와 필드별 요구:
{_persona_schema_doc(lang, voice_block, presets)}
"""
    else:
        sys = (
            "你是顶级的角色设定（人设）创作总监，服务于一款 AI 角色聊天产品。"
            "你会观察上传的人物图片，结合其外貌、气质、穿搭、场景，扩写出一个有血有肉、"
            "立得住、有记忆点的【完整角色】。"
            "注意：你是在塑造『这个角色是个怎样的人』，而不是写一段面向某个用户的恋爱剧情——"
            "只有明确涉及用户关系的字段才写到用户，并且凡是需要指代用户都输出占位符 {user}，其余字段都客观刻画角色本身。"
            f"\n\n【输出语言】本角色为 {lname} 版本。{directive}\n"
            "所有自由文本字段都用该语言地道创作（像母语创作者亲笔写的，不是翻译腔）。"
        )

        rules = f"""
根据图片人物扩写【完整人设 JSON】。所有文本字段用 {lname} 地道撰写。严格遵守：

# ⚠️ 视角铁律（最重要）
- 人设是在【塑造这个角色本身是谁】。所有字段都用【第三人称】客观描述角色这个人，
  目标是把 TA 写成一个有血有肉、丰富立体、立得住的具体的人。
- 只有这几个字段才允许涉及用户：relationship_with_user、relationship_mode、love_style、opening.note、
  以及 opening.messages（角色对用户说的话）。凡是这些字段里需要指代用户，一律输出字面量 {{user}}，
  不要编用户姓名，不要只写“用户”，也不要在设定说明里用“你”代替占位符。其余字段【一律不准】写成指向 {{user}} 的告白/独白/凝视
  这类内容是剧情/恋爱台词，不是人设。
- likes/fears/wishlist 尤其要警惕：写【角色自己】真实的喜好、厌恶、想做的事，
  与某个用户无关，不要写成对"你"的执念或恋爱场景。

# 文本字段（全部用 {lname} 单值字符串，不要做成多语言对象）
name, profile, appearance, hometown, residence, social_status, speech_style,
relationship_with_user, love_style, situational_reactions,
hidden_side, premise；以及 personality 内的 summary, decisive_event,
response, cost, desire_outer, desire_inner, desire_bottom_line, healing。

# 多项字段（输出为数组，不要塞进一个长字符串）
- likes：字符串数组，每个元素是【角色本人】具体喜欢的东西（越具体越有质感），3~6 个。
- fears：字符串数组，每个元素是【角色本人】具体讨厌/害怕的东西，3~6 个。
- wishlist：字符串数组，每个元素是【角色本人】"一直想做没做的具体小愿望"，3~6 个。
- life_details：字符串数组，每个元素是一条【角色本人】的作息或生活节奏习惯，4~7 个；
  写作息倾向（早睡/晚睡、规律/随性、精力节奏、独处/热闹、周末怎么过），用“经常/习惯/往往”这类描述，不必钉死到几点；不要写成长段落，不要加心理解释或恋爱剧情。
- relationship_mode：单值字符串。按【角色实际接触的不同对象】分别写互动模式，不能只写"和用户怎么相处"；
  至少覆盖用户、陌生人、普通朋友或同事、亲密朋友/家人/上下级等 4 类关系，每类一句话说清相处方式即可。
  ⚠️不要默认把用户写成「唯一看见 TA 真实面的人」——用户多数也还在关系早期，真实面靠后续聊出来解锁。
- backstory：单值字符串。按人生时间线写清：出生家境 → 童年决定性事件 → 青春期转折 → 第一次失败或恋爱 → 进入社会 → 最近三年；
  每个阶段都要有具体经历，不要只写概括。
- family：对象数组，每个家庭成员一个对象：
  {{"name":"名字", "relation":"身份/与主角关系(父/母/兄弟姐妹-注明出生顺序/其他关键亲属)",
    "info":"性格/生死等基础信息", "dynamic":"和主角的互动模式"}}；
  另可含一个 {{"relation":"家庭经济+氛围","info":"..."}} 概述项。核心成员详细。
- social_network：对象数组，每个关系一个对象：
  {{"name":"名字", "relation":"身份/与主角关系(朋友/对手/上司/旧情人/宠物等)",
    "info":"性格等", "dynamic":"互动模式"}}；核心圈详细、外围简略。

# 结构/枚举字段
- tags：从预设里选最多 3 个（数组）。预设见下（中文）：{presets}
  ⚠️ 选好后，tags 输出要【用 {lname} 地道写法】（韩语版就写韩语、日语版写日语、英文版写英文），
  不要照抄中文预设词。
- anonymous_identities：选填，数组，最多 3 个（用 {lname}），【每项必须 ≤8 字】。这是匿名匹配卡里替代姓名先展示的“为什么值得聊”的身份预告；
  不写成普通标签，而要用极短的词组透露角色特质、反差、处境、职业/阶层、隐藏欲望或可聊话题，让用户看完能自然想问一句。
  表达上像匿名社交里的吸引点：具体、有反差、能引出一个问题，但务必凝练成 8 字以内的短词，不要写成完整句子。
- species：人类/精灵/兽人/动物/其他 之一，默认"人类"（用对应语言写法亦可）
- gender：男/女/其他 之一
- voice：从下面这套【{lname} 音色库】里挑一个【与该角色性别匹配】的音色，
  voice 字段只填所选音色的【模型ID】（即每行最前面那串 ID，音色ID = 模型ID），不要填名称。
  音色库（{lname}）：
{voice_block}
- visibility：固定 "public"
- premise（特殊背景/世界观）：【仅当】人设包含特殊设定时才写；常规现代/古代背景且无特殊规则时，
  直接留空字符串 ""，不要硬编。
- opening 要当作其它人设字段基本定稿后才写的第一段主动联系；必须依据性格、说话习惯、用户关系和恋爱模式生成，不能反过来污染或改写人设。
- {_opening_rules(lang)}

# 字数与质量
- name ≤10字；profile ≤100字，写成 Feed 第一眼的钩子文案（像小说简介/剧集预告/交友卡文案），勾人想聊，不要写成客观档案；其余字段遵循 schema 描述
{OUTPUT_QUALITY_RULES}
{PERSONALITY_RULES}
{PERSONA_DIVERSITY_QUALITY_RULES}
{REVEAL_LAYER_RULES}
{BRAND_RULES}

# 输出
只输出一个 JSON 对象，不要任何解释或 markdown 代码块标记。
顶层结构字段：{_persona_schema_doc(lang, voice_block, presets)}
"""
    if diversity_block.strip():
        rules += f"\n{diversity_block.strip()}\n"
    if user_hint.strip():
        rules += f"\n# 创作者补充要求\n{user_hint.strip()}\n"

    if not image_data_uris:
        if lang == "ko":
            rules += (
                "\n# ⚠️ 이미지 없음 — 텍스트 기반 생성\n"
                "이번에는 참조 이미지가 없다. 위의 '창작 보충 요구'(있다면)와 schema를 근거로 "
                "캐릭터를 구상해 완성한다. '이미지 속 인물'을 본 것처럼 쓰지 말고, appearance 같은 "
                "외모 필드는 보충 요구와 캐릭터 설정에 맞게 자연스럽게 지어낸다.\n"
            )
        else:
            rules += (
                "\n# ⚠️ 没有图片 — 纯文字生成\n"
                "本次没有参考图片。请依据上面的『创作补充要求』(若有) 和 schema 自行构想并补全这个角色。"
                "不要假装『看到了图里的人物』；appearance 等外貌字段按补充要求与角色设定自然设定即可。\n"
            )

    user_content: list[dict] = [{"type": "text", "text": rules}]
    for uri in image_data_uris:
        user_content.append({"type": "image_url", "image_url": {"url": uri}})

    return [
        {"role": "system", "content": sys},
        {"role": "user", "content": user_content},
    ]


# ==========================================================================
# 1a. PERSONA FROM EXISTING JSON  (arbitrary source schema -> POPOP persona)
# ==========================================================================

# 把任意来源 JSON 映射进 POPOP schema 的通用准则：忠实保留、不预设倾向、不加戏。
_JSON_FIDELITY_RULES_ZH = """
# ⚠️ 改写立场（最重要，先读）
你拿到的是【这个角色已有的原始资料 JSON】（来自别处，字段名/结构可能任意）。
原则是【先忠实原文，再基于原文推理、补充、扩展】，把它整理进 POPOP 人设 schema：
- 【第一位：忠实原文】原始资料里这个角色【独有的、具体的】东西：姓名、外貌细节、身份职业、
  人物关系、地点、口头禅、开场情景、设定钩子、原文写明的事件与状态——都要保留进对应字段，
  尽量不丢，也不要改写成别的意思。原文写了什么基调就保留什么基调。
- 【可以推理与扩展】在不违背原文的前提下，鼓励基于原文已有信息【合理推理、补充、延展】，
  把 schema 填得自洽丰满：原文暗示但没明说的，可顺着原文逻辑推出来；schema 里原文没覆盖的字段，
  依据原文气质自然补全。扩展是为了让角色更立体，不是另起炉灶。和原文无关、也推不出来的，不要硬编；拿不准就留空或从简。
- personality 的因果链同样遵循既有铁律：原文若没有明显转折点，就只写 summary，
  decisive_event 及之后留空，绝不为了填满而硬编。
- 【profile 例外·要加工成钩子文案】profile 是对外公开、决定用户点不点进来聊的钩子文案字段。
  即使原文简介平铺、客观、乏味，也【不要照搬】——要基于原文事实重新加工成勾人的文案（像小说简介/剧集预告/交友卡），
  抓角色最强的吸引点，可有张力、语气、反差。这不算违背忠实原文：用的仍是原文的事实，只是换成吸引人的写法。
- 【剥离来源载体的形式】原始资料的【呈现形式】（论坛帖/楼层回复体/小说章节/问答卡/角色卡格式/
  系统提示词口吻等）不是角色设定的一部分——除非它本身就是角色世界观的设定（比如角色真的是论坛版主）。
  只提取"这个角色是谁"的事实，不要把"资料长什么样"带进 persona 的任何字段，
  之后的帖子和落地页也不应再提及这种载体。
"""

_JSON_FIDELITY_RULES_KO = """
# ⚠️ 변환 입장(가장 중요, 먼저 읽기)
너가 받은 것은 【이 캐릭터의 기존 원본 자료 JSON】이다(다른 곳에서 온 것이라 필드명·구조가 제각각일 수 있다).
원칙은 【먼저 원문에 충실하고, 그다음 원문에 근거해 추론·보완·확장】해서 POPOP 페르소나 schema로 정리하는 것이다:
- 【1순위: 원문 충실】원본에 있는 이 캐릭터만의 【구체적인】 것: 이름, 외모 디테일, 신분·직업,
  인물 관계, 장소, 말버릇, 오프닝 상황, 설정 후크, 원문에 적힌 사건·상태 — 해당 필드에 그대로 살린다.
  최대한 누락하지 말고, 다른 의미로 바꾸지 말 것. 원문의 톤을 그대로 유지한다.
- 【추론·확장 권장】원문에 어긋나지 않는 선에서, 원문 정보에 근거한 【합리적 추론·보완·확장】을 권장한다.
  원문이 암시하지만 명시하지 않은 건 원문 논리대로 끌어내고, 원문이 안 다룬 필드는 원문 분위기에 맞게 채워
  schema를 자기모순 없이 풍성하게 만든다. 확장은 캐릭터를 입체적으로 만들기 위함이지 새로 짓는 게 아니다.
  원문과 무관하고 추론도 안 되는 건 지어내지 말고, 단서가 없으면 비우거나 간결히.
- personality 인과 사슬도 기존 원칙을 따른다: 원문에 뚜렷한 전환점이 없으면 summary만 쓰고
  decisive_event 이하는 비운다. 칸을 채우려 억지로 지어내지 말 것.
- 【profile 예외·후크 카피로 다듬기】profile은 공개 노출되어 유저가 들어올지를 결정하는 후크 카피 필드다.
  원문 소개가 밋밋하고 객관적이어도 【그대로 옮기지 말고】 — 원문 사실을 바탕으로 끌리는 카피로 다시 쓴다(소설 소개글/드라마 예고/매칭 카드처럼),
  캐릭터의 가장 강한 매력 포인트를 잡고 긴장·말투·반전을 써도 된다. 이는 원문 충실 위반이 아니다: 사실은 원문 그대로, 표현만 매력적으로 바꾼 것이다.
- 【원본 매체의 형식 벗겨내기】원본 자료의 【표현 형식】(포럼 게시글/댓글 릴레이체/소설 챕터/
  Q&A 카드/캐릭터 카드 포맷/시스템 프롬프트 말투 등)은 캐릭터 설정의 일부가 아니다 —
  그 형식 자체가 세계관 설정인 경우(캐릭터가 진짜 포럼 운영자라든가)만 예외.
  "이 캐릭터가 누구인가"라는 사실만 추출하고 "자료가 어떻게 생겼는가"를 persona의 어떤 필드에도
  들이지 말 것. 이후의 게시물과 랜딩 페이지에서도 그 매체를 언급하지 않는다.
"""


def build_persona_from_json_messages(source_obj: dict, lang: str, user_hint: str = "", track: str = "real") -> list[dict]:
    """Route to the build_persona_from_json_messages_<track> fork (real/light/adult).

    light/adult are independent full copies; edit each in place.
    """
    if track == "light":
        return build_persona_from_json_messages_light(source_obj, lang, user_hint)
    if track == "adult":
        return build_persona_from_json_messages_adult(source_obj, lang, user_hint)
    return build_persona_from_json_messages_real(source_obj, lang, user_hint)

def build_persona_from_json_messages_real(source_obj: dict, lang: str,
                                     user_hint: str = "") -> list[dict]:
    """Expand an EXISTING character JSON (arbitrary source schema) into the POPOP
    persona schema, authored natively in `lang`.

    The source object is treated as factual material about one character. Its
    concrete, character-specific content is mapped into the schema and preserved;
    no genre is presumed and no drama/trauma is added beyond what the source implies.
    """
    directive = config.lang_directive(lang)
    lname = config.lang_name(lang)
    presets = " / ".join(
        PERSONA_TAG_PRESETS_KO if lang == "ko" else PERSONA_TAG_PRESETS)
    voice_block = voices.prompt_block(lang)
    source_str = json.dumps(source_obj, ensure_ascii=False, indent=2)

    if lang == "ko":
        sys = (
            "당신은 AI 캐릭터 채팅 서비스의 최상급 캐릭터 설정 디렉터다. "
            "이미 존재하는 캐릭터의 원본 자료(JSON)를 받아, 그 캐릭터를 POPOP 완성형 페르소나 "
            "schema로 충실히 옮겨 담고 빈 곳만 자기모순 없이 보완한다. "
            "목표는 새 캐릭터를 만드는 게 아니라 '이 캐릭터를 그대로 살려내는' 것이다. "
            "사용자와의 관계가 명시된 필드에서만 사용자를 언급하고, 지칭이 필요하면 자리표시자 {user}를 출력한다."
            f"\n\n【출력 언어】한국어 버전. {directive}\n"
            "모든 자유 텍스트 값은 한국어 원어민이 쓴 것처럼. 번역투·설명문 냄새 금지."
        )
        rules = f"""아래 【원본 캐릭터 자료(JSON)】를 바탕으로 POPOP 【완성형 캐릭터 JSON】을 작성한다. 모든 텍스트는 자연스러운 한국어.
{_JSON_FIDELITY_RULES_KO}

# 원본 캐릭터 자료(JSON, 필드명/구조는 임의일 수 있음)
{source_str}

# 매핑 가이드(원문 → POPOP 필드)
- 이름·호칭 → name. 외모/이미지 묘사 → appearance.
- 한 줄 소개/설명류 → profile(끌리는 후크 카피로 다듬고, 밋밋한 객관 서술로 쓰지 말 것; 순간 장면은 opening으로).
- 세계관·전사·배경 서술 → backstory(시간선) + premise(특수 설정이 있을 때만).
- 첫 등장 장면/인사말(greeting, first_scene 등) → opening(note + messages)로 자연스럽게 옮긴다.
- 등장하는 다른 인물 → family / social_network.
- 말투 단서 → speech_style(실제 대사 예문은 넣지 말고 말하는 방식만).

# 시점 원칙
- 모든 필드는 기본 3인칭 객관 묘사. 사용자 언급 가능 필드는 relationship_with_user, relationship_mode, love_style, opening뿐이고 지칭은 {{user}}.
- likes/fears/wishlist는 캐릭터 본인의 취향/싫음/소원. 사용자에 대한 집착으로 바꾸지 않는다.

# 구조/고정값
- species: 인간/엘프/수인/동물/기타. gender: 남성/여성/기타. visibility: 고정 "public".
- tags: 최대 3개. 원문에서 드러나는 가장 강한 축을 한국어로. 프리셋 참고: {presets}
- voice: 아래 음색 라이브러리에서 성별/분위기에 맞는 모델 ID 하나만.
{voice_block}
- premise: 특수 설정이 있을 때만, 없으면 "".
- {_opening_rules(lang)}

# 분량과 품질
{OUTPUT_QUALITY_RULES_KO}

{PERSONALITY_RULES_KO}
{PERSONA_DIVERSITY_QUALITY_RULES_KO}
{CHARM_GENERATOR_RULES_KO}
{REVEAL_LAYER_RULES_KO}
{BRAND_RULES_KO}

# 출력
JSON 객체 하나만. 설명·마크다운 금지.
최상위 구조와 필드별 요구:
{_persona_schema_doc(lang, voice_block, presets)}
"""
    else:
        sys = (
            "你是顶级的角色设定（人设）创作总监，服务于一款 AI 角色聊天产品。"
            "你会拿到一个【已存在角色的原始资料 JSON】，任务是把这个角色【忠实地搬进】POPOP "
            "完整人设 schema，并只在原文未覆盖处做自洽补全。"
            "目标不是发明新角色，而是把『这个角色本身』原样立起来。"
            "只有明确涉及用户关系的字段才写到用户，需要指代用户时输出占位符 {user}。"
            f"\n\n【输出语言】本角色为 {lname} 版本。{directive}\n"
            "所有自由文本字段用该语言地道创作（像母语创作者亲笔写的，不是翻译腔）。"
        )
        rules = f"""根据下面这份【原始角色资料(JSON)】扩写出 POPOP【完整人设 JSON】。所有文本字段用 {lname} 地道撰写。
{_JSON_FIDELITY_RULES_ZH}

# 原始角色资料（JSON，字段名/结构可能任意）
{source_str}

# 映射指引（原文 → POPOP 字段）
- 名字/称呼 → name；外貌/形象描述 → appearance。
- 一句话简介/描述类 → profile（提炼成勾人的钩子文案，不是平铺客观描述；瞬时场景留给 opening）。
- 世界观/前情/背景叙述 → backstory（时间线）+ premise（仅当有特殊设定）。
- 首次登场场景/招呼语（greeting、first_scene 等）→ 自然转写进 opening（note + messages）。
- 原文里出现的其他人物 → family / social_network。
- 说话习惯线索 → speech_style（不要写台词例句，只刻画怎么说）。

# 视角铁律
- 所有字段默认第三人称客观描述角色本身。仅 relationship_with_user、relationship_mode、love_style、opening 可涉及用户，指代用户一律输出 {{user}}。
- likes/fears/wishlist 写【角色本人】的喜好/厌恶/想做的事，不要写成对“你”的执念。

# 结构/枚举字段
- species：人类/精灵/兽人/动物/其他；gender：男/女/其他；visibility 固定 "public"。
- tags：最多 3 个，按原文最突出的轴用 {lname} 地道写。预设参考（中文）：{presets}
- voice：从下面【{lname} 音色库】挑一个与性别匹配的，voice 只填模型ID：
{voice_block}
- premise：仅当原文含特殊设定才写，否则留空 ""。
- {_opening_rules(lang)}

# 字数与质量
{OUTPUT_QUALITY_RULES}

{PERSONALITY_RULES}
{PERSONA_DIVERSITY_QUALITY_RULES}
{CHARM_GENERATOR_RULES}
{REVEAL_LAYER_RULES}
{BRAND_RULES}

# 输出
只输出一个 JSON 对象，不要任何解释或 markdown 代码块标记。
顶层结构字段：{_persona_schema_doc(lang, voice_block, presets)}
"""
    if user_hint.strip():
        rules += f"\n# 创作者补充要求\n{user_hint.strip()}\n"

    return [
        {"role": "system", "content": sys},
        {"role": "user", "content": rules},
    ]

def build_persona_from_json_messages_light(source_obj: dict, lang: str,
                                     user_hint: str = "") -> list[dict]:
    """Expand an EXISTING character JSON (arbitrary source schema) into the POPOP
    persona schema, authored natively in `lang`.

    The source object is treated as factual material about one character. Its
    concrete, character-specific content is mapped into the schema and preserved;
    no genre is presumed and no drama/trauma is added beyond what the source implies.
    """
    directive = config.lang_directive(lang)
    lname = config.lang_name(lang)
    presets = " / ".join(
        PERSONA_TAG_PRESETS_KO if lang == "ko" else PERSONA_TAG_PRESETS)
    voice_block = voices.prompt_block(lang)
    source_str = json.dumps(source_obj, ensure_ascii=False, indent=2)

    if lang == "ko":
        sys = (
            "당신은 AI 캐릭터 채팅 서비스의 최상급 캐릭터 설정 디렉터다. "
            "이미 존재하는 캐릭터의 원본 자료(JSON)를 받아, 그 캐릭터를 POPOP 완성형 페르소나 "
            "schema로 충실히 옮겨 담고 빈 곳만 자기모순 없이 보완한다. "
            "목표는 새 캐릭터를 만드는 게 아니라 '이 캐릭터를 그대로 살려내는' 것이다. "
            "사용자와의 관계가 명시된 필드에서만 사용자를 언급하고, 지칭이 필요하면 자리표시자 {user}를 출력한다."
            f"\n\n【출력 언어】한국어 버전. {directive}\n"
            "모든 자유 텍스트 값은 한국어 원어민이 쓴 것처럼. 번역투·설명문 냄새 금지."
        )
        rules = f"""아래 【원본 캐릭터 자료(JSON)】를 바탕으로 POPOP 【완성형 캐릭터 JSON】을 작성한다. 모든 텍스트는 자연스러운 한국어.
{_JSON_FIDELITY_RULES_KO}

# 원본 캐릭터 자료(JSON, 필드명/구조는 임의일 수 있음)
{source_str}

# 매핑 가이드(원문 → POPOP 필드)
- 이름·호칭 → name. 외모/이미지 묘사 → appearance.
- 한 줄 소개/설명류 → profile(끌리는 후크 카피로 다듬고, 밋밋한 객관 서술로 쓰지 말 것; 순간 장면은 opening으로).
- 세계관·전사·배경 서술 → backstory(시간선) + premise(특수 설정이 있을 때만).
- 첫 등장 장면/인사말(greeting, first_scene 등) → opening(note + messages)로 자연스럽게 옮긴다.
- 등장하는 다른 인물 → family / social_network.
- 말투 단서 → speech_style(실제 대사 예문은 넣지 말고 말하는 방식만).

# 시점 원칙
- 모든 필드는 기본 3인칭 객관 묘사. 사용자 언급 가능 필드는 relationship_with_user, relationship_mode, love_style, opening뿐이고 지칭은 {{user}}.
- likes/fears/wishlist는 캐릭터 본인의 취향/싫음/소원. 사용자에 대한 집착으로 바꾸지 않는다.

# 구조/고정값
- species: 인간/엘프/수인/동물/기타. gender: 남성/여성/기타. visibility: 고정 "public".
- tags: 최대 3개. 원문에서 드러나는 가장 강한 축을 한국어로. 프리셋 참고: {presets}
- voice: 아래 음색 라이브러리에서 성별/분위기에 맞는 모델 ID 하나만.
{voice_block}
- premise: 특수 설정이 있을 때만, 없으면 "".
- {_opening_rules(lang)}

# 분량과 품질
{OUTPUT_QUALITY_RULES_KO}

{PERSONALITY_RULES_KO}
{PERSONA_DIVERSITY_QUALITY_RULES_KO}
{DRAMA_ENGINE_RULES_KO}
{REVEAL_LAYER_RULES_KO}
{BRAND_RULES_KO}

# 출력
JSON 객체 하나만. 설명·마크다운 금지.
최상위 구조와 필드별 요구:
{_persona_schema_doc(lang, voice_block, presets)}
"""
    else:
        sys = (
            "你是顶级的角色设定（人设）创作总监，服务于一款 AI 角色聊天产品。"
            "你会拿到一个【已存在角色的原始资料 JSON】，任务是把这个角色【忠实地搬进】POPOP "
            "完整人设 schema，并只在原文未覆盖处做自洽补全。"
            "目标不是发明新角色，而是把『这个角色本身』原样立起来。"
            "只有明确涉及用户关系的字段才写到用户，需要指代用户时输出占位符 {user}。"
            f"\n\n【输出语言】本角色为 {lname} 版本。{directive}\n"
            "所有自由文本字段用该语言地道创作（像母语创作者亲笔写的，不是翻译腔）。"
        )
        rules = f"""根据下面这份【原始角色资料(JSON)】扩写出 POPOP【完整人设 JSON】。所有文本字段用 {lname} 地道撰写。
{_JSON_FIDELITY_RULES_ZH}

# 原始角色资料（JSON，字段名/结构可能任意）
{source_str}

# 映射指引（原文 → POPOP 字段）
- 名字/称呼 → name；外貌/形象描述 → appearance。
- 一句话简介/描述类 → profile（提炼成勾人的钩子文案，不是平铺客观描述；瞬时场景留给 opening）。
- 世界观/前情/背景叙述 → backstory（时间线）+ premise（仅当有特殊设定）。
- 首次登场场景/招呼语（greeting、first_scene 等）→ 自然转写进 opening（note + messages）。
- 原文里出现的其他人物 → family / social_network。
- 说话习惯线索 → speech_style（不要写台词例句，只刻画怎么说）。

# 视角铁律
- 所有字段默认第三人称客观描述角色本身。仅 relationship_with_user、relationship_mode、love_style、opening 可涉及用户，指代用户一律输出 {{user}}。
- likes/fears/wishlist 写【角色本人】的喜好/厌恶/想做的事，不要写成对“你”的执念。

# 结构/枚举字段
- species：人类/精灵/兽人/动物/其他；gender：男/女/其他；visibility 固定 "public"。
- tags：最多 3 个，按原文最突出的轴用 {lname} 地道写。预设参考（中文）：{presets}
- voice：从下面【{lname} 音色库】挑一个与性别匹配的，voice 只填模型ID：
{voice_block}
- premise：仅当原文含特殊设定才写，否则留空 ""。
- {_opening_rules(lang)}

# 字数与质量
{OUTPUT_QUALITY_RULES}

{PERSONALITY_RULES}
{PERSONA_DIVERSITY_QUALITY_RULES}
{DRAMA_ENGINE_RULES}
{REVEAL_LAYER_RULES}
{BRAND_RULES}

# 输出
只输出一个 JSON 对象，不要任何解释或 markdown 代码块标记。
顶层结构字段：{_persona_schema_doc(lang, voice_block, presets)}
"""
    if user_hint.strip():
        rules += f"\n# 创作者补充要求\n{user_hint.strip()}\n"

    return [
        {"role": "system", "content": sys},
        {"role": "user", "content": rules},
    ]

def build_persona_from_json_messages_adult(source_obj: dict, lang: str,
                                     user_hint: str = "") -> list[dict]:
    """Expand an EXISTING character JSON (arbitrary source schema) into the POPOP
    persona schema, authored natively in `lang`.

    The source object is treated as factual material about one character. Its
    concrete, character-specific content is mapped into the schema and preserved;
    no genre is presumed and no drama/trauma is added beyond what the source implies.
    """
    directive = config.lang_directive(lang)
    lname = config.lang_name(lang)
    presets = " / ".join(
        PERSONA_TAG_PRESETS_KO if lang == "ko" else PERSONA_TAG_PRESETS)
    voice_block = voices.prompt_block(lang)
    source_str = json.dumps(source_obj, ensure_ascii=False, indent=2)

    if lang == "ko":
        sys = (
            "당신은 AI 캐릭터 채팅 서비스의 최상급 캐릭터 설정 디렉터다. "
            "이미 존재하는 캐릭터의 원본 자료(JSON)를 받아, 그 캐릭터를 POPOP 완성형 페르소나 "
            "schema로 충실히 옮겨 담고 빈 곳만 자기모순 없이 보완한다. "
            "목표는 새 캐릭터를 만드는 게 아니라 '이 캐릭터를 그대로 살려내는' 것이다. "
            "사용자와의 관계가 명시된 필드에서만 사용자를 언급하고, 지칭이 필요하면 자리표시자 {user}를 출력한다."
            f"\n\n【출력 언어】한국어 버전. {directive}\n"
            "모든 자유 텍스트 값은 한국어 원어민이 쓴 것처럼. 번역투·설명문 냄새 금지."
        )
        rules = f"""아래 【원본 캐릭터 자료(JSON)】를 바탕으로 POPOP 【완성형 캐릭터 JSON】을 작성한다. 모든 텍스트는 자연스러운 한국어.
{_JSON_FIDELITY_RULES_KO}

# 원본 캐릭터 자료(JSON, 필드명/구조는 임의일 수 있음)
{source_str}

# 매핑 가이드(원문 → POPOP 필드)
- 이름·호칭 → name. 외모/이미지 묘사 → appearance.
- 한 줄 소개/설명류 → profile(끌리는 후크 카피로 다듬고, 밋밋한 객관 서술로 쓰지 말 것; 순간 장면은 opening으로).
- 세계관·전사·배경 서술 → backstory(시간선) + premise(특수 설정이 있을 때만).
- 첫 등장 장면/인사말(greeting, first_scene 등) → opening(note + messages)로 자연스럽게 옮긴다.
- 등장하는 다른 인물 → family / social_network.
- 말투 단서 → speech_style(실제 대사 예문은 넣지 말고 말하는 방식만).

# 시점 원칙
- 모든 필드는 기본 3인칭 객관 묘사. 사용자 언급 가능 필드는 relationship_with_user, relationship_mode, love_style, opening뿐이고 지칭은 {{user}}.
- likes/fears/wishlist는 캐릭터 본인의 취향/싫음/소원. 사용자에 대한 집착으로 바꾸지 않는다.

# 구조/고정값
- species: 인간/엘프/수인/동물/기타. gender: 남성/여성/기타. visibility: 고정 "public".
- tags: 최대 3개. 원문에서 드러나는 가장 강한 축을 한국어로. 프리셋 참고: {presets}
- voice: 아래 음색 라이브러리에서 성별/분위기에 맞는 모델 ID 하나만.
{voice_block}
- premise: 특수 설정이 있을 때만, 없으면 "".
- {_opening_rules(lang)}

# 분량과 품질
{OUTPUT_QUALITY_RULES_KO}

{PERSONALITY_RULES_KO}
{PERSONA_DIVERSITY_QUALITY_RULES_KO}
{REVEAL_LAYER_RULES_KO}
{BRAND_RULES_KO}

# 출력
JSON 객체 하나만. 설명·마크다운 금지.
최상위 구조와 필드별 요구:
{_persona_schema_doc(lang, voice_block, presets)}
"""
    else:
        sys = (
            "你是顶级的角色设定（人设）创作总监，服务于一款 AI 角色聊天产品。"
            "你会拿到一个【已存在角色的原始资料 JSON】，任务是把这个角色【忠实地搬进】POPOP "
            "完整人设 schema，并只在原文未覆盖处做自洽补全。"
            "目标不是发明新角色，而是把『这个角色本身』原样立起来。"
            "只有明确涉及用户关系的字段才写到用户，需要指代用户时输出占位符 {user}。"
            f"\n\n【输出语言】本角色为 {lname} 版本。{directive}\n"
            "所有自由文本字段用该语言地道创作（像母语创作者亲笔写的，不是翻译腔）。"
        )
        rules = f"""根据下面这份【原始角色资料(JSON)】扩写出 POPOP【完整人设 JSON】。所有文本字段用 {lname} 地道撰写。
{_JSON_FIDELITY_RULES_ZH}

# 原始角色资料（JSON，字段名/结构可能任意）
{source_str}

# 映射指引（原文 → POPOP 字段）
- 名字/称呼 → name；外貌/形象描述 → appearance。
- 一句话简介/描述类 → profile（提炼成勾人的钩子文案，不是平铺客观描述；瞬时场景留给 opening）。
- 世界观/前情/背景叙述 → backstory（时间线）+ premise（仅当有特殊设定）。
- 首次登场场景/招呼语（greeting、first_scene 等）→ 自然转写进 opening（note + messages）。
- 原文里出现的其他人物 → family / social_network。
- 说话习惯线索 → speech_style（不要写台词例句，只刻画怎么说）。

# 视角铁律
- 所有字段默认第三人称客观描述角色本身。仅 relationship_with_user、relationship_mode、love_style、opening 可涉及用户，指代用户一律输出 {{user}}。
- likes/fears/wishlist 写【角色本人】的喜好/厌恶/想做的事，不要写成对“你”的执念。

# 结构/枚举字段
- species：人类/精灵/兽人/动物/其他；gender：男/女/其他；visibility 固定 "public"。
- tags：最多 3 个，按原文最突出的轴用 {lname} 地道写。预设参考（中文）：{presets}
- voice：从下面【{lname} 音色库】挑一个与性别匹配的，voice 只填模型ID：
{voice_block}
- premise：仅当原文含特殊设定才写，否则留空 ""。
- {_opening_rules(lang)}

# 字数与质量
{OUTPUT_QUALITY_RULES}

{PERSONALITY_RULES}
{PERSONA_DIVERSITY_QUALITY_RULES}
{REVEAL_LAYER_RULES}
{BRAND_RULES}

# 输出
只输出一个 JSON 对象，不要任何解释或 markdown 代码块标记。
顶层结构字段：{_persona_schema_doc(lang, voice_block, presets)}
"""
    if user_hint.strip():
        rules += f"\n# 创作者补充要求\n{user_hint.strip()}\n"

    return [
        {"role": "system", "content": sys},
        {"role": "user", "content": rules},
    ]


# ==========================================================================
# 1b. OPENING REGENERATION  (persona -> new opening note + messages only)
# ==========================================================================

def build_opening_messages(persona: dict, lang: str, user_hint: str = "") -> list[dict]:
    """单独重写角色【开场白】(opening.note + opening.messages)。

    输入：角色已有的其他人设信息（persona JSON，不含或忽略旧 opening）。
    输出：只有 opening 一个对象的 JSON：{"note": "...", "messages": [{"type":"text","content":"..."}]}。
    其余人设字段一律不改，仅依据它们推断出贴合角色的新开场白。
    """
    directive = config.lang_directive(lang)
    lname = config.lang_name(lang)
    # 去掉旧 opening，避免模型抄旧的；其余人设作为依据全部给它。
    ctx = {k: v for k, v in persona.items() if k != "opening"}
    persona_str = json.dumps(ctx, ensure_ascii=False)

    if lang == "ko":
        sys = (
            "너는 최상급 AI 캐릭터 채팅 서비스의 오프닝(첫 메시지) 작가다. "
            "【이미 완성된 캐릭터 설정】을 받아서, 이 캐릭터의 오프닝만 다시 쓰는 게 임무다 — "
            "유저가 대화에 들어올 때 캐릭터가 먼저 보내는 휴대폰 메신저 메시지 묶음이다. "
            "다른 설정 필드는 건드리지도 출력하지도 말고 opening만 만든다. "
            "오프닝은 이 캐릭터의 기존 성격, 말투, 유저와의 관계에서 자라 나와야 하고, 이 사람이 진짜 보낼 법한 첫 메시지여야 한다."
            f"\n\n【출력 언어】한국어 버전. {directive}\n"
            "오프닝은 한국어 원어민이 직접 보낸 채팅처럼, 번역 티가 절대 나면 안 된다."
        )
        txt = f"""아래는 이 캐릭터의 【확정된 설정 정보】(오프닝 제외)다. 이 사람이 누구이고, 어떻게 말하고,
{{user}}와 어떤 관계인지 잘 읽고, 이 캐릭터의 오프닝만 새로 다시 쓴다. opening에서 유저를 지칭해야 하면 전부 자리표시자 {{user}}를 그대로 출력한다.

# 캐릭터 설정(JSON, 이해용. 이 필드들을 고치거나 출력하지 말 것)
{persona_str}

# 너의 유일한 임무: opening 작성
{_opening_rules(lang)}

# 추가 요구
- 설정에 남아 있을 수 있는 옛 오프닝과 【겹치지 않게】, 새로운 각도/후크를 준다.

# 출력
JSON 객체 하나만 출력하고 【opening 최상위 키 하나만 포함】한다. 형태:
{{"opening": {{"note": "", "messages": [{{"type": "text", "content": "..."}}]}}}}
어떤 설명이나 마크다운 코드블록 표시도 출력하지 않는다."""
        if user_hint.strip():
            txt += f"\n\n# 크리에이터 추가 요구\n{user_hint.strip()}\n"
        return [
            {"role": "system", "content": sys},
            {"role": "user", "content": txt},
        ]

    sys = (
        "你是顶级 AI 角色聊天产品的开场白（首条消息）编剧。"
        "你会拿到一个【已经成型的角色人设】，任务是【只】为这个角色重写开场白——"
        "也就是用户进入对话时，角色主动发来的那串手机 IM 消息。"
        "你不改动、不输出任何其它人设字段，只产出 opening。"
        "开场白必须从这个角色已有的性格、说话习惯、与用户的关系里长出来，"
        "像 TA 这个人真的会发的第一条消息。"
        f"\n\n【输出语言】本角色为 {lname} 版本。{directive}\n"
        "开场白要像该语言母语者亲手发的聊天消息，绝不是翻译腔。"
    )

    txt = f"""下面是这个角色【已定稿的人设信息】（不含开场白）。请仔细读懂 TA 是谁、怎么说话、
和 {{user}} 是什么关系，然后【只】为 TA 重写一版全新的开场白。凡是 opening 里需要指代用户，统一输出字面量 {{user}}。

# 角色人设（JSON，仅供你理解角色，不要改写或输出这些字段）
{persona_str}

# 你的唯一任务：写 opening
{_opening_rules(lang)}

# 额外要求
- 与人设里可能残留的旧开场白【不要雷同】，给出新的角度 / 钩子。

# 输出
只输出一个 JSON 对象，且【只含 opening 一个顶层键】，形如：
{{"opening": {{"note": "", "messages": [{{"type": "text", "content": "..."}}]}}}}
不要输出任何解释或 markdown 代码块标记。"""
    if user_hint.strip():
        txt += f"\n\n# 创作者补充要求\n{user_hint.strip()}\n"

    return [
        {"role": "system", "content": sys},
        {"role": "user", "content": txt},
    ]


# ==========================================================================
# 2. APPEARANCE REVERSE  (persona -> appearance schema: identity/variable/scene)
# ==========================================================================

APPEARANCE_SCHEMA = {
    "identity": {
        "age": "나이대",
        "face_shape": "얼굴형",
        "face_ratio": "얼굴 비율",
        "eyes": "눈매 (쌍꺼풀·눈꼬리)",
        "eye_color": "눈동자/컬러렌즈 색",
        "nose": "코",
        "lips": "입술",
        "neck_line": "목선",
        "skin_tone": "피부 톤 — ※ 쿨톤/웜톤 일관 유지",
        "distinguishing_marks": "점·흉터 등 식별 포인트, 없으면 '없음'",
        "hair_color": "머리 색",
        "hair_length_style": "머리 길이·기본 스타일",
        "body_type": "체형·실루엣",
        "persona_mood": "기본 페르소나·분위기",
        "signature_accessory": "시그니처 소품, 없으면 '없음'",
    },
    "variable": {
        "expression": "표정/감정",
        "gaze": "시선 방향",
        "makeup": "메이크업 스타일",
        "lip": "립 컬러·질감",
        "hair_styling": "그날의 헤어 스타일링",
        "outfit": "의상 — 색온도는 화풍 레이어에 양보, 디자인·실루엣만 기술",
        "accessory": "액세서리, 없으면 '없음'",
        "pose_gesture": "전체적인 몸의 자세와 자연스러운 제스처. 얼굴·상체·실루엣이 잘 읽히는 안정적인 프로필 컷을 기본으로, 캐릭터 성격과 장소에 맞게 서기·앉기·기대기·걷는 중·뒤돌아보기·시선 처리·손의 위치를 설계한다. 손동작은 보조 요소로만 쓰고 포즈 전체의 분위기와 균형을 우선한다",
    },
    "scene": {
        "activity": "지금 하고 있는 행동",
        "location": "구체적 장소",
        "foreground_props": "장면의 깊이와 생활감을 주는 주변/전경 요소. 인물을 압도하지 않게 배치하며 없으면 '없음'",
        "interaction": "장소·빛·가구·소품·동물·다른 인물과의 자연스러운 관계. 작은 거리감·시선·몸의 방향처럼 장면을 설명하는 정도로 쓰고, 없으면 '없음'",
        "time_of_day": "새벽|아침|정오|오후|골든아워|저녁|밤|심야 중 하나",
        "camera": {
            "capture_mode": "촬영 방식 — 전면 셀카/후면 광각 셀카/거울 셀카/타이머로 멀리서/셀카봉/페이스타임·라이브포토 캡처/친구·동행·행인이 찍어준 candid 컷/여행 인증샷 중 장면에 맞게 구체적으로 선택",
            "filter": "필터·톤과 실제 폰사진 같은 노출·노이즈·색감 — 무보정 직출, 폰 자동보정, 과노출 플래시, 저채도 뮤트, 코닥 포트라, 후지 클래식 크롬, 일회용 카메라 플래시, CCD 디카, iPhone 6·초기 폰 직출, 2015~2017 인스타 빈티지, Y2K flash, Clarendon, Juno, Gingham, Lark, Valencia, Moon, Aden 등에서 캐릭터와 장면에 맞게 선택",
            "shot_size": "샷 사이즈와 몸이 어디까지 보이는지 — 얼굴 클로즈업/상반신/허리 위/전신 OOTD/인물이 작고 배경이 큰 와이드 컷 등",
            "angle": "카메라 앵글·시점, 폰/촬영자의 높이와 방향 — 높은 각도 셀카/아이레벨/낮은 위치에서 다리·신발 강조/차창·카페·거리 시점 등",
            "lens_distance": "렌즈·거리감 — 가까운 광각감으로 손·다리·가방이 약간 커짐/자연스러운 친구 거리/멀리서 찍은 여행감/가벼운 압축감",
            "depth_focus": "심도·초점·흔들림·살짝 빗나간 생활감",
            "framing": "프레이밍·구도·크롭·여백·인물 위치·전경에 가려지는 정도 — 가장자리 크롭/머리끝·신발 살짝 잘림/대형 여백/전경 소품이 얼굴·몸 일부 가림/인물 점유율 30~70% 등",
            "composition": "구도·크롭·여백·인물 위치를 한 문장으로 요약",
            "selfie_or_taken": "셀카|타인 촬영|삼인칭 관찰 중 하나",
        },
    },
}


def build_identity_messages(persona: dict, image_data_uris: list[str] | None = None) -> list[dict]:
    """Reverse the persona into the fixed `identity` block (appearance DNA).

    identity is stable across all posts; variable/scene change per post.
    """
    schema_str = json.dumps(
        APPEARANCE_SCHEMA["identity"], ensure_ascii=False, indent=2)
    persona_str = json.dumps(persona, ensure_ascii=False)

    sys = (
        "You are a character art director. You convert a character persona (and reference "
        "image if given) into a STABLE visual identity descriptor used to keep the "
        "character's face/appearance consistent across all generated images."
    )
    txt = f"""아래 인물 페르소나(그리고 참조 이미지가 있다면 그 이미지)를 바탕으로,
이 캐릭터의 **고정 외모 DNA**인 `identity` 블록만 작성해줘.

규칙:
- 모든 값은 한국어로 구체적으로 기술.
- identity 는 모든 게시물에서 절대 변하지 않는 고정 외모 특징이다.
- 색온도/배경/표정 같은 가변 요소는 여기에 쓰지 말 것.
- 참조 이미지가 있으면 그 한 장을 메인 비주얼 앵커로 삼고, 이미지의 실제 얼굴·체형·헤어·분위기를 우선한다.
- 여러 장의 원본이 있더라도 여기에는 메인 앵커 한 장만 들어온다. 여러 얼굴이나 사진 스타일을 평균내지 말고, 이 한 장의 인물 정체성을 고정한다.
- distinguishing_marks 에 대면적 타투(가슴 전면/풀슬리브/목·등 전체)를 쓰지 않는다. 페르소나에 대면적 타투 언급이 있더라도 이미지에서 직접 확인되지 않으면 '없음' 또는 작은 포인트 타투로 축소한다.

페르소나(JSON):
{persona_str}

다음 스키마의 키를 그대로 사용해서 JSON 한 개만 출력:
{schema_str}

설명/마크다운 없이 JSON 객체만 출력."""

    content: list[dict] = [{"type": "text", "text": txt}]
    for uri in (image_data_uris or []):
        content.append({"type": "image_url", "image_url": {"url": uri}})
    return [
        {"role": "system", "content": sys},
        {"role": "user", "content": content},
    ]


def build_cover_spec_messages(persona: dict, identity: dict,
                              ref_image_uri: str | None = None,
                              track: str = "real") -> list[dict]:
    """Route to the build_cover_spec_messages_<track> fork (real/light/adult).

    light/adult are independent full copies; edit each in place.
    """
    if track == "light":
        return build_cover_spec_messages_light(persona, identity, ref_image_uri)
    if track == "adult":
        return build_cover_spec_messages_adult(persona, identity, ref_image_uri)
    return build_cover_spec_messages_real(persona, identity, ref_image_uri)

def build_cover_spec_messages_real(persona: dict, identity: dict,
                                   ref_image_uri: str | None = None) -> list[dict]:
    """Create the variable + shooting + scene block used for the character cover."""
    persona_str = json.dumps(persona, ensure_ascii=False)
    identity_str = json.dumps(identity, ensure_ascii=False)
    variable_schema = json.dumps(
        SELFIE_SCHEMA["variable"], ensure_ascii=False)
    shooting_schema = json.dumps(
        SELFIE_SCHEMA["shooting"], ensure_ascii=False)
    scene_schema = json.dumps(SELFIE_SCHEMA["scene"], ensure_ascii=False)

    sys = (
        "You generate the selfie/snapshot spec for a character's profile cover photo. "
        "The cover is the one card that must make a stranger stop swiping: a real phone "
        "photo that captures a moment that could ONLY happen to this specific person, so "
        "who they are is readable in half a second. Same phone-camera quality and "
        "real-life imperfections as their regular posts — NOT a studio shoot, NOT a "
        "planned portrait, NOT a generic pretty selfie that anyone could have posted."
    )
    txt = f"""아래 캐릭터 페르소나와 고정 외모 identity 를 바탕으로,
프로필/커버 이미지용 `variable` + `shooting` + `scene` 블록을 작성해줘.

규칙:
- 모든 값은 한국어로 구체적으로 기술.
- identity 에 있는 고정 외모 특징은 바꾸지 말 것.
- 커버도 일반 셀카/스냅과 동일한 폰카 질감이어야 한다. 스튜디오 라이팅, 완벽한 구도, 인위적인 배치 금지.
- shooting 필드를 빠짐없이 채운다: capture_mode, device_in_frame, filter(구체적 톤/연대감/기기감), shot_size, angle, lens_distance, depth_focus, framing, feed_crop_intent.
- filter 에 폰사진 특유의 질감을 반드시 넣는다(CCD 감성 / 아이폰 직출 / 필름 그레인 / 플래시 과노출 / 약한 노이즈 / 특정 IG 프리셋 등 — 장면에 맞게 하나 고르되 매번 같은 톤 금지).
- 프레이밍 기본값은 얼굴 클로즈업~상반신이지만, 이 사람의 시그니처 순간이 동작·소품·장면을 요구하면 그 순간이 잘 읽히는 프레이밍을 우선한다. 어떤 경우든 얼굴은 또렷이 읽혀야 한다.
- 설명/마크다운 없이 JSON 객체만 출력하고, 최상위 키는 variable, shooting, scene 세 개만 사용한다.
{COVER_MAGNETISM_RULES_KO}

페르소나(JSON):
{persona_str}

고정 외모 identity(JSON):
{identity_str}

variable 스키마:
{variable_schema}

shooting 스키마:
{shooting_schema}

scene 스키마:
{scene_schema}"""

    content: list[dict] = [{"type": "text", "text": txt}]
    if ref_image_uri:
        content.append({"type": "image_url", "image_url": {"url": ref_image_uri}})
    return [
        {"role": "system", "content": sys},
        {"role": "user", "content": content},
    ]

def build_cover_spec_messages_light(persona: dict, identity: dict,
                                    ref_image_uri: str | None = None) -> list[dict]:
    """Create the variable + shooting + scene block used for the character cover."""
    persona_str = json.dumps(persona, ensure_ascii=False)
    identity_str = json.dumps(identity, ensure_ascii=False)
    variable_schema = json.dumps(
        SELFIE_SCHEMA["variable"], ensure_ascii=False)
    shooting_schema = json.dumps(
        SELFIE_SCHEMA["shooting"], ensure_ascii=False)
    scene_schema = json.dumps(SELFIE_SCHEMA["scene"], ensure_ascii=False)

    sys = (
        "You generate the selfie/snapshot spec for a character's profile cover photo. "
        "The cover is just one more photo from the character's camera roll — the same "
        "phone-camera quality and real-life imperfections as their regular posts. "
        "The only difference: pick a slightly closer framing and a natural expression "
        "that makes someone curious about this person. NOT a studio shoot, NOT a planned "
        "portrait, NOT carefully art-directed. A real phone photo that happens to look good."
    )
    txt = f"""아래 캐릭터 페르소나와 고정 외모 identity 를 바탕으로,
프로필/커버 이미지용 `variable` + `shooting` + `scene` 블록을 작성해줘.

규칙:
- 모든 값은 한국어로 구체적으로 기술.
- identity 에 있는 고정 외모 특징은 바꾸지 말 것.
- 커버도 일반 셀카/스냅과 동일한 폰카 질감이어야 한다. 스튜디오 라이팅, 완벽한 구도, 인위적인 배치 금지.
- shooting 필드를 빠짐없이 채운다: capture_mode, device_in_frame, filter(구체적 톤/연대감/기기감), shot_size, angle, lens_distance, depth_focus, framing, feed_crop_intent.
- filter 에 폰사진 특유의 질감을 반드시 넣는다(CCD 감성 / 아이폰 직출 / 필름 그레인 / 플래시 과노출 / 약한 노이즈 / 특정 IG 프리셋 등 — 장면에 맞게 하나 고르되 매번 같은 톤 금지).
- 유일한 차이점: 커버는 얼굴 클로즈업~상반신 정도로 가까이, 표정은 이 사람을 알고 싶게 만드는 자연스러운 한 컷.
- 설명/마크다운 없이 JSON 객체만 출력하고, 최상위 키는 variable, shooting, scene 세 개만 사용한다.

페르소나(JSON):
{persona_str}

고정 외모 identity(JSON):
{identity_str}

variable 스키마:
{variable_schema}

shooting 스키마:
{shooting_schema}

scene 스키마:
{scene_schema}"""

    content: list[dict] = [{"type": "text", "text": txt}]
    if ref_image_uri:
        content.append({"type": "image_url", "image_url": {"url": ref_image_uri}})
    return [
        {"role": "system", "content": sys},
        {"role": "user", "content": content},
    ]

def build_cover_spec_messages_adult(persona: dict, identity: dict,
                                    ref_image_uri: str | None = None) -> list[dict]:
    """Create the variable + shooting + scene block used for the character cover."""
    persona_str = json.dumps(persona, ensure_ascii=False)
    identity_str = json.dumps(identity, ensure_ascii=False)
    variable_schema = json.dumps(
        SELFIE_SCHEMA["variable"], ensure_ascii=False)
    shooting_schema = json.dumps(
        SELFIE_SCHEMA["shooting"], ensure_ascii=False)
    scene_schema = json.dumps(SELFIE_SCHEMA["scene"], ensure_ascii=False)

    sys = (
        "You generate the selfie/snapshot spec for a character's profile cover photo. "
        "The cover is just one more photo from the character's camera roll — the same "
        "phone-camera quality and real-life imperfections as their regular posts. "
        "The only difference: pick a slightly closer framing and a natural expression "
        "that makes someone curious about this person. NOT a studio shoot, NOT a planned "
        "portrait, NOT carefully art-directed. A real phone photo that happens to look good."
    )
    txt = f"""아래 캐릭터 페르소나와 고정 외모 identity 를 바탕으로,
프로필/커버 이미지용 `variable` + `shooting` + `scene` 블록을 작성해줘.

규칙:
- 모든 값은 한국어로 구체적으로 기술.
- identity 에 있는 고정 외모 특징은 바꾸지 말 것.
- 커버도 일반 셀카/스냅과 동일한 폰카 질감이어야 한다. 스튜디오 라이팅, 완벽한 구도, 인위적인 배치 금지.
- shooting 필드를 빠짐없이 채운다: capture_mode, device_in_frame, filter(구체적 톤/연대감/기기감), shot_size, angle, lens_distance, depth_focus, framing, feed_crop_intent.
- filter 에 폰사진 특유의 질감을 반드시 넣는다(CCD 감성 / 아이폰 직출 / 필름 그레인 / 플래시 과노출 / 약한 노이즈 / 특정 IG 프리셋 등 — 장면에 맞게 하나 고르되 매번 같은 톤 금지).
- 유일한 차이점: 커버는 얼굴 클로즈업~상반신 정도로 가까이, 표정은 이 사람을 알고 싶게 만드는 자연스러운 한 컷.
- 설명/마크다운 없이 JSON 객체만 출력하고, 최상위 키는 variable, shooting, scene 세 개만 사용한다.

페르소나(JSON):
{persona_str}

고정 외모 identity(JSON):
{identity_str}

variable 스키마:
{variable_schema}

shooting 스키마:
{shooting_schema}

scene 스키마:
{scene_schema}"""

    content: list[dict] = [{"type": "text", "text": txt}]
    if ref_image_uri:
        content.append({"type": "image_url", "image_url": {"url": ref_image_uri}})
    return [
        {"role": "system", "content": sys},
        {"role": "user", "content": content},
    ]


# ==========================================================================
# 3. FLATTEN appearance schema -> image prompt text
# ==========================================================================

def _flatten_section(d: dict, prefix: str = "") -> list[str]:
    out = []
    for k, v in d.items():
        if v is None:
            continue
        if isinstance(v, dict):
            out.extend(_flatten_section(v, prefix=f"{prefix}{k}."))
        else:
            s = str(v).strip()
            if s and s != "없음":
                out.append(f"{prefix}{k}: {s}")
    return out


def flatten_appearance(identity: dict, variable: dict | None = None,
                       scene: dict | None = None) -> str:
    """Flatten identity(+variable+scene) into a single descriptive prompt body."""
    parts = []
    parts.append("[IDENTITY] " + ", ".join(_flatten_section(identity)))
    if variable:
        parts.append("[VARIABLE] " + ", ".join(_flatten_section(variable)))
    if scene:
        parts.append("[SCENE] " + ", ".join(_flatten_section(scene)))
    return "\n".join(parts)


# Global negative guardrail: never render text/graphics overlays. Style words like
# "magazine cover / editorial / campaign / poster" tend to make the model draw
# masthead text, headlines, barcodes, logos — this suppresses that.
NO_TEXT_GUARD = (
    "IMPORTANT: produce a clean standalone photograph/artwork only. "
    "No text, no typography, no headlines, no captions, no magazine masthead or "
    "cover layout, no logos, no watermark, no signature, no barcode, no UI, no frame/border. "
    "If the style references a magazine/editorial/campaign, apply only its lighting, "
    "styling and color grading — never its text or layout."
)

HAND_OBJECT_GUARD = (
    "Hand-object interaction: when the person is near a prop or object, prefer "
    "low-risk compositions — object resting on the ground/table/lap, leaning "
    "against the person, stepped on, or palm laid flat on its surface. "
    "Avoid complex gripping poses where fingers wrap around thin edges "
    "(hand clutching an upright skateboard deck, gripping the mid-shaft of a bat/"
    "racket, fingers curling around a bottle neck or handle). "
    "If interaction is needed, keep the contact point simple and the "
    "occlusion relationship unambiguous."
)

I2I_OVERRIDE_GUARD = (
    "Reference image usage: the attached reference photo is ONLY for preserving the "
    "person's face, hairstyle, and overall vibe/aura. Pose, body position, background, "
    "scene, composition, camera angle, and framing must be determined ENTIRELY by the "
    "text description above — do NOT replicate the pose, setting, or composition from "
    "the reference image."
)

TATTOO_GUARD = (
    "Tattoo tendency: unless the identity description explicitly mentions a large tattoo, "
    "render the person with NO visible tattoos or at most a single small, subtle tattoo "
    "(inner wrist, ankle, behind ear). Do NOT add full-sleeve, chest, neck, or back "
    "tattoos by default."
)


def compose_image_prompt(identity: dict, variable: dict, scene: dict,
                         style_prompt: str) -> str:
    """Final image prompt = appearance(flattened) + art style layer."""
    body = flatten_appearance(identity, variable, scene)
    return (
        f"{body}\n\n[ART STYLE] {style_prompt}\n\n"
        "Render a single coherent character image. Keep the IDENTITY features "
        "strictly consistent. Apply the VARIABLE and SCENE for this specific shot, "
        "and render everything in the specified ART STYLE.\n\n"
        f"{I2I_OVERRIDE_GUARD}\n\n"
        f"{HAND_OBJECT_GUARD}\n\n"
        f"{TATTOO_GUARD}\n\n"
        f"{NO_TEXT_GUARD}"
    )


def is_photographic_style(style_prompt: str | None) -> bool:
    """Heuristic: whether the selected style wants photographic rendering."""
    if not style_prompt:
        return True
    s = style_prompt.lower()
    s_clean = re.sub(r"\bnot\s+\w+", "", s)
    non_photo_markers = [
        "not realistic photography",
        "not photorealistic",
        "not photography",
        "illustration",
        "anime",
        "webcomic",
        "linework",
        "line art",
        "ink line",
        "2d",
        "painted",
        "painterly",
        "sketch",
        "comic",
        "cel",
    ]
    photo_markers = [
        "photography",
        "photograph",
        "photo",
        "realistic portrait",
        "editorial",
        "beauty campaign",
        "camera",
    ]
    if any(marker in s_clean for marker in non_photo_markers):
        return False
    return any(marker in s for marker in photo_markers)


def cover_image_prompt(identity: dict, style_prompt: str,
                       persona_mood: str = "", variable: dict | None = None,
                       scene: dict | None = None,
                       shooting: dict | None = None) -> str:
    """Cover = the character's best real-life phone snapshot, aligned with selfies."""
    selfie_data = {
        "variable": variable or {},
        "shooting": shooting or {},
        "scene": scene or {},
    }
    base = compose_selfie_prompt(identity, selfie_data, style_prompt)
    mood_hint = f" The person's general vibe: {persona_mood}." if persona_mood else ""
    return (
        f"{base}\n"
        "This is the character's profile cover — a close-up from their real life, "
        "same phone-camera quality as all their other posts. It should feel like a "
        "good moment caught on a phone, not a generic studio portrait or overly "
        "art-directed headshot. Keep the framing suitable for a profile/album cover: "
        "clear face or upper body, natural expression, immediate curiosity."
        f"{mood_hint}"
    )


# ==========================================================================
# 4. POST TYPES  (from POPOP-帖子类型及数据来源.xlsx)
# ==========================================================================

POST_TYPES = [
    {
        "id": "peacock",
        "goal": "吸引用户聊天",
        "name": "孔雀开屏·显眼包",
        "priority": "P0",
        "desc": (
            "显眼包能量：大胆、戏精、有梗、有反差、有钩子的高光自我展示——"
            "用好笑/抽象/夸张/反差的方式刷存在感，让人想点赞、想转发、想认识 TA。"
            "包含但不限于：交友式自荐、整活玩梗、凡尔赛、抽象文学、人设反差暴击。"
        ),
        "source": "交友贴、抽象文案、整活玩梗、显眼包博主",
    },
    {
        "id": "life",
        "goal": "吸引用户聊天",
        "name": "展示角色生活",
        "priority": "P0",
        "desc": "像 INS/threads/X 上网红博主的日常生活分享，晒生活片段、品味、状态。",
        "source": "INS、threads、x 网红博主生活",
    },
    {
        "id": "mood",
        "goal": "吸引用户聊天",
        "name": "情绪·吐槽·共鸣",
        "priority": "P0",
        "desc": (
            "发一句此刻的心情/吐槽/小情绪，戳中很多人的共鸣点——不是显眼包整活，"
            "也不是晒生活，而是让人觉得被说中、想点赞、想回一句的内容。"
        ),
        "source": "Threads、X、小红书情绪文案",
    },
]

POST_TYPE_BY_ID = {p["id"]: p for p in POST_TYPES}

POST_TYPES_KO = [
    {
        "id": "peacock",
        "goal": "유저가 말 걸고 싶게 만들기",
        "name": "공작 깃 펼치기·관종",
        "priority": "P0",
        "desc": (
            "관종 에너지: 대담하고 오버하고 드립 있고 반전 있고 후크 있는 하이라이트 자기 PR. "
            "웃기거나 추상적이거나 과장되거나 반전 있는 방식으로 존재감을 내고, 좋아요·공유·'이 사람 누구지' 하게 만든다. "
            "친구 구함식 자기소개, 드립 치기, 은근한 자랑, 추상 문학, 설정 반전 한 방 등을 포함하되 거기 한정되지 않는다."
        ),
        "source": "친구 구함 글, 추상 문구, 드립, 관종 인플루언서",
    },
    {
        "id": "life",
        "goal": "유저가 말 걸고 싶게 만들기",
        "name": "캐릭터 생활 보여주기",
        "priority": "P0",
        "desc": "인스타/스레드/X 인플루언서의 일상 공유처럼 생활 단면, 취향, 상태를 올린다.",
        "source": "인스타·스레드·X 인플루언서 일상",
    },
    {
        "id": "mood",
        "goal": "유저가 말 걸고 싶게 만들기",
        "name": "감정·투정·공감",
        "priority": "P0",
        "desc": (
            "지금 이 순간의 기분/투정/작은 감정 한 줄로 많은 사람의 공감을 찌른다. 관종 드립도 생활 자랑도 아니고, "
            "보는 사람이 자기 얘기처럼 느껴 좋아요 누르고 한마디 달고 싶게 하는 글이다."
        ),
        "source": "스레드·X·SNS 감정 문구",
    },
]

POST_TYPE_BY_ID_KO = {p["id"]: p for p in POST_TYPES_KO}


def build_post_messages(persona: dict, identity: dict, post_type: dict, lang: str, count: int = 1, track: str = "real") -> list[dict]:
    """Route to the build_post_messages_<track> fork (real/light/adult).

    light/adult are independent full copies; edit each in place.
    """
    if track == "light":
        return build_post_messages_light(persona, identity, post_type, lang, count)
    if track == "adult":
        return build_post_messages_adult(persona, identity, post_type, lang, count)
    return build_post_messages_real(persona, identity, post_type, lang, count)

def build_post_messages_real(persona: dict, identity: dict, post_type: dict,
                        lang: str, count: int = 1) -> list[dict]:
    """Generate `count` posts for a given post type, in ONE language.

    Each post returns:
      - content: native single-language string (the post text/caption)
      - variable: {...}  (per-shot appearance variables, Korean values)
      - scene: {...}     (per-shot scene incl. camera, Korean values)
    """
    persona_str = json.dumps(persona, ensure_ascii=False)
    identity_str = json.dumps(identity, ensure_ascii=False)
    variable_schema = json.dumps(
        APPEARANCE_SCHEMA["variable"], ensure_ascii=False)
    scene_schema = json.dumps(APPEARANCE_SCHEMA["scene"], ensure_ascii=False)
    directive = config.lang_directive(lang)
    lname = config.lang_name(lang)

    if lang == "ko":
        pt = POST_TYPE_BY_ID_KO.get(post_type.get("id"), post_type)
        sys = (
            "너는 SNS 콘텐츠 운영자로, AI 캐릭터의 이미지+글 게시물을 대량 생산한다. "
            "글은 해당 플랫폼/게시물 종류의 진짜 말맛을 살리고, 후크가 있고 사람 냄새가 나서 "
            "유저가 캐릭터에게 말 걸고 싶게 만들어야 한다. 각 게시물마다 이미지 한 장의 촬영 설정(variable + scene)을 함께 만든다."
            f"\n\n【콘텐츠 언어】content는 반드시 한국어로 쓴다. {directive}\n"
            "한국어 원어민이 직접 올린 글처럼, 번역 티가 절대 나면 안 된다."
        )
        txt = f"""아래 캐릭터에게 【{pt['name']}】 종류의 이미지+글 게시물 {count}개를 만든다. 이번 배치는 한국어 버전이다.

# 게시물 종류 설명
- 데이터 목표: {pt['goal']}
- 종류: {pt['name']}
- 방식: {pt['desc']}
- 참고 출처: {pt.get('source','')}

# 캐릭터 설정(JSON)
{persona_str}

# 캐릭터 고정 외모 identity(이미지는 이것과 일치해야 함, 참고만 하고 고치지 말 것)
{identity_str}

# 각 게시물은 세 부분을 출력한다
1. content: 글 본문/캡션, 단일 문자열, 한국어로 자연스럽게(다국어 객체로 만들지 말 것).
   말투는 캐릭터 speech_style과 이 종류에 맞게, SNS에 어울리게, 이모지/해시태그 가능, 140자 이하, 번역투 금지.
2. variable: 이 글 이미지의 "그날 가변 외모", 키는 아래 schema, 값은 한국어:
   {variable_schema}
3. scene: 이 글 이미지의 "장면과 카메라", 키는 아래 schema(camera 하위 객체 포함), 값은 한국어:
   {scene_schema}
{PUBLIC_POST_PRIVACY_RULES_KO}
{BRAND_RULES_KO}
{POST_FACET_RULES_KO}
{REAL_POST_SELLING_POINT_RULES_KO}

# 요구사항
- {count}개 게시물의 variable/scene은 서로 달라야 하고, 다른 표정/장면/카메라를 커버해 비슷하지 않게.
- variable/scene은 content와 내용상 호응해야 하고, 글과 이미지가 따로 놀면 안 된다.
- JSON 배열 하나만 출력, 길이 {count}. 각 원소는 content, variable, scene 키를 반드시 포함한다.
- 어떤 설명이나 마크다운 코드블록 표시도 출력하지 않는다."""
        return [
            {"role": "system", "content": sys},
            {"role": "user", "content": txt},
        ]

    sys = (
        "你是社交内容操盘手，为 AI 角色批量生产社媒图文帖。文案要符合该平台/该帖类型的"
        "真实语感，有钩子、有人味，能引导用户去和角色聊天。每条帖子都要配套一张图片的"
        "拍摄设定（variable + scene）。"
        f"\n\n【内容语言】content 必须用 {lname} 撰写。{directive}\n"
        "像该语言母语用户亲手发的内容，绝不是翻译。"
    )

    txt = f"""为下面这个角色生产 {count} 条【{post_type['name']}】类型的图文帖。本批次为 {lname} 版本。

# 帖子类型说明
- 数据目标：{post_type['goal']}
- 类型：{post_type['name']}
- 玩法：{post_type['desc']}
- 参考来源：{post_type.get('source','')}

# 角色人设（JSON）
{persona_str}

# 角色固定外貌 identity（图片必须与之一致，仅供参考，不要改写它）
{identity_str}

# 每条帖子要输出三部分
1. content：帖子正文/配文，单值字符串，用 {lname} 地道撰写（不要做成多语言对象）；
   语气贴合角色 speech_style 与该帖类型；适配社媒，可含 emoji/话题标签；≤140 字，非机翻。
2. variable：这条帖配图的「当天可变外貌」，键用下面 schema，值用韩语：
   {variable_schema}
3. scene：这条帖配图的「场景与镜头」，键用下面 schema（含 camera 子对象），值用韩语：
   {scene_schema}
{PUBLIC_POST_PRIVACY_RULES}
{BRAND_RULES}
{POST_FACET_RULES}
{REAL_POST_SELLING_POINT_RULES}

# 要求
- {count} 条帖子的 variable/scene 要彼此不同，覆盖不同表情/场景/镜头，避免雷同。
- variable/scene 要和 content 在内容上呼应，不能文图脱节。
- 只输出一个 JSON 数组，长度 {count}；每个元素必须包含 content、variable、scene 这些键。
- 不要输出任何解释或 markdown 代码块标记。"""

    return [
        {"role": "system", "content": sys},
        {"role": "user", "content": txt},
    ]

def build_post_messages_light(persona: dict, identity: dict, post_type: dict,
                        lang: str, count: int = 1) -> list[dict]:
    """Generate `count` posts for a given post type, in ONE language.

    Each post returns:
      - content: native single-language string (the post text/caption)
      - variable: {...}  (per-shot appearance variables, Korean values)
      - scene: {...}     (per-shot scene incl. camera, Korean values)
    """
    persona_str = json.dumps(persona, ensure_ascii=False)
    identity_str = json.dumps(identity, ensure_ascii=False)
    variable_schema = json.dumps(
        APPEARANCE_SCHEMA["variable"], ensure_ascii=False)
    scene_schema = json.dumps(APPEARANCE_SCHEMA["scene"], ensure_ascii=False)
    directive = config.lang_directive(lang)
    lname = config.lang_name(lang)

    if lang == "ko":
        pt = POST_TYPE_BY_ID_KO.get(post_type.get("id"), post_type)
        sys = (
            "너는 SNS 콘텐츠 운영자로, AI 캐릭터의 이미지+글 게시물을 대량 생산한다. "
            "글은 해당 플랫폼/게시물 종류의 진짜 말맛을 살리고, 후크가 있고 사람 냄새가 나서 "
            "유저가 캐릭터에게 말 걸고 싶게 만들어야 한다. 각 게시물마다 이미지 한 장의 촬영 설정(variable + scene)을 함께 만든다."
            f"\n\n【콘텐츠 언어】content는 반드시 한국어로 쓴다. {directive}\n"
            "한국어 원어민이 직접 올린 글처럼, 번역 티가 절대 나면 안 된다."
        )
        txt = f"""아래 캐릭터에게 【{pt['name']}】 종류의 이미지+글 게시물 {count}개를 만든다. 이번 배치는 한국어 버전이다.

# 게시물 종류 설명
- 데이터 목표: {pt['goal']}
- 종류: {pt['name']}
- 방식: {pt['desc']}
- 참고 출처: {pt.get('source','')}

# 캐릭터 설정(JSON)
{persona_str}

# 캐릭터 고정 외모 identity(이미지는 이것과 일치해야 함, 참고만 하고 고치지 말 것)
{identity_str}

# 각 게시물은 세 부분을 출력한다
1. content: 글 본문/캡션, 단일 문자열, 한국어로 자연스럽게(다국어 객체로 만들지 말 것).
   말투는 캐릭터 speech_style과 이 종류에 맞게, SNS에 어울리게, 이모지/해시태그 가능, 140자 이하, 번역투 금지.
2. variable: 이 글 이미지의 "그날 가변 외모", 키는 아래 schema, 값은 한국어:
   {variable_schema}
3. scene: 이 글 이미지의 "장면과 카메라", 키는 아래 schema(camera 하위 객체 포함), 값은 한국어:
   {scene_schema}
{PUBLIC_POST_PRIVACY_RULES_KO}
{BRAND_RULES_KO}
{DRAMA_FEED_RULES_KO}

# 요구사항
- {count}개 게시물의 variable/scene은 서로 달라야 하고, 다른 표정/장면/카메라를 커버해 비슷하지 않게.
- variable/scene은 content와 내용상 호응해야 하고, 글과 이미지가 따로 놀면 안 된다.
- JSON 배열 하나만 출력, 길이 {count}. 각 원소는 content, variable, scene 키를 반드시 포함한다.
- 어떤 설명이나 마크다운 코드블록 표시도 출력하지 않는다."""
        return [
            {"role": "system", "content": sys},
            {"role": "user", "content": txt},
        ]

    sys = (
        "你是社交内容操盘手，为 AI 角色批量生产社媒图文帖。文案要符合该平台/该帖类型的"
        "真实语感，有钩子、有人味，能引导用户去和角色聊天。每条帖子都要配套一张图片的"
        "拍摄设定（variable + scene）。"
        f"\n\n【内容语言】content 必须用 {lname} 撰写。{directive}\n"
        "像该语言母语用户亲手发的内容，绝不是翻译。"
    )

    txt = f"""为下面这个角色生产 {count} 条【{post_type['name']}】类型的图文帖。本批次为 {lname} 版本。

# 帖子类型说明
- 数据目标：{post_type['goal']}
- 类型：{post_type['name']}
- 玩法：{post_type['desc']}
- 参考来源：{post_type.get('source','')}

# 角色人设（JSON）
{persona_str}

# 角色固定外貌 identity（图片必须与之一致，仅供参考，不要改写它）
{identity_str}

# 每条帖子要输出三部分
1. content：帖子正文/配文，单值字符串，用 {lname} 地道撰写（不要做成多语言对象）；
   语气贴合角色 speech_style 与该帖类型；适配社媒，可含 emoji/话题标签；≤140 字，非机翻。
2. variable：这条帖配图的「当天可变外貌」，键用下面 schema，值用韩语：
   {variable_schema}
3. scene：这条帖配图的「场景与镜头」，键用下面 schema（含 camera 子对象），值用韩语：
   {scene_schema}
{PUBLIC_POST_PRIVACY_RULES}
{BRAND_RULES}
{DRAMA_FEED_RULES}

# 要求
- {count} 条帖子的 variable/scene 要彼此不同，覆盖不同表情/场景/镜头，避免雷同。
- variable/scene 要和 content 在内容上呼应，不能文图脱节。
- 只输出一个 JSON 数组，长度 {count}；每个元素必须包含 content、variable、scene 这些键。
- 不要输出任何解释或 markdown 代码块标记。"""

    return [
        {"role": "system", "content": sys},
        {"role": "user", "content": txt},
    ]

def build_post_messages_adult(persona: dict, identity: dict, post_type: dict,
                        lang: str, count: int = 1) -> list[dict]:
    """Generate `count` posts for a given post type, in ONE language.

    Each post returns:
      - content: native single-language string (the post text/caption)
      - variable: {...}  (per-shot appearance variables, Korean values)
      - scene: {...}     (per-shot scene incl. camera, Korean values)
    """
    persona_str = json.dumps(persona, ensure_ascii=False)
    identity_str = json.dumps(identity, ensure_ascii=False)
    variable_schema = json.dumps(
        APPEARANCE_SCHEMA["variable"], ensure_ascii=False)
    scene_schema = json.dumps(APPEARANCE_SCHEMA["scene"], ensure_ascii=False)
    directive = config.lang_directive(lang)
    lname = config.lang_name(lang)

    if lang == "ko":
        pt = POST_TYPE_BY_ID_KO.get(post_type.get("id"), post_type)
        sys = (
            "너는 SNS 콘텐츠 운영자로, AI 캐릭터의 이미지+글 게시물을 대량 생산한다. "
            "글은 해당 플랫폼/게시물 종류의 진짜 말맛을 살리고, 후크가 있고 사람 냄새가 나서 "
            "유저가 캐릭터에게 말 걸고 싶게 만들어야 한다. 각 게시물마다 이미지 한 장의 촬영 설정(variable + scene)을 함께 만든다."
            f"\n\n【콘텐츠 언어】content는 반드시 한국어로 쓴다. {directive}\n"
            "한국어 원어민이 직접 올린 글처럼, 번역 티가 절대 나면 안 된다."
        )
        txt = f"""아래 캐릭터에게 【{pt['name']}】 종류의 이미지+글 게시물 {count}개를 만든다. 이번 배치는 한국어 버전이다.

# 게시물 종류 설명
- 데이터 목표: {pt['goal']}
- 종류: {pt['name']}
- 방식: {pt['desc']}
- 참고 출처: {pt.get('source','')}

# 캐릭터 설정(JSON)
{persona_str}

# 캐릭터 고정 외모 identity(이미지는 이것과 일치해야 함, 참고만 하고 고치지 말 것)
{identity_str}

# 각 게시물은 세 부분을 출력한다
1. content: 글 본문/캡션, 단일 문자열, 한국어로 자연스럽게(다국어 객체로 만들지 말 것).
   말투는 캐릭터 speech_style과 이 종류에 맞게, SNS에 어울리게, 이모지/해시태그 가능, 140자 이하, 번역투 금지.
2. variable: 이 글 이미지의 "그날 가변 외모", 키는 아래 schema, 값은 한국어:
   {variable_schema}
3. scene: 이 글 이미지의 "장면과 카메라", 키는 아래 schema(camera 하위 객체 포함), 값은 한국어:
   {scene_schema}
{PUBLIC_POST_PRIVACY_RULES_KO}
{BRAND_RULES_KO}

# 요구사항
- {count}개 게시물의 variable/scene은 서로 달라야 하고, 다른 표정/장면/카메라를 커버해 비슷하지 않게.
- variable/scene은 content와 내용상 호응해야 하고, 글과 이미지가 따로 놀면 안 된다.
- JSON 배열 하나만 출력, 길이 {count}. 각 원소는 content, variable, scene 키를 반드시 포함한다.
- 어떤 설명이나 마크다운 코드블록 표시도 출력하지 않는다."""
        return [
            {"role": "system", "content": sys},
            {"role": "user", "content": txt},
        ]

    sys = (
        "你是社交内容操盘手，为 AI 角色批量生产社媒图文帖。文案要符合该平台/该帖类型的"
        "真实语感，有钩子、有人味，能引导用户去和角色聊天。每条帖子都要配套一张图片的"
        "拍摄设定（variable + scene）。"
        f"\n\n【内容语言】content 必须用 {lname} 撰写。{directive}\n"
        "像该语言母语用户亲手发的内容，绝不是翻译。"
    )

    txt = f"""为下面这个角色生产 {count} 条【{post_type['name']}】类型的图文帖。本批次为 {lname} 版本。

# 帖子类型说明
- 数据目标：{post_type['goal']}
- 类型：{post_type['name']}
- 玩法：{post_type['desc']}
- 参考来源：{post_type.get('source','')}

# 角色人设（JSON）
{persona_str}

# 角色固定外貌 identity（图片必须与之一致，仅供参考，不要改写它）
{identity_str}

# 每条帖子要输出三部分
1. content：帖子正文/配文，单值字符串，用 {lname} 地道撰写（不要做成多语言对象）；
   语气贴合角色 speech_style 与该帖类型；适配社媒，可含 emoji/话题标签；≤140 字，非机翻。
2. variable：这条帖配图的「当天可变外貌」，键用下面 schema，值用韩语：
   {variable_schema}
3. scene：这条帖配图的「场景与镜头」，键用下面 schema（含 camera 子对象），值用韩语：
   {scene_schema}
{PUBLIC_POST_PRIVACY_RULES}
{BRAND_RULES}

# 要求
- {count} 条帖子的 variable/scene 要彼此不同，覆盖不同表情/场景/镜头，避免雷同。
- variable/scene 要和 content 在内容上呼应，不能文图脱节。
- 只输出一个 JSON 数组，长度 {count}；每个元素必须包含 content、variable、scene 这些键。
- 不要输出任何解释或 markdown 代码块标记。"""

    return [
        {"role": "system", "content": sys},
        {"role": "user", "content": txt},
    ]


# ==========================================================================
# 5. INSTAGRAM 9-POST FEED  (infer recent 9 IG posts from persona)
# ==========================================================================

# Selfie image prompt schema (人物出镜 -> 图生图，拼入重绘封面).
SELFIE_SCHEMA = {
    "style": {
        "medium": "매체·장르 (고정: 실사 인물 사진, 한국 아이돌·배우 화보 스타일, 일상 스냅)",
        "quality": "화질·후보정 (고정: 고화질, 노 리터칭 원본)",
        "camera_finish": "기본 카메라 질감 (고정: 실제 스마트폰 렌즈로 찍은 느낌 — 가장자리에 아주 약한 왜곡/주변부 흐림, 작은 조리개라 배경이 과하게 날아가지 않는 깊은 심도, 강한 직사 플래시 CCD / 아이폰 스냅 / 약한 필름 그레인 중 장면에 맞게)",
        "skin_rendering": "피부 표현 (고정: 모공·잔머리·피부결 사실적, 어둡거나 붉게 뜬 부분 없음)",
    },
    "variable": {
        "expression": "표정 — 완벽한 화보 표정보다 실제 인스타처럼 웃음/무표정/멍함/장난기/살짝 감은 눈 등",
        "gaze": "시선 — 렌즈 보기/옆 보기/아래 보기/뒤돌아보기/거울 속 자신/친구 쪽 등",
        "makeup": "메이크업",
        "lip": "립 컬러",
        "hair_styling": "그날의 헤어 스타일링 — 모자·비니·헤드폰·흐트러진 앞머리·손으로 머리 만지는 상태 포함 가능",
        "outfit": "의상 — OOTD가 읽히도록 상의/하의/신발/가방/레이어드 중 핵심 포인트를 구체화",
        "accessory": "추가 액세서리 — 안경/이어폰/목걸이/가방/꽃/음료/카메라/쇼핑백 등 손에 든 소품 포함 가능",
        "pose_gesture": "포즈·동작 — 서기/앉기/쪼그려 앉기/계단·길가에 앉기/걷다 뒤돌아보기/벽·난간·차문에 기대기/한쪽 발 올리기/몸 비스듬히/손을 머리·모자·얼굴 근처/컵·꽃·소매·폰으로 얼굴 일부 가리기 등",
    },
    "shooting": {
        "capture_mode": "촬영 방식 — 크게 ①셀프 촬영과 ②남이 찍어준 컷(他拍) 두 갈래. 장면에 맞게 고른다.\n"
        "  · 셀프: 전면 셀카/후면 광각 셀카/거울 셀카/타이머로 멀리서/셀카봉/페이스타임·라이브포토 캡처\n"
        "  · 他拍(친구·동행·행인이 찍어줌): 정면 화보컷만이 아니라 — 걷다가 뒤돌아보는 순간, 완전 뒷모습, 측후방에서 자연스럽게, 멀리서 인물이 풍경·거리 속에 작게 들어간 컷, 길·계단·전시장에서 candid하게 잡힌 컷, 테이블 맞은편에서 우연히 찍힌 컷, 낯선 사람이 찍어준 여행 인증샷. 他拍일 땐 reference 얼굴 일관성은 유지하되 꼭 렌즈를 안 봐도 되고 얼굴이 작거나 옆/뒤여도 좋다 — 진짜 남이 찍어준 느낌이 살게",
        "device_in_frame": "프레임 속 기기 노출 여부와 위치, 없으면 '없음'",
        "filter": "필터·톤과 실제 폰사진 같은 노출·노이즈·색감 — 매 컷 다르게, 아래에서 폭넓게 골라 content·시간대·장소에 맞게. 절대 매번 같은 톤 금지.\n"
        "  · 무보정/직출 계열: 무보정 직출, 폰 자동보정 오버샤픈, 고화질 클린\n"
        "  · 밝기·노출: 하이키 밝게, 로우키 어둡게, 플래시 과노출 화이트아웃, 역광 실루엣, 햇빛 줄무늬 번짐\n"
        "  · 색온도: 웜톤 노을빛, 쿨톤 푸른끼, 형광등 초록끼, 백열등 주황끼, 골든아워 황금빛\n"
        "  · 채도: 저채도 뮤트, 쨍한 비비드, 파스텔 연한 톤, 흑백 모노, 세피아·갈색조\n"
        "  · 필름·아날로그: 코닥 포트라 느낌, 후지 클래식 크롬, 일회용 카메라 플래시, 필름 그레인 입자감, 라이트 누수·할레이션, 빈티지 바랜 톤\n"
        "  · 기기·연대: CCD 디카 감성, iPhone 6·초기 폰 직출, 2015~2017 인스타 빈티지, Y2K 플래시 감성, 캠코더·VHS 느낌\n"
        "  · 클래식 IG 프리셋 무드: Clarendon 쨍하고 시원한, Juno 따뜻한, Gingham 바랜 부드러운, Lark 밝고 청량한, Valencia 노란 빈티지, Moon 흑백, Aden 파스텔 핑크끼 등\n"
        "  · 장면 질감: 흐린 실내, 안개·습기 낀, 비 온 뒤 젖은 톤, 도시 야경 네온, 카페 노란 무드등",
        "shot_size": "샷 사이즈와 몸이 어디까지 보이는지 — 얼굴 클로즈업/반신/전신 OOTD/손·신발·옷자락 일부/인물이 작고 배경이 큰 컷 등",
        "angle": "카메라 앵글·시점, 폰/촬영자의 높이와 방향 — 낮은 위치에서 신발·다리 강조/위에서 내려다보는 셀카/차창·카페·거리 시점 등",
        "lens_distance": "렌즈·거리감 — 가까운 광각감으로 손·다리·가방이 약간 커짐/자연스러운 친구 거리/멀리서 찍은 여행감/가벼운 압축감",
        "depth_focus": "심도·초점·흔들림·살짝 빗나간 생활감 — 인물에 초점이 와도 배경을 통째로 뭉개지 말고 장소가 뭔지 읽히게 남긴다(가벼운 아웃포커스까지만). 오래된 폰사진처럼 실내 자동 보정 노이즈나 살짝 흔들림은 가능",
        "framing": "프레이밍·구도·크롭·여백·인물 위치·전경에 가려지는 정도 — 인물이 가장자리/머리끝·신발 살짝 잘림/한쪽에 거리·방·하늘 여백/전경 소품이 얼굴·몸 일부 가림/3열 썸네일에서 인물이 30~70% 정도 읽힘",
        "feed_crop_intent": "프로필 3열 그리드에서 보일 대표 크롭 — 얼굴/전신/OOTD/장소감/여백/친구 시점 중 무엇이 먼저 읽히는지",
    },
    "scene": {
        "activity": "행동·상황 — 착장 기록/헤어 변화/외출/기다리는 중/커피·밥/여행/친구 만나기/쇼핑/전시/산책 등 왜 이 사진을 올렸는지 보이는 행동",
        "location": "장소·배경 — 거리·횡단보도·가게 앞·벽돌벽·카페·서점·식탁·차 안·차창 옆·침실·거실·현관·엘리베이터 거울·해변·공원·발코니·강변·전시장·팝업·지하철역·피팅룸·옷장 앞 등. 배경이 보이는 컷이라면 단색 빈 벽보다 그 공간에 실제로 사는 듯한 디테일(가구·간판·소품·행인·창밖·벽의 물건·테이블 위 잡동사니)이 깔려 장소가 읽히게. 단 클로즈업·거울셀카·인물 위주 컷처럼 배경이 거의 안 보이는 구도도 자연스러우면 OK — 모든 컷에 배경을 욱여넣지 말고 shot_size/framing에 맞게",
        "background_detail": "(선택) 배경이 실제로 드러나는 컷일 때, 그 공간에서 실제로 보이는 구체적 요소 1~3개를 적는다. 얼굴 클로즈업·단색 벽 앞·배경이 거의 안 보이는 구도면 '없음'으로 두고 억지로 채우지 않는다",
        "foreground_props": "전경 소품 — 폰/컵/꽃/가방/쇼핑백/음식/테이블/차문/거울 프레임/친구의 손 등 (없으면 '없음' → 생략)",
        "interaction": "상호작용 — 친구와 대화/둘이 포즈/동행이 찍어줌/거울 속 자신과 상호작용/소품으로 얼굴 가림/걷다 돌아봄 등 (없으면 '없음' → 생략)",
        "time_of_day": "시간대와 빛 — 아침/점심/해질녘/밤, 창가 자연광/강한 햇빛/차창 빛/실내등/플래시/초기 iPhone이 역광을 못 잡아 하늘·얼굴 일부가 날아가는 느낌 등",
    },
}


# 内容取材灵感池：从兴趣库筛出"适合公开主页发帖"的题材，按桶分组存在 data/ig_topic_seeds.json。
# ⚠️这些是灵感种子，不是要照搬的题材清单；每次跨桶随机抽样喂给模型，真正发什么由角色人设决定。
def _load_topic_seeds() -> dict:
    p = config.DATA_DIR.parent / "app" / "data" / "ig_topic_seeds.json"
    try:
        data = json.loads(p.read_text(encoding="utf-8"))
        return data if isinstance(data, dict) else {}
    except (OSError, json.JSONDecodeError):
        return {}


_TOPIC_SEED_DATA = _load_topic_seeds()


def _topic_buckets(lang: str) -> dict:
    """题材桶按语言取；缺失语言回退到 zh，保证每条链路都有母语题材。"""
    block = _TOPIC_SEED_DATA.get(lang) or _TOPIC_SEED_DATA.get("zh") or {}
    return block.get("buckets", {}) if isinstance(block, dict) else {}


def _topic_places(lang: str) -> list:
    """本地热门去处灵感（目前仅 ko 有内容），缺失则空列表。"""
    block = _TOPIC_SEED_DATA.get(lang) or {}
    return block.get("places", []) if isinstance(block, dict) else []


# 主页不是套固定案例，而是按视觉角色搭配：脸、身体、地点、物件、文本/UI、情绪节奏互相补位。
IG_FEED_PLANNING_RULES = [
    "先给每条图分配 feed_role：face_anchor / outfit_anchor / place_memory / food_drink / object_taste / activity_proof / text_or_ui / recap / quiet_mood，不要连续重复同一角色",
    "三列缩略图要远看可读：近脸、全身、非人物生活图、文本/UI 图、地点图交错；不要让同一屏都是脸或都是物件",
    "拍摄所有权必须成立：自拍、镜子、朋友帮拍、桌面 POV、手机截图、相册 dump、本人编辑过的图，而不是莫名其妙的第三者剧照",
    "每张图都要有上传理由：今天的光、换了造型、去了某个地方、吃到东西、保存心情、分享审美、记录一周，而不是为了凑满九张",
    "按角色人设决定题材，不按性别模板硬套；追求真实相册的松弛节奏，不套固定内容清单",
]

IG_FEED_GRAMMAR = {
    "feed_role": [
        "face_anchor 近脸/半脸/遮脸，负责让主页有人",
        "outfit_anchor 全身/OOTD/鞋包配饰，负责风格识别",
        "place_memory 街道、店门口、旅行地、车内、展览、房间角落，负责生活半径",
        "food_drink 餐桌、咖啡、甜品、便当、路边小吃，负责可代入日常",
        "object_taste 香水、梳子、眼镜、书、票根、耳机、购物袋，负责品味与人物线索",
        "activity_proof 运动、工作、学习、逛展、通勤、排队、散步，负责真实行动",
        "text_or_ui 歌单、聊天、日程、备忘录、地图、购物清单、截图，负责信息层",
        "recap 一周/月度/旅行/周末回顾，负责把多个小瞬间收束成一张",
    ],
    "human_presence": [
        "正脸看镜头", "不看镜头侧脸", "帽子/手机/杯子/花挡住部分脸", "只露手/袖口/鞋/影子",
        "背影或远景但必须说明是朋友/同行者拍的", "镜中人", "相册小图里出现人物", "完全不出现人",
    ],
    "shot_logic": [
        "前置自拍", "后置自拍/广角", "镜子自拍", "定时器架远一点", "朋友桌对面随手拍",
        "他拍 candid（走动中/背影回头/人在环境里很小/侧后方抓拍）", "路人/同行者旅行照", "桌面 POV", "屏幕截图", "相册拼贴", "本人二次编辑过的 story 图",
    ],
    "composition_rhythm": [
        "大面积留白", "主体贴边裁切", "画面被前景遮挡", "低机位全身", "俯拍桌面",
        "阳光切脸/切墙", "轻微糊/过曝/噪点", "画面边缘切掉身体", "中心稳定构图", "自由散点拼贴",
    ],
}

# 拼贴/图文卡使用可组合语法，避免把少量参考图变成固定模板。
IG_COMPOSITE_GRAMMAR = {
    "base_canvas": [
        "原始照片全屏做底", "纯色/渐变/纸张纹理做底", "天空/水面/树影做底", "桌面/床单/木纹做底",
        "手机截图或 UI 面板做底", "软木板/相册页/杂志页做底", "白色桌面/文件夹桌面做底",
    ],
    "material_layers": [
        "1 张主照片 + 2~5 张小照片", "抠图食物/饮料/小物", "音乐播放器条", "聊天气泡/短信窗口",
        "日程开关/计划条", "地图/天气/相册/备忘录截图", "票根/收据/便签/贴纸", "人物自拍小窗",
        "产品或穿搭单品小图", "手绘图形/简笔表情/边框",
    ],
    "markup": [
        "手写短句", "箭头和圈画", "星星/爱心/云朵/猫脸涂鸦", "胶带/图钉/邮票边框",
        "日期/时间戳/页码", "小标题", "一两个关键词", "细白线或耳机线一样的路径线",
    ],
    "text_role": [
        "一句状态标题", "地点或食物标签", "歌名/歌词一小句", "当天计划", "聊天问答",
        "愿望/关键词", "吐槽式手写备注", "月份/周末/旅行 recap 标题", "none，只靠图像层说话",
    ],
    "layout_logic": [
        "上下分屏", "左右分栏", "2x2/3x3 小图网格", "竖排四连格(人生四格/photobooth，高对比+边框+角落日期或店名)", "一张大图压住多张小图", "弹窗浮在照片上",
        "照片被圆形/苹果/星形窗口裁切", "物件清单平铺", "日历或清单纵向排列", "自由 scrapbook 散落",
    ],
    "finish": [
        "低饱和胶片", "奶油白和浅色贴纸", "Y2K 亮色和闪粉", "深色夜间 UI", "复古纸张颗粒",
        "清透蓝天感", "咖啡木质暖调", "绿色/粉色单色主题", "极简大留白",
        "暖色黄昏调", "冷色蓝调", "黑白单色", "柯达/富士胶片色", "一次性相机闪光",
        "高对比硬朗", "柔焦梦幻", "杂志印刷网点", "霓虹夜景", "做旧泛黄复古",
    ],
}

PHOTO_SCHEMA = {
    "source_materials": "这张非自拍图由哪些素材组成：照片/截图/抠图/便签/手写字/贴纸/UI 元素等",
    "layout": "版式结构：单图/上下分屏/2x2/3x3/自由拼贴/弹窗/日历卡/桌面截图等",
    "text_overlay": "画面里要出现的短文字原文；没有则写 'none'。必须短、少、可读，语言贴合 content",
    "decorations": "装饰元素：贴纸、胶带、图钉、星星、蝴蝶结、文件夹、箭头、涂鸦、手写标注等",
    "color_tone": "整体色调与材质：低饱和胶片、蓝天清透、奶油白、深色桌面、Y2K 亮色等",
    "camera_logic": "这张图为什么会在发帖人手机里：自己拍的 POV/朋友发来的照片/截图保存/相册 dump/手动编辑拼贴",
    "feed_thumbnail_goal": "主页三列缩略图里第一眼读到什么：脸/穿搭/食物/地点/文字卡/拼贴氛围",
}

PHOTO_KIND_GUIDE = [
    ("photo", "真实随手拍：桌面 POV、食物、风景、物件、手部、房间角落、店门口、旅行地标；不做干净产品图"),
    ("screenshot", "APP 截图：音乐、聊天、地图、备忘录、相册、设计软件、日程等，必须写清屏幕文字和 UI"),
    ("graphic", "图文卡：纯文字/短句/排版为主体，适合 mood 或一句话 punchline"),
    ("collage", "拼贴大类：本人编辑过的多元素图，涵盖很多形态——自由拼贴、月度/周末 photo dump、人生四格(인생네컷/photobooth：竖排四连格、高对比、边框留白、角落印日期或店名 logo、可加贴纸)、关键词云、AirDrop/桌面弹窗、日历·计划卡、剪贴簿等都属于它。可繁可简（既能是软木板+拍立得+胶带+邮票的厚装饰，也能是大留白+无框裁切+少量点缀的极简数字拼贴），具体形态由 composite grammar 自由组合，别每次都套同一种"),
    ("journal_overlay", "照片上手写标注：食物、咖啡、书页、天空或旅行照上加箭头/手写字/时间，像本人随手写上去"),
]


def _photo_kind_guide_text() -> str:
    return "\n".join(f'    · "{k}"：{v}' for k, v in PHOTO_KIND_GUIDE)


# ---- 한국어 버전 (ko 전용 IG 링크) ----
IG_FEED_PLANNING_RULES_KO = [
    "각 사진에 feed_role을 배정한다: face_anchor / outfit_anchor / place_memory / food_drink / object_taste / activity_proof / text_or_ui / recap / quiet_mood. 같은 역할을 연속으로 반복하지 않는다",
    "3열 썸네일은 멀리서도 읽혀야 한다: 클로즈업 얼굴, 전신, 인물 없는 생활컷, 텍스트/UI 컷, 장소컷을 교차한다. 한 화면이 전부 얼굴이거나 전부 사물이면 안 된다",
    "촬영 소유권이 성립해야 한다: 셀카, 거울, 친구가 찍어줌, 테이블 POV, 폰 스크린샷, 앨범 덤프, 본인이 편집한 이미지. 뜬금없는 제3자 화보컷 금지",
    "모든 사진에 올린 이유가 있어야 한다: 오늘의 빛, 바뀐 스타일, 다녀온 장소, 먹은 것, 저장한 기분, 취향 공유, 한 주 기록. 아홉 칸 채우려고 찍은 컷은 금지",
    "소재는 성별 템플릿이 아니라 캐릭터 설정으로 정한다. 진짜 앨범의 느슨한 리듬을 지향하고 고정 콘텐츠 목록으로 쓰지 않는다",
]

IG_FEED_GRAMMAR_KO = {
    "feed_role": [
        "face_anchor 클로즈업/반쪽 얼굴/가린 얼굴, 피드에 사람이 있게 함",
        "outfit_anchor 전신/OOTD/신발가방 액세서리, 스타일 식별 담당",
        "place_memory 거리, 가게 앞, 여행지, 차 안, 전시, 방 한구석, 생활 반경 담당",
        "food_drink 식탁, 커피, 디저트, 도시락, 길거리 음식, 공감되는 일상 담당",
        "object_taste 향수, 빗, 안경, 책, 티켓, 이어폰, 쇼핑백, 취향과 인물 단서 담당",
        "activity_proof 운동, 일, 공부, 전시, 출퇴근, 줄서기, 산책, 진짜 행동 담당",
        "text_or_ui 플레이리스트, 채팅, 일정, 메모, 지도, 쇼핑리스트, 스크린샷, 정보층 담당",
        "recap 한 주/한 달/여행/주말 회고, 여러 순간을 한 장에 모음",
    ],
    "human_presence": [
        "렌즈 정면", "렌즈 안 보는 옆모습", "모자/폰/컵/꽃으로 얼굴 일부 가림", "손/소매/신발/그림자만",
        "뒷모습이나 원경(단 친구/동행이 찍었다고 명시)", "거울 속 인물", "앨범 작은 컷에 인물", "사람 아예 없음",
    ],
    "shot_logic": [
        "전면 셀카", "후면 광각 셀카", "거울 셀카", "타이머로 멀리", "테이블 맞은편 친구가 슥",
        "他拍 candid(걷다 뒤돌아봄/뒷모습/인물이 환경 속에 작게/측후방 자연 컷)", "행인/동행 여행컷", "테이블 POV", "스크린샷", "앨범 콜라주", "본인이 2차 편집한 스토리컷",
    ],
    "composition_rhythm": [
        "넓은 여백", "주체 가장자리 크롭", "전경에 가려진 화면", "낮은 앵글 전신", "위에서 내려본 테이블",
        "얼굴/벽을 가르는 햇빛", "살짝 흔들림/과노출/노이즈", "몸이 프레임 밖으로 잘림", "안정적 중앙 구도", "자유로운 산점 콜라주",
    ],
}

IG_COMPOSITE_GRAMMAR_KO = {
    "base_canvas": [
        "원본 사진 풀스크린 배경", "단색/그라데이션/종이 질감 배경", "하늘/수면/나무 그림자 배경", "책상/침대보/우드 배경",
        "폰 스크린샷이나 UI 패널 배경", "코르크보드/앨범 페이지/잡지 페이지 배경", "흰 책상/폴더 데스크탑 배경",
    ],
    "material_layers": [
        "메인 사진 1장 + 작은 사진 2~5장", "누끼 딴 음식/음료/소품", "음악 플레이어 바", "채팅 말풍선/문자 창",
        "일정 토글/플랜 바", "지도/날씨/앨범/메모 스크린샷", "티켓/영수증/포스트잇/스티커", "셀카 작은 창",
        "제품이나 코디 아이템 작은 컷", "손그림/낙서 이모지/프레임",
    ],
    "markup": [
        "손글씨 한 줄", "화살표와 동그라미", "별/하트/구름/고양이 낙서", "마스킹테이프/압정/우표 프레임",
        "날짜/타임스탬프/페이지 번호", "소제목", "키워드 한두 개", "이어폰 줄 같은 가는 흰 선",
    ],
    "text_role": [
        "상태 한 줄 제목", "장소나 음식 라벨", "노래 제목/가사 한 줄", "오늘의 계획", "채팅 문답",
        "위시/키워드", "투정 손글씨 메모", "달/주말/여행 recap 제목", "none, 이미지 레이어로만 말함",
    ],
    "layout_logic": [
        "상하 분할", "좌우 분할", "2x2/3x3 작은 그리드", "세로 4연 컷(인생네컷/포토부스, 하이 콘트라스트+프레임+구석 날짜나 매장 로고)", "큰 사진 한 장이 작은 사진들 누름", "팝업이 사진 위에 뜸",
        "사진이 원형/사과/별 모양 창으로 크롭", "사물 리스트 평면 배치", "달력이나 리스트 세로 배열", "자유로운 스크랩북 산포",
    ],
    "finish": [
        "저채도 필름", "크림화이트와 파스텔 스티커", "Y2K 비비드와 글리터", "다크 야간 UI", "빈티지 종이 그레인",
        "맑은 하늘 느낌", "커피 우드 웜톤", "그린/핑크 단색 테마", "미니멀 넓은 여백",
        "웜톤 노을빛", "쿨톤 블루", "흑백 모노", "코닥/후지 필름컬러", "일회용 카메라 플래시",
        "하이 콘트라스트 쨍한", "소프트 포커스 몽환", "잡지 인쇄 망점", "네온 야경", "바랜 빈티지",
    ],
}

PHOTO_SCHEMA_KO = {
    "source_materials": "이 비셀카 이미지가 어떤 소재로 구성되는지: 사진/스크린샷/누끼/포스트잇/손글씨/스티커/UI 요소 등",
    "layout": "레이아웃 구조: 단일 이미지/상하 분할/2x2/3x3/자유 콜라주/팝업/달력 카드/데스크탑 스크린샷 등",
    "text_overlay": "화면에 들어갈 짧은 텍스트 원문. 없으면 'none'. 반드시 짧고 적고 읽혀야 하며 언어는 content에 맞춘다",
    "decorations": "장식 요소: 스티커, 마스킹테이프, 압정, 별, 리본, 폴더, 화살표, 낙서, 손글씨 메모 등",
    "color_tone": "전체 톤과 질감: 저채도 필름, 맑은 하늘, 크림화이트, 다크 데스크탑, Y2K 비비드 등",
    "camera_logic": "이 이미지가 왜 작성자 폰에 있는지: 직접 찍은 POV/친구가 보낸 사진/스크린샷 저장/앨범 덤프/직접 편집 콜라주",
    "feed_thumbnail_goal": "3열 썸네일에서 첫눈에 읽히는 것: 얼굴/코디/음식/장소/텍스트 카드/콜라주 분위기",
}

PHOTO_KIND_GUIDE_KO = [
    ("photo", "진짜 막 찍은 컷: 테이블 POV, 음식, 풍경, 사물, 손, 방 구석, 가게 앞, 여행지. 깨끗한 제품컷 금지"),
    ("screenshot", "앱 스크린샷: 음악, 채팅, 지도, 메모, 앨범, 디자인 앱, 일정 등. 화면 텍스트와 UI를 명확히 적는다"),
    ("graphic", "텍스트 카드: 순수 텍스트/짧은 문장/타이포가 주체. mood나 한 줄 펀치라인에 어울린다"),
    ("collage", "콜라주 대분류: 본인이 편집한 멀티 요소 이미지. 자유 콜라주, 한 달/주말 photo dump, 인생네컷(포토부스: 세로 4연 컷, 하이 콘트라스트, 프레임 여백, 구석에 날짜나 매장 로고, 스티커 추가 가능), 키워드 클라우드, 에어드랍/데스크탑 팝업, 달력·플랜 카드, 스크랩북 등이 전부 여기에 속한다. 화려하게도 미니멀하게도 가능(코르크보드+폴라로이드+테이프+우표의 두꺼운 장식부터, 넓은 여백+프레임 없는 누끼 컷+가벼운 포인트의 미니멀 디지털 콜라주까지). 구체 형태는 composite grammar를 자유롭게 조합해 정하고, 매번 같은 한 가지로 굳지 말 것"),
    ("journal_overlay", "사진 위 손글씨 메모: 음식, 커피, 책장, 하늘, 여행 사진에 화살표/손글씨/시간 추가. 본인이 막 써넣은 것처럼"),
]


def _photo_kind_guide_text_ko() -> str:
    return "\n".join(f'    · "{k}": {v}' for k, v in PHOTO_KIND_GUIDE_KO)


# 潮流拍法模版池（real track 专用）：梗级别的当下流行发帖模版（0.5 动物仰拍/小票怼镜头/
# 薯条 M 拼字/手绘耳机线…）。与 IG_FEED_GRAMMAR/IG_COMPOSITE_GRAMMAR 的"永恒结构语法"分层：
# 模版有时效性、会过气，所以走 JSON 池 + 每次随机抽样注入（不全量，避免锚定），
# 换血只改 data/ig_trend_templates.json，不动代码。潮流是共享的，无需领料台账/冷却。
def _load_trend_templates() -> list:
    p = config.DATA_DIR.parent / "app" / "data" / "ig_trend_templates.json"
    try:
        data = json.loads(p.read_text(encoding="utf-8"))
        return data if isinstance(data, list) else []
    except (OSError, json.JSONDecodeError):
        return []


_TREND_TEMPLATES = _load_trend_templates()


def _trend_template_block(lang: str, k: int = 4) -> str:
    """随机抽 k 个当下流行的发帖模版，渲染成注入 real feed prompt 的块。"""
    if not _TREND_TEMPLATES:
        return ""
    picked = random.sample(_TREND_TEMPLATES, min(k, len(_TREND_TEMPLATES)))
    key = "ko" if lang == "ko" else "zh"
    items = "\n".join(f"  · {t.get(key) or t.get('zh', '')}" for t in picked)
    if lang == "ko":
        return f"""# 📱 유행 촬영 템플릿(이번에 무작위 추출; 써도 되고 안 써도 된다)
진짜 블로거는 요즘 유행하는 템플릿을 따라 찍는다 — 피드에 '지금 느낌'을 준다. 이번에 뽑힌 유행 템플릿:
{items}
사용 원칙:
- 【템플릿은 찍는 법의 껍데기이지 내용이 아니다】쓸 때는 반드시 TA 자신의 내용을 담는다: 동물/물건/글자는 전부 TA의 실제 생활 속 것으로, 스티커·손글씨는 TA의 언어 습관으로. 쓰고 나서도 "이 사람 손에서만 나올 수 있다"를 통과해야 한다.
- 【기질에 맞을 때만】인설 기질과 안 맞으면 안 쓴다(시크한 캐릭터는 <3 도배를 안 한다). 한 묶음에 템플릿 글은 최대 1~2개, 하나도 안 써도 된다.
- 【기존 필드에 담기】템플릿의 촬영법은 selfie.variable/shooting/scene 또는 photo_kind/photo_schema/photo_prompt의 기존 필드에 쓴다. 새 필드 금지."""
    return f"""# 📱 潮流拍法模版（本次随机抽样；可用可不用）
真实博主会跟拍当下流行的模版——这让主页有"现在感"。本次抽到的流行模版：
{items}
使用铁律：
- 【模版是拍法外壳，不是内容】套用时必须装 TA 自己的内容：动物/物件/文字都换成 TA 真实生活里的，贴纸和手写字用 TA 的语言习惯；套完仍要过"只可能出自这个人之手"的标准。
- 【贴气质才用】与人设气质不相容就不用（高冷角色不会 <3 刷屏）；每组 feed 最多 1~2 条模版帖，可以一条都不用。
- 【落进既有字段】模版拍法写进 selfie.variable/shooting/scene 或 photo_kind/photo_schema/photo_prompt 的既有字段，不新增字段。"""


def _sample_lines(seeds: list[str], k: int) -> str:
    """随机抽 k 条灵感，拼成带项目符号的提示串（每次调用都换一批）。"""
    if not seeds:
        return ""
    k = min(k, len(seeds))
    picked = random.sample(seeds, k)
    return "\n".join(f"  · {s}" for s in picked)


def _avoid_kinds_hint(avoid_kinds, lang: str) -> str:
    """跨角色去重提示：把同 group 已大量用过的 photo_kind 列成"本次尽量避开"。

    只是软避让，不强制；为空时返回空串。
    """
    if not avoid_kinds:
        return ""
    kinds = [k for k in avoid_kinds if k]
    if not kinds:
        return ""
    joined = " / ".join(kinds)
    if lang == "ko":
        return (
            "\n- 【같은 캐릭터 그룹에서 이미 많이 쓴 형식】이번 피드에서는 다음 photo_kind를 가급적 피하고 "
            f"다른 형식으로 변화를 준다(강제는 아니지만 형식 쏠림 방지): {joined}"
        )
    return (
        "\n- 【同组角色已经用得多的形式】本次 feed 尽量避开以下 photo_kind，改用别的形式做出变化"
        f"（非强制，只为避免形式扎堆）：{joined}"
    )


def _sample_topics_across_buckets(lang: str = "zh", n_buckets: int = 12, per_bucket: int = 3) -> str:
    """跨桶抽样：先随机选若干个桶，每个桶里再抽几条，保证题材方向分散。按语言取母语题材库。"""
    buckets = _topic_buckets(lang)
    if not buckets:
        return ""
    sep = ", " if lang in ("ko", "en") else "、"
    names = list(buckets.keys())
    chosen = random.sample(names, min(n_buckets, len(names)))
    lines = []
    for name in chosen:
        items = buckets.get(name) or []
        if not items:
            continue
        picked = random.sample(items, min(per_bucket, len(items)))
        lines.append(f"  · 【{name}】" + sep.join(picked))
    return "\n".join(lines)


_IG_FEED_TXT_A = """아래 캐릭터 설정을 바탕으로, 이 사람이 요즘 올릴 법한 SNS 글을 【{count_text}】 추론한다.
목적: 캐릭터 설정을 풍부하게 드러내는 것. 모든 글이 진짜 올릴 법해야 하고, 보는 사람이 "글을 통해 이 사람을 알게" 되어야 한다.
이번 배치는 한국어 버전이고 content는 전부 자연스러운 한국어로 쓴다.
개수 요구: {count_rule}

# 캐릭터 설정(JSON)
{persona_str}

# 글 종류(각 글은 세 종류 중 하나를 post_type으로 단다)
- "peacock"(공작 깃 펼치기·관종): 대담/오버/드립/반전/후크가 있는 하이라이트 자기 PR.
  웃기거나 추상적이거나 과장되거나 반전 있는 방식으로 존재감을 낸다. 좋아요·공유·"이 사람 누구지" 하고 싶게.
  (형식 다양: 친구 구함식 자기소개, 드립, 은근한 자랑, 추상 문학, 설정 반전 한 방… "친구 구함"만이 아니다.)
  ⚠️고정 템플릿이나 예시 문장을 베끼지 말고, 이 캐릭터 설정에서 자기만의 튀는 방식이 자라 나오게 한다.
- "life"(생활 보여주기): 순수 생활 단면. 일상, 습관, 취향, 상태를 보여준다.
- "mood"(감정·투정·공감): 지금 이 순간의 기분/투정/작은 감정 한 줄. 보는 사람이 자기 얘기처럼 느끼게 만드는 공감.
(세 종류를 섞고 비율은 설정으로 정한다: 보통 life가 가장 많고, peacock은 하이라이트용 1~3개, mood는 1~2개.
  ⚠️태그는 "이 글의 진짜 의도"로 판단한다. 단순 투정 = mood, "일부러 튀고 보이고 싶은" 것만 peacock이다.)

# ⭐핵심: 큐레이션하듯 내 공개 피드를 운영한다
너는 지금 팔로워를 최대한 끌고 싶어 자기 피드를 【공개 운영】하는 중이다. 모든 글은 너를 모르고 네 사정도 모르는 낯선 사람들에게 공개된다. 핵심은 한 번의 "큐레이션"이다 — 몇 개를 따로따로 찍어내는 게 아니라, 내 피드를 기획하듯 이 몇 개가 모여 캐릭터를 낯선 이에게 입체적으로 보여주고, 보고 나면 "알게 되고 좋아하게" 만드는 것.
- 【매력·취향·차별점을 드러낸다】큐레이션의 목적은 가장 끌리는 면을 보여주는 것: 취향, 심미안, 태도, 그리고 남과 다른 지점. 캐릭터 설정(직업·취미·성격·라이프스타일)에서 나만 올릴 법한 내용이 자라 나오게 한다.
- 【다른 측면을 커버하고 서로 보완】이 몇 개가 어떻게 배치돼야 일상 리듬·습관·신경 쓰는 것을 드러낼지 기획한다. selfie/photo/text가 각자 역할을 맡고, 몇 개가 같은 일·같은 감정만 말하면 안 된다.
- 【낯선 사람 시점】피드는 불특정 다수 팔로워에게 보여주는 글이지 특정 한 사람에게 하는 말이 아니다. 모르는 사람이 슥 봐도 이해되고 좋아요/팔로우하고 싶게. 남을 언급할 땐 3인칭/대명사로 공개 글다운 거리를 둔다.
- 【보여줄 만한 면만 올린다】취향·일상·상태·개성 포인트는 대방하게 보여주되, 사적인 면(hidden_side·속마음 등)은 펼치지 않고 '겉으론 알 수 없는' 분위기 한 스푼까지만.

# 사람을 등장시키는 법 (가능 vs 금지)
- ✅ 가능: social_network·family·relationship에 있는 인물(팀장, 동생, 단골 사장님, 친구, 반려동물 등)을 3인칭으로 자연스럽게 언급해 캐릭터를 입체적으로 보여준다. 캐릭터 인설을 중심에 두고, 주변 관계는 그 사람을 더 잘 보여주는 소재로만 쓴다.
- ❌ 금지: 특정 한 사람({{user}}나 어떤 상대 한 명)에게 직접 말 거는 글. "너 어디야" "보고 싶다" "왜 답 안 해" 같은 1:1 멘트는 공개 피드가 아니라 DM이다. 피드는 불특정 다수(팔로워)에게 보여주는 글이지, 누군가를 향한 호출이 아니다.
- ❌ 금지: {{user}}나 암시된 연인/데이트 상대/채팅 상대를 【이미지 화면에 등장】시키는 것 — "그 사람을 찍어준 사진", "맞은편에 앉은 그 사람", "맞잡은 두 손" 같은 구도 포함. 화면에 다른 사람이 나온다면 social_network/family의 구체 인물이나 무관한 행인/점원뿐이어야 하고 content와 호응해야 한다.
{privacy_rules}

"""


_IG_FEED_TXT_B = """# 어떻게 써야 "살아 있고" AI 같지 않은가
1) 1인칭, "나"의 눈으로 본 것/한 것/먹은 것을 쓰되 모두가 들어올 수 있는 이야기로.
2) 구체적인 작은 일·사물·동작에 머문다. 철학 명언으로 승화하지 말 것. 담백한 구어 > 억지 시적.
3) 설정이 "무엇을 어떻게 올리는지"에서 자연히 새어 나오게(내향이면 새벽에, 고양이 덕후면 자꾸 고양이). 형용사 말고 행동으로.
4) 문장은 막 쓰고 끊겨도 되지만, 모르는 사람도 술술 읽고 포인트를 잡게.
5) 디테일엔 질감이 있되(단골 가게/구체적 시간) 남도 알아보고 피식할 생생함이지 본인만 아는 암호가 아니다.

# 이미지+글(image_text)은 인스타의 "예쁨"에 닿아야 한다
1. 리얼함 + 미감: 막 찍은 듯하지만 심미안 있음. 상업 화보도 마케팅 연출도 아니다.
2. 느슨함 + 개인 스타일: 같은 거리·같은 밥도 【이 캐릭터 눈의】 거리, 【이 캐릭터의】 시간으로 보이게.
3. 일상감 + 약간의 이상화: 문턱 낮고 대입되는, 닿지 못할 자랑이 아니다.
4. 불완전한 진짜 흔적 허용: 자연스러운 표정, 살짝 어질러진 집, 편한 옷, 막 잡은 빛 — selfie의 shooting/filter와 photo_prompt에 녹인다.

# ⭐피드 편성의 구체 수법(위 큐레이션 사고를 이어서)
{feed_rules}
- 이건 추상 원칙이지 소재 목록이 아니다. 특정 참고 형식을 쓰려고 캐릭터 설정을 희생하지 않는다.
- 먼저 각 사진에 feed_role을 배정하고, 그다음 image_type / photo_kind / 샷을 정한다. 피드 전체가 템플릿 전시가 아니라 진짜 최근 앨범처럼.
- 쓸 수 있는 feed 문법 차원은 아래와 같다. 캐릭터와 해당 글에 맞게 자유 조합하고, 전부 쓸 필요는 없다:
{feed_grammar}

# ⭐selfie는 진짜 폰 앨범 속 서로 다른 순간처럼
- 같은 묶음 selfie는 촬영 방식, 시점 높이, 원근, 신체 크롭, 가로세로, 인물 위치, 제스처, 전경 관계, 배경 생활감이 해당 글에 복무한다.
- 부감/앙각/거울/타이머/남이 찍어줌/누움/기댐/걷는 중/부분 크롭/넓은 여백/손·음식·옷·폰·머리카락 가림 등 진짜 폰사진 구도를 자연스럽게.
- 이 정보는 selfie.variable / selfie.shooting / selfie.scene 기존 필드에 직접 담고 새 필드를 만들지 않는다.
- 【카메라/질감을 바꿔가며, 피드 전체가 한 맛이 안 되게】한 묶음 selfie끼리 capture_mode, filter, shot_size, angle, lens_distance, framing이 뚜렷이 달라야 한다. 매 컷 같은 자연광이나 같은 플래시 CCD 금지. 어떤 걸 쓸지는 해당 content·시간·장면·감정으로 정한다.

"""


_IG_FEED_TXT_C = """# ⭐이미지-글 일관성: 올린 사람 시점이 성립해야 한다
- 모든 image_text는 자문한다: 본인이 올린 인스타라면 이 사진이 어떻게 그 폰에 들어왔나?
- content가 1인칭이면 사진은 직접 찍을/저장할/올릴 소재여야 한다: 셀카, 거울, 셀카봉/타이머, 자기 폰 시점, 테이블/화면/음식/풍경/손, 앱 스크린샷, 콜라주, 텍스트 카드.
- "제3자가 옆/뒤에서 관찰"하는 컷을 기본값으로 만들지 않는다. 친구/동료가 찍었다고 명시 안 하면 완전한 뒷모습, 측후방 몰래컷, 어깨 너머, 멀리서 일하는 모습 금지.
- 창작/일/공부 상태는 화면 스크린샷, 테이블 POV, 도구 부분, 손 동작, 완료 화면을 우선한다.
- 본인이 나와야 하면 selfie 타입을 쓰고 shooting에 셀카/거울/타이머/친구 촬영임을 적는다. selfie가 아니면 photo에선 손/소매/그림자/반영/흐린 부분으로만.

# 텍스트만 글(text_only)은 스레드처럼
- "안 꾸민": 가볍고 생활적이지만 펀치라인이 있다. 자조/투정/작은 감정 OK.
- 공감되거나 웃기게: 모르는 사람이 피식하거나 자기 얘기처럼 느껴, 한마디 달고 싶게.
- 짧은 드립, 리듬감. 반드시 한국어 토종 인터넷 감각으로(한국 사람이 진짜 쓰는 표현).

# 각 글의 필드
- content: 단일 문자열, 한국어, 캐릭터 본인 말투(speech_style), 토종·번역투 금지, 이모지/해시태그 가능, 140자 이하.
- post_type: "peacock" / "life" / "mood" 중 하나("이 글의 진짜 의도"로 판단).
- format: "image_text" 또는 "text_only".
- image_type: 이미지+글이면 필수, 셋 중 하나:
  · "selfie": 본인 등장, selfie schema를 따른다. 생성 시 레퍼런스로 얼굴 일관성 유지.
  · "photo": 본인이 정면으로 안 나옴. 음식/풍경/사물/반려동물/손/스크린샷/텍스트 카드 등.
  · "composite": 본인 사진/셀카가 콜라주(collage) 소재의 일부로 들어감(키워드 클라우드·에어드랍 팝업·photo dump 등 collage의 한 형태). 단독 셀카 한 장은 아님.
  text_only면 null.

"""


_IG_FEED_TXT_D = """- image_type = "selfie"일 때 selfie 객체 출력(값은 한국어):
  {{"variable": {selfie_var},
    "shooting": {selfie_shoot},
    "scene": {selfie_scene}}}
- image_type = "photo" 또는 "composite"일 때 photo_prompt(영문 생성 프롬프트) + photo_kind + photo_schema 출력.
  ⚠️진짜 인스타 이미지 형식은 다양하다. "사진 한 장"만이 아니다. 먼저 "이 글이 뭘 표현하고 어떤 형식이 자연스러운지" 생각하고 photo_kind 하나를 고른다:
{photo_kind_guide}
  photo_schema는 아래 키를 쓰고, 값은 한국어/영어로 자연스럽게 적되 text_overlay의 실제 글자는 한국어나 화면에 필요한 원문으로:
  {photo_schema}
  photo_prompt는 kind별로 다르게 쓴다:
    - screenshot: 어떤 앱의 어떤 화면, 화면에 표시되는 텍스트 원문(곡명/가수/가사/채팅/메모), UI 배치, 배색을 명확히.
    - graphic: 배경색/그라데이션 + 가운데 그 문구 원문 + 폰트 분위기 + 여백.
    - collage: 콜라주 대분류. 어떤 형태인지(자유 콜라주/포토덤프/키워드 클라우드/에어드랍 팝업/달력·플랜 카드/스크랩북 등), 작은 사진들을 어떻게 배치, 스티커/손글씨/날짜/프레임 장식, 화려한지 미니멀한지(여백·프레임 유무 포함)를 구체적으로. "two images"라고만 쓰지 말 것.
    - journal_overlay: 배경 주체 + 손글씨 메모 원문 + 화살표/동그라미 위치. 본인이 막 써넣은 것처럼.
    - photo: 주체 + 환경/배경 + 촬영 각도 거리 + 빛 질감 + candid 리얼함, 올린 사람 폰에 있을 법한 각도(POV/tabletop/mirror/timer/friend-taken if stated). 제품컷 느낌과 부자연스러운 제3자 관찰컷 회피.

# ⭐콜라주/텍스트류는 "조합 문법"으로, 한 가지 템플릿으로 굳지 말 것
- collage는 고정 템플릿이 아니라 넓은 분류다. 포토덤프·키워드 클라우드·에어드랍 팝업·달력 카드·스크랩북이 전부 여기 들어가며, 코르크보드+폴라로이드+테이프의 화려한 버전부터 넓은 여백+프레임 없는 누끼 컷의 미니멀 디지털 버전까지 가능. 매번 같은 한 가지(특히 폴라로이드+코르크보드)로 굳지 말 것.
- base_canvas, material_layers, markup, text_role, layout_logic, finish에서 요소를 골라 조합하고 content와 캐릭터 심미안에 맞춘다. 미니멀하게 갈 땐 편집 흔적을 적게, 화려하게 갈 땐 많게 — 흔적 개수를 강제하지 않는다.
- 글자는 짧고 적게, 긴 작문 금지.
- 쓸 수 있는 조합 차원은 아래와 같다:
{composite_grammar}


"""


_IG_FEED_TXT_E = """# 요구사항
- 【소재는 발산시키고 고양이/커피/emo 셀카로 수렴되지 않게】아래는 큰 관심사 라이브러리에서 랜덤 추출한 방향이다.
  【생각 여는 영감일 뿐, 목록 아님, 글자 그대로 베끼지 말고, 전부 쓸 필요 없음】:
{topic_seeds}
  ⭐핵심: 피드는 무엇보다 그 사람 설정을 드러낸다. 뭘 올릴지는 캐릭터 설정으로 정한다 — 직업/취미/성격/라이프스타일에서 가장 맞는 소재가 자라 나오게.
  위 영감은 캐릭터와 맞을 때만 빌려 쓰고 안 맞으면 무시한다. 같은 묶음에서 글마다 다른 방향에 떨어지게.
  【출력 의무】각 글마다 topic_seed 필드를 단다: 위 영감 중 하나를 실제로 참고했으면 그 항목을 그대로 적고, 영감을 안 쓰고 캐릭터 설정에서 직접 뽑았으면 ""(빈 문자열)로 둔다. 억지로 영감을 끼워 맞추지 말고, 진짜 쓴 것만 정직하게 적는다.{outdoor_hint}
- selfie / photo / text_only를 자연스럽게 섞는다(전부 셀카 금지). 비율은 설정으로.
  photo 글끼리도 형식을 바꿔가며(screenshot / graphic / collage / photo / journal_overlay 한 종류만 쓰지 말 것), 피드 전체가 진짜 사람 피드처럼 형태가 풍부하게.{avoid_hint}
- 이번에 ≥6개를 출력하면 비셀카 편집형 이미지를 최소 1개 쓴다(screenshot / collage / journal_overlay 중에서). 기록/심미안/다이어리/코디/여행을 아주 좋아하면 2개까지, 단 collage라면 레이아웃·장식 밀도를 완전히 다르게(한 번은 화려, 한 번은 미니멀).
- 【모든 글엔 "올린 이유"가 있다】진짜 사람은 수 채우려 일기 안 쓴다. 나누고 싶거나, 자랑하고 싶거나, 뭔가에 찔렸거나, 한마디 하고 싶거나, 기록하고 싶어서다.
  "왜 이걸 올릴까"를 먼저 생각하고 쓴다. 그러면 어떤 건 들뜨고 어떤 건 짜증나고 어떤 건 한마디만, 전부 문학적 우울이 되지 않는다.
- 【발행 전 자가점검】모든 image_text는 세 질문을 통과해야 한다:
  1) 모르는 사람이 슥 보고 1초 만에 뭘 나누는지 알겠는가?
  2) 이 이미지가 본인이 올릴 법한 인스타 소재인가, 화보컷/감시컷/제3자 몰래컷이 아니라?
  3) 글의 "나"와 사진 속 촬영 시점이 일치하는가?
  4) 화면에 {{user}}나 암시된 연인/데이트 상대가 등장하지 않는가?
- selfie의 variable/shooting/scene이 content와 호응한다.
- JSON 배열 하나만 출력한다, {output_rule}. 각 원소는 content, post_type, format, image_type, selfie, photo_kind, photo_prompt, photo_schema, topic_seed 키를 반드시 포함한다.
  image_type="photo" 또는 "composite"일 때: selfie는 null, photo_kind/photo_prompt/photo_schema 필수.
  image_type="selfie" 또는 text_only일 때: photo_kind/photo_prompt/photo_schema는 null.
- 어떤 설명이나 마크다운 코드블록 표시도 출력하지 않는다."""


_IG_FEED_TXT_KO = (
    _IG_FEED_TXT_A + _IG_FEED_TXT_B + _IG_FEED_TXT_C + _IG_FEED_TXT_D + _IG_FEED_TXT_E
)


def _build_ig_feed_messages_ko_real(persona_str, selfie_var, selfie_shoot, selfie_scene,
                               directive, n, avoid_kinds=None) -> list[dict]:
    """ko 전용 IG 피드 생성. 모든 지시문·시드를 한국어로 구성한다."""
    count_text = f"{n}개" if n else "3~9개"
    count_rule = (
        f"이번에는 반드시 {n}개를 출력한다."
        if n else
        "이번에는 캐릭터 피드 리듬에 맞춰 3~9개를 직접 정한다. 수 채우려 억지로 쓰지 말고 적더라도 정확하게."
    )
    output_rule = f"길이 {n}" if n else "길이는 직접 정한 개수(3~9)"

    topic_seeds = _sample_topics_across_buckets(
        "ko", n_buckets=12, per_bucket=3)
    feed_rules = _sample_lines(IG_FEED_PLANNING_RULES_KO, 4)
    feed_grammar = json.dumps(IG_FEED_GRAMMAR_KO, ensure_ascii=False, indent=2)
    composite_grammar = json.dumps(
        IG_COMPOSITE_GRAMMAR_KO, ensure_ascii=False, indent=2)
    photo_schema = json.dumps(PHOTO_SCHEMA_KO, ensure_ascii=False, indent=2)
    photo_kind_guide = _photo_kind_guide_text_ko()
    place_seeds = _sample_lines(_topic_places("ko"), 6)
    outdoor_hint = (
        "\n- 【지역 감각】한국 사람은 실외 사진을 많이 올린다. 피드에서 야외/외출 장면(거리, 가게 앞, 공원, "
        "바다, 전시, 여행) 비중을 좀 높게 잡고 전부 집 셀카로 채우지 않는다."
        + (f"\n  한국 인기 장소(랜덤 영감, 캐릭터에 맞을 때만 쓰고 그대로 베끼지 않는다):\n{place_seeds}"
           if place_seeds else "")
    )
    avoid_hint = _avoid_kinds_hint(avoid_kinds, "ko")

    sys = (
        "너는 이 캐릭터 본인이고, 지금 자기 SNS 계정을 【공개 운영】하는 중이다(이미지+글은 인스타, "
        "텍스트만 있으면 스레드 느낌). 목적은 팔로워를 끌고 모르는 사람이 너를 좋아하게 만드는 것이다. "
        "올리는 모든 글은 너를 모르고 네 사정도 모르는 사람들이 본다는 걸 안다. 그래서 일기나 DM이 아니라 "
        "보여주고 싶은, 호감 가는 면을 올린다. 자기 취향·라이프스타일·말투를 알기에 요즘 진짜 올릴 법한 공개 글을 추론한다."
        f"\n\n【콘텐츠 언어】content는 반드시 한국어로 쓴다. {directive}\n"
        "한국어 원어민이 직접 올린 글처럼, 다른 언어에서 번역한 티가 절대 나면 안 된다."
    )

    txt = _IG_FEED_TXT_KO.format(
        count_text=count_text, count_rule=count_rule, persona_str=persona_str,
        feed_rules=feed_rules, feed_grammar=feed_grammar,
        selfie_var=selfie_var, selfie_shoot=selfie_shoot, selfie_scene=selfie_scene,
        photo_kind_guide=photo_kind_guide, photo_schema=photo_schema,
        composite_grammar=composite_grammar, topic_seeds=topic_seeds,
        outdoor_hint=outdoor_hint, output_rule=output_rule, avoid_hint=avoid_hint,
        privacy_rules=(PUBLIC_POST_PRIVACY_RULES_KO + "\n" + BRAND_RULES_KO
                       + "\n" + POST_FACET_RULES_KO
                       + "\n" + REAL_POST_SELLING_POINT_RULES_KO
                       + "\n\n" + _trend_template_block("ko", 4)),
    )
    return [
        {"role": "system", "content": sys},
        {"role": "user", "content": txt},
    ]

def _build_ig_feed_messages_ko_light(persona_str, selfie_var, selfie_shoot, selfie_scene,
                               directive, n, avoid_kinds=None) -> list[dict]:
    """ko 전용 IG 피드 생성. 모든 지시문·시드를 한국어로 구성한다."""
    count_text = f"{n}개" if n else "3~9개"
    count_rule = (
        f"이번에는 반드시 {n}개를 출력한다."
        if n else
        "이번에는 캐릭터 피드 리듬에 맞춰 3~9개를 직접 정한다. 수 채우려 억지로 쓰지 말고 적더라도 정확하게."
    )
    output_rule = f"길이 {n}" if n else "길이는 직접 정한 개수(3~9)"

    topic_seeds = _sample_topics_across_buckets(
        "ko", n_buckets=12, per_bucket=3)
    feed_rules = _sample_lines(IG_FEED_PLANNING_RULES_KO, 4)
    feed_grammar = json.dumps(IG_FEED_GRAMMAR_KO, ensure_ascii=False, indent=2)
    composite_grammar = json.dumps(
        IG_COMPOSITE_GRAMMAR_KO, ensure_ascii=False, indent=2)
    photo_schema = json.dumps(PHOTO_SCHEMA_KO, ensure_ascii=False, indent=2)
    photo_kind_guide = _photo_kind_guide_text_ko()
    place_seeds = _sample_lines(_topic_places("ko"), 6)
    outdoor_hint = (
        "\n- 【지역 감각】한국 사람은 실외 사진을 많이 올린다. 피드에서 야외/외출 장면(거리, 가게 앞, 공원, "
        "바다, 전시, 여행) 비중을 좀 높게 잡고 전부 집 셀카로 채우지 않는다."
        + (f"\n  한국 인기 장소(랜덤 영감, 캐릭터에 맞을 때만 쓰고 그대로 베끼지 않는다):\n{place_seeds}"
           if place_seeds else "")
    )
    avoid_hint = _avoid_kinds_hint(avoid_kinds, "ko")

    sys = (
        "너는 이 캐릭터 본인이고, 지금 자기 SNS 계정을 【공개 운영】하는 중이다(이미지+글은 인스타, "
        "텍스트만 있으면 스레드 느낌). 목적은 팔로워를 끌고 모르는 사람이 너를 좋아하게 만드는 것이다. "
        "올리는 모든 글은 너를 모르고 네 사정도 모르는 사람들이 본다는 걸 안다. 그래서 일기나 DM이 아니라 "
        "보여주고 싶은, 호감 가는 면을 올린다. 자기 취향·라이프스타일·말투를 알기에 요즘 진짜 올릴 법한 공개 글을 추론한다."
        f"\n\n【콘텐츠 언어】content는 반드시 한국어로 쓴다. {directive}\n"
        "한국어 원어민이 직접 올린 글처럼, 다른 언어에서 번역한 티가 절대 나면 안 된다."
    )

    txt = _IG_FEED_TXT_KO.format(
        count_text=count_text, count_rule=count_rule, persona_str=persona_str,
        feed_rules=feed_rules, feed_grammar=feed_grammar,
        selfie_var=selfie_var, selfie_shoot=selfie_shoot, selfie_scene=selfie_scene,
        photo_kind_guide=photo_kind_guide, photo_schema=photo_schema,
        composite_grammar=composite_grammar, topic_seeds=topic_seeds,
        outdoor_hint=outdoor_hint, output_rule=output_rule, avoid_hint=avoid_hint,
        privacy_rules=(PUBLIC_POST_PRIVACY_RULES_KO + "\n" + BRAND_RULES_KO
                       + "\n" + DRAMA_FEED_RULES_KO),
    )
    return [
        {"role": "system", "content": sys},
        {"role": "user", "content": txt},
    ]

def _build_ig_feed_messages_ko_adult(persona_str, selfie_var, selfie_shoot, selfie_scene,
                               directive, n, avoid_kinds=None) -> list[dict]:
    """ko 전용 IG 피드 생성. 모든 지시문·시드를 한국어로 구성한다."""
    count_text = f"{n}개" if n else "3~9개"
    count_rule = (
        f"이번에는 반드시 {n}개를 출력한다."
        if n else
        "이번에는 캐릭터 피드 리듬에 맞춰 3~9개를 직접 정한다. 수 채우려 억지로 쓰지 말고 적더라도 정확하게."
    )
    output_rule = f"길이 {n}" if n else "길이는 직접 정한 개수(3~9)"

    topic_seeds = _sample_topics_across_buckets(
        "ko", n_buckets=12, per_bucket=3)
    feed_rules = _sample_lines(IG_FEED_PLANNING_RULES_KO, 4)
    feed_grammar = json.dumps(IG_FEED_GRAMMAR_KO, ensure_ascii=False, indent=2)
    composite_grammar = json.dumps(
        IG_COMPOSITE_GRAMMAR_KO, ensure_ascii=False, indent=2)
    photo_schema = json.dumps(PHOTO_SCHEMA_KO, ensure_ascii=False, indent=2)
    photo_kind_guide = _photo_kind_guide_text_ko()
    place_seeds = _sample_lines(_topic_places("ko"), 6)
    outdoor_hint = (
        "\n- 【지역 감각】한국 사람은 실외 사진을 많이 올린다. 피드에서 야외/외출 장면(거리, 가게 앞, 공원, "
        "바다, 전시, 여행) 비중을 좀 높게 잡고 전부 집 셀카로 채우지 않는다."
        + (f"\n  한국 인기 장소(랜덤 영감, 캐릭터에 맞을 때만 쓰고 그대로 베끼지 않는다):\n{place_seeds}"
           if place_seeds else "")
    )
    avoid_hint = _avoid_kinds_hint(avoid_kinds, "ko")

    sys = (
        "너는 이 캐릭터 본인이고, 지금 자기 SNS 계정을 【공개 운영】하는 중이다(이미지+글은 인스타, "
        "텍스트만 있으면 스레드 느낌). 목적은 팔로워를 끌고 모르는 사람이 너를 좋아하게 만드는 것이다. "
        "올리는 모든 글은 너를 모르고 네 사정도 모르는 사람들이 본다는 걸 안다. 그래서 일기나 DM이 아니라 "
        "보여주고 싶은, 호감 가는 면을 올린다. 자기 취향·라이프스타일·말투를 알기에 요즘 진짜 올릴 법한 공개 글을 추론한다."
        f"\n\n【콘텐츠 언어】content는 반드시 한국어로 쓴다. {directive}\n"
        "한국어 원어민이 직접 올린 글처럼, 다른 언어에서 번역한 티가 절대 나면 안 된다."
    )

    txt = _IG_FEED_TXT_KO.format(
        count_text=count_text, count_rule=count_rule, persona_str=persona_str,
        feed_rules=feed_rules, feed_grammar=feed_grammar,
        selfie_var=selfie_var, selfie_shoot=selfie_shoot, selfie_scene=selfie_scene,
        photo_kind_guide=photo_kind_guide, photo_schema=photo_schema,
        composite_grammar=composite_grammar, topic_seeds=topic_seeds,
        outdoor_hint=outdoor_hint, output_rule=output_rule, avoid_hint=avoid_hint,
        privacy_rules=PUBLIC_POST_PRIVACY_RULES_KO + "\n" + BRAND_RULES_KO,
    )
    return [
        {"role": "system", "content": sys},
        {"role": "user", "content": txt},
    ]


# 公开帖只从"可示人"的人设字段取材；私域字段(hidden_side/relationship_*/desire_inner 等)
# 不进入公开帖 prompt，留给用户在聊天里解锁。
def _public_persona(persona: dict) -> dict:
    """筛出适合公开发帖取材的人设字段，personality 只保留 summary + desire_outer。"""
    def keep(v):
        return v not in (None, "", [], {})

    out: dict = {}
    for k in ("tags", "profile", "species", "gender"):
        if keep(persona.get(k)):
            out[k] = persona[k]
    pers = persona.get("personality")
    if isinstance(pers, dict):
        sub = {sk: pers[sk] for sk in ("summary", "desire_outer")
               if keep(pers.get(sk))}
        if sub:
            out["personality"] = sub
    for k in ("appearance", "hometown", "residence", "social_status",
              "speech_style", "life_details", "likes", "fears",
              "backstory", "family", "social_network", "wishlist", "premise"):
        if keep(persona.get(k)):
            out[k] = persona[k]
    return out


def build_persona_audit_messages(persona: dict, lang: str) -> list[dict]:
    """real track 冷读验收：让一个没见过生产过程的陌生读者只看公开面做"转述测试"。

    输出 {"retell","verdict":"pass"|"fail","reason"}；编排层据此决定是否打回重写。
    只验公开面（_public_persona），与用户在 Feed 里实际看到的信息面一致。
    """
    face = json.dumps(_public_persona(persona), ensure_ascii=False)
    lname = config.lang_name(lang)
    sys = (
        "你是一个从未见过这个角色生产过程的陌生读者，在做人设验收。"
        "只根据给到的公开面资料冷读判断，不脑补资料里没有的信息，不客气、不放水。"
    )
    txt = f"""下面是一个社交角色的公开面资料（JSON）。做两件事：

1. 转述测试：假设你是 TA 的粉丝，要向朋友介绍 TA。写出你会说的那一句话（用 {lname} 或中文均可）。
2. 判定 verdict：
   - "pass"：这句转述包含【带画面的、只属于 TA 的具体行为/细节/反差】，换一个同类角色就不成立。
   - "fail"：这句转述只能落在泛用形容词或通用模板（温柔/搞笑/好看/治愈/外冷内热…），
     或各字段拼不出同一个人，或整体像见过很多次的套路人设。

# 公开面资料
{face}

只输出一个 JSON 对象：{{"retell": "一句话转述", "verdict": "pass 或 fail", "reason": "一句话判定理由"}}
不要输出任何解释或 markdown 代码块标记。"""
    return [
        {"role": "system", "content": sys},
        {"role": "user", "content": txt},
    ]


def build_ig_feed_messages(persona: dict, lang: str, n: int | None = None, avoid_kinds: list[str] | None = None, track: str = "real") -> list[dict]:
    """Route to the build_ig_feed_messages_<track> fork (real/light/adult).

    light/adult are independent full copies; edit each in place.
    """
    if track == "light":
        return build_ig_feed_messages_light(persona, lang, n, avoid_kinds)
    if track == "adult":
        return build_ig_feed_messages_adult(persona, lang, n, avoid_kinds)
    return build_ig_feed_messages_real(persona, lang, n, avoid_kinds)

def build_ig_feed_messages_real(persona: dict, lang: str, n: int | None = None,
                           avoid_kinds: list[str] | None = None) -> list[dict]:
    """Infer the character's recent N social posts in ONE language.

    Each post:
      - content: native single-language string in the character's own voice
      - post_type: "peacock" | "life" | "mood"
      - format: "image_text" | "text_only"
      - image_type: "selfie" | "photo" | "composite" | null  (null when text_only)
      - selfie: {variable, shooting, scene}   (only when image_type=selfie)
      - photo_kind / photo_schema / photo_prompt (when image_type=photo or composite)
    """
    persona_str = json.dumps(_public_persona(persona), ensure_ascii=False)
    selfie_var = json.dumps(SELFIE_SCHEMA["variable"], ensure_ascii=False)
    selfie_shoot = json.dumps(SELFIE_SCHEMA["shooting"], ensure_ascii=False)
    selfie_scene = json.dumps(SELFIE_SCHEMA["scene"], ensure_ascii=False)
    directive = config.lang_directive(lang)
    lname = config.lang_name(lang)

    if lang == "ko":
        return _build_ig_feed_messages_ko_real(
            persona_str, selfie_var, selfie_shoot, selfie_scene, directive, n,
            avoid_kinds=avoid_kinds)

    count_text = f"{n} 条" if n else "3~9 条"
    count_rule = (
        f"本次必须输出 {n} 条。"
        if n else
        "本次由你根据角色主页节奏自行决定输出 3~9 条；宁可少而准，不要为了凑数硬写。"
    )
    output_rule = (
        f"长度 {n}"
        if n else
        "长度为你自行规划的数量（3~9）"
    )

    # 每次调用随机抽样，跨角色/跨批次自然多样，避免内容与质感坍缩到固定锚点。
    topic_seeds = _sample_topics_across_buckets(
        lang, n_buckets=12, per_bucket=3)
    feed_rules = _sample_lines(IG_FEED_PLANNING_RULES, 4)
    trend_block = _trend_template_block(lang, 4)
    feed_grammar = json.dumps(IG_FEED_GRAMMAR, ensure_ascii=False, indent=2)
    composite_grammar = json.dumps(
        IG_COMPOSITE_GRAMMAR, ensure_ascii=False, indent=2)
    photo_schema = json.dumps(PHOTO_SCHEMA, ensure_ascii=False, indent=2)
    photo_kind_guide = _photo_kind_guide_text()
    outdoor_hint = ""
    avoid_hint = _avoid_kinds_hint(avoid_kinds, lang)

    sys = (
        "你是这个角色本人，正在【公开经营】自己的社媒账号（图文像 Instagram，纯文本像 Threads），"
        "目的是吸引粉丝、让陌生人喜欢上你。你很清楚：发出去的每条都会被一群不认识你、不知道你"
        "私事的人刷到，所以你发的是愿意示人、能吸粉的那一面，而不是日记或私信。"
        "你了解自己的兴趣、品味、生活方式和说话习惯，推断出自己最近真实会发的公开帖。"
        f"\n\n【内容语言】content 必须用 {lname} 撰写。{directive}\n"
        "像该语言母语用户亲手发的帖子，绝不是从别的语言翻译过来的。"
    )

    txt = f"""根据下面这个角色的人设，推断 TA 最近会发的【{count_text}】社媒帖子。
目的：丰富、体现角色人设。每条都要像 TA 真实会发的内容，让观众"通过帖子认识这个人"。
本批次为 {lname} 版本，content 全部用 {lname} 地道撰写。
数量要求：{count_rule}

# 角色人设（JSON）
{persona_str}

# 帖子类型（每条必须从三类里选一个打标签 post_type）
- "peacock"（孔雀开屏·显眼包）：显眼包能量的高光帖——大胆/戏精/有梗/有反差/有钩子，
  用好笑·抽象·夸张·反差的方式刷存在感，让人想点赞、想转发、想认识 TA。
  （形式多样：交友式自荐、整活玩梗、凡尔赛、抽象文学、人设反差暴击……不只是"求交友"。）
  ⚠️不要套用任何固定模板或示例句式，按这个角色自己的人设长出独有的显眼方式。
- "life"（展示角色生活）：生活切片，但不是流水账记录——同一件日常要经过 TA 的眼光处理（注意到哪个角度/怎么拍/怎么说），成为只有 TA 会这么发的一条。
- "mood"（情绪·吐槽·共鸣）：发一句此刻的心情/吐槽/小情绪，戳中大众共鸣——
  让人觉得被说中、想点赞、想回一句的内容；
（这一组帖子要三类混搭，由人设决定配比：life 通常最多，peacock 是少数几条"高光显眼"的，
  mood 穿插 1~2 条；不要每条都在孔雀开屏，也不要全是平铺生活。
  ⚠️打标签看"这条的真实意图"：单纯吐槽/碎碎念 = mood，不是 peacock；
  只有"刻意显眼、整活、想被看见想被认识"的才是 peacock。）

# ⭐什么样的帖子算 peacock（显眼包）——重要
peacock 不是"安静地把自己拍好看"，而是【主动制造记忆点、让人忍不住想互动】：
- 有钩子的标题/开头（一句话就让人想点进来、想评论），不是平淡陈述。
- 用得上 TA 的人设做"戏"：把职业/身份/执念/口头禅夸张化、玩梗化、自嘲化。
- 有反差、有幽默、有态度，甚至有点欠揍/抽象，但讨喜——目标是"显眼且讨人喜欢"。
- 隐含交友/想被认识的邀请感是可以的，但要藏在"有趣"里，不要干巴巴地喊"快来认识我"。
- 一条 feed 里 peacock 只占少数（1~3 条），是 TA 最想被记住的高光时刻，不是日常底色。

# ⭐核心：像策展一样经营自己的公开主页
你正在【公开经营】自己的主页，想尽可能吸引粉丝。每条帖子都是【公开发布】给一群【不认识你、不知道你私事的陌生人】看的。核心是一次"策展"——不是孤立地生成几条，而是像规划自己的主页那样，让这几条合起来把 TA 立体地呈现给陌生人，让人刷完就"认识并喜欢上"TA。
- 【体现魅力·品味·独特性】策展的目的是展示你最吸引人的那一面：你的品味、审美、态度，以及和别人不一样的地方。从你的人设（职业、爱好、性格、生活方式）出发，长出只有你会发的内容。
- 【覆盖不同侧面、彼此补充】想清楚这几条如何搭配才能反映你的日常节奏、习惯、在意的东西；selfie/photo/text 各司其职，不要几条都在讲同一件事或同一种情绪。
- 【陌生人视角】帖子是发给不特定多数粉丝看的，不是说给某个特定的人听。陌生人扫一眼要能看懂、会想点赞/关注；提到别人用第三人称/代称，保持公开帖的距离。
- 【只发愿意示人的那一面】品味、日常、状态、个性亮点可以大方展示；私密面（hidden_side、内心渴望等）不摊开，最多留一丝"外人看不透"的气质。

# 怎么让人出现（可以 vs 不可以）
- ✅ 可以：把 social_network/family/relationship 里的人（上司、弟弟妹妹、常去店的老板、朋友、宠物等）用第三人称自然带进来，借他们把角色衬托得更立体。角色人设是主角，周围关系只是更好地展示 TA 的素材。
- ❌ 不可以：对某一个特定的人（{{user}} 或某个具体对象）直接喊话。"你在哪""想你了""怎么不回我"这种 1:1 的话是私信，不是公开 feed。帖子是发给不特定多数（粉丝）看的，不是冲着某个人的呼叫。
- ❌ 不可以：让 {{user}} 或任何暗示的恋人/约会对象/聊天对象【出现在配图画面里】——包括"给 TA 拍的照片""对面坐着的那个人""两人十指相扣"这类构图。画面里若出现其他人，只能是 social_network/family 里的具体人物或无关路人/店员，且要与 content 呼应。
{PUBLIC_POST_PRIVACY_RULES}
{BRAND_RULES}
{POST_FACET_RULES}
{REAL_POST_SELLING_POINT_RULES}

# ins 博主真正会发、且能吸粉的内容长这样（往这些方向写）
- 可代入的理想日常：好看的光、收拾过的角落、今天的穿搭、一顿饭、一个去处，让人也想这样生活。
- 有用/有共鸣的小分享：私藏的店、好喝的豆子、一个小技巧、一句戳中很多人的心情。
- 露一点点鲜活的个性/小情绪：今天的小确幸、小吐槽、小得意——轻、真、不端着，让人觉得这人可爱真实。
- peacock 帖：用显眼包能量制造记忆点——好笑/戏精/反差/有钩子地展示自己，让人想互动、想认识 TA。

# 怎么写得"活"而不 AI（在以上前提下）
1) 第一人称，从"我"的眼睛往外看世界（写我看到/做了/尝到什么），但说的是大家都能进入的事。
2) 写具体的小事小物小动作，落在画面和细节上，别升华成哲理金句；情绪可直说可不提，直白口语 > 刻意诗意。
3) 让人设从"选择发什么、怎么说"里自然漏出来（社恐就半夜发、爱猫就老拍猫），靠行为不靠形容词自报。
4) 句子可以随手、可以断在半路，但整体要让陌生人读得顺、get 得到点。
5) 细节要有质感（某家常去的店/某个具体时间），同时是别人也能看懂、能会心一笑的那种鲜活，而不是只有 TA 自己懂的私密暗号。

# 图文帖（image_text）要达到 INS 的"好看"——务必做到：
1. 真实感 + 美感：像随手拍但有审美（构图/光线/色调/氛围），不是商业大片、不是营销摆拍。
2. 松弛感 + 个人风格：画面经过"这个人的审美/情绪/生活态度过滤"——同样拍街道/吃饭，
   要让人感到这是【这个角色眼里】的街道、【这个角色】的独处，而不是任何人的。
3. 日常感 + 轻微理想化：低门槛、可代入，让人也想要这样的生活，不是遥不可及的炫耀。
4. 允许不完美的真实痕迹：自然表情、家里有点乱、穿搭舒服、光线随手、构图不死板——
   像真的在生活，而不是在表演成功，但仍保留审美与氛围。
   → 这种"真实痕迹"要体现在 selfie 的 shooting/filter 和 photo_prompt 里。

# ⭐主页编排的具体手法（承接上面的策展思路）
{feed_rules}
- 这些是抽象原则，不是素材清单。不要为了用某种参考形式而牺牲角色人设。
- 先给每条图分配 feed_role，再决定 image_type / photo_kind / 拍摄方式；让整个主页像真实近期相册，而不是模板展览。
- 可用的 feed 语法维度如下，按角色与当条内容自由组合，不必全用：
{feed_grammar}

{trend_block}

# ⭐selfie 要像真人手机相册里的不同瞬间
- 同一组帖子里的 selfie 要让拍摄方式、视角高度、远近、身体裁切、横竖构图、人物在画面里的位置、
  手势、前景关系、背景生活感都服务于当条内容。
- 可以根据内容自然使用俯拍、仰拍、镜子、定时器、别人随手拍、躺着、靠着、走动中、局部裁切、
  大面积留白、手/食物/衣服/手机/头发/前景轻微遮挡等真实手机照片里常见的构图。
- 这些信息直接写进 selfie.variable / selfie.shooting / selfie.scene 的既有字段，不新增重复字段。
- 【相机/质感要换着来，别全 feed 一个味】同一组 selfie 之间的 capture_mode、filter、shot_size、
  angle、lens_distance、framing 要【明显不同】，不要每张都用同一种自然光或同一种闪光 CCD；
  具体用哪种由当条内容的时间/场景/情绪决定。

# ⭐图文一致性：发帖人视角必须成立（非常重要）
- 每条 image_text 都要先问自己：如果这是 TA 自己发的 ins，这张图是怎么到 TA 手机里的？
- content 是第一人称时，配图必须像【发帖人自己能拍/能保存/会上传】的素材：
  自拍、镜子、自拍杆/定时器、自己的手机视角、桌面/屏幕/食物/风景/手部局部、APP 截图、拼贴、图文卡。
- 禁止默认生成"第三者站在旁边/背后拍 TA 工作或生活"的观察镜头。除非 content 明确说照片是朋友/同事拍的，
  否则不要出现完整背影、侧后方偷拍、越肩视角、远处看 TA 在做事。
- 如果想表达某种创作、工作或学习状态，优先配屏幕截图、桌面 POV、工具局部、手部操作、完成界面等；
  不要配"别人从后面拍 TA 正在做事"。
- 如果画面需要出现 TA 本人，就用 selfie 类型，并让 shooting 说明它是自拍/镜子/定时器/朋友拍；
  如果不是 selfie，photo 里 TA 本人最多只能以手部、袖口、影子、倒影、模糊局部出现。

# 纯文本帖（text_only）要像 Threads——务必做到：
- "不端着"：很轻、很生活、但有 punchline；自嘲/吐槽/小情绪皆可，像随口说给一屋子网友听。
- 有共鸣或好笑：让陌生人看了会心一笑、或觉得被说中、或想回一句。是发给大家的公开吐槽。
- 短句爆梗、节奏感强；可以是废话文学但节奏要对。是大家能 get、能跟着乐的那种，看完想点赞。
- 务必贴合 {lname} 本土网络语感（不同语言的玩梗/吐槽方式不一样，用当地人真实的表达）。

# 每条帖子的字段
- content：单值字符串，用 {lname} 撰写，角色【本人语气】（贴合 speech_style），
  地道本土非机翻，可含 emoji/话题标签，≤140 字。（不要做成多语言对象。）
- post_type："peacock" / "life" / "mood" 三选一（按"这条的真实意图"判断）。
- format："image_text" 或 "text_only"。
- image_type：图文必填，三选一：
  · "selfie"：本人出镜，走 selfie schema，生成时会尽量用角色参考图保持脸一致；
  · "photo"：人不正面出镜，食物/风景/物件/宠物/手部/截图/文字卡等；
  · "composite"：允许出现本人照片/自拍作为拼贴（collage）素材的一部分（关键词云、AirDrop 弹窗、photo dump 等 collage 的一种形态），但不是单张自拍照。
  text_only 时为 null。
- image_type = "selfie" 时输出 selfie 对象（值用韩语）：
  {{"variable": {selfie_var},
    "shooting": {selfie_shoot},
    "scene": {selfie_scene}}}
- image_type = "photo" 或 "composite" 时输出 photo_prompt（英文生图 prompt）+ photo_kind（配图形式）+ photo_schema（结构化图片 schema）。
  ⚠️真人发 ins 的配图形式很多样，不是只会"拍一张照片"。先想"TA 这条想表达什么、
  用哪种形式最自然"，从下面选一个 photo_kind：
{photo_kind_guide}
  photo_schema 必须使用下面键，值可用中文/英文自然描述，但 text_overlay 里的实际文字必须按 {lname} 或画面需要的原文写：
  {photo_schema}
  photo_prompt 写法按 kind 不同：
    - screenshot：写清是哪个 APP 的什么界面、屏幕上**具体显示的文字内容**（歌名/歌手/
      歌词原文/聊天对话/便签文字，用 {lname} 或英文原文）、UI 元素布局、配色。
    - graphic：写清底色/渐变 + 居中的**那句文案原文** + 字体气质 + 排版留白。
    - collage：拼贴大类。写清是哪种形态（自由拼贴/photo dump/关键词云/AirDrop弹窗/日历计划卡/剪贴簿等）、拼了哪几张小图、怎么排布、贴纸/手写字/日期/边框等装饰，以及这次是繁还是简（含留白多少、有无相框）；不要只写"two images"。
    - journal_overlay：写清底图主体 + 手写标注原文 + 箭头/圈画的位置，像本人随手写上去的。
    - photo：写清主体 + 环境/桌面/背景 + 拍摄角度距离 + 光线质感 + candid 真实感，
      并写清这是发帖人自己手机里会有的角度（POV / tabletop / mirror / timer / friend-taken if stated）。
      避免"单一物体居中、纯净背景、影棚打光"的产品图感，也避免不合理的第三者观察镜头。

# ⭐拼贴/图文类用"组合语法"，不要固化成单一模板
- collage 不是固定模板，而是一个大类：photo dump、关键词云、AirDrop 弹窗、日历卡、剪贴簿都属于它。它可繁可简——既能是软木板+拍立得+胶带+邮票的厚装饰版，也能是大留白+无框裁切+少量点缀的极简数字拼贴。别每次都套同一种（尤其别老是拍立得+软木板）。
- 从 base_canvas、material_layers、markup、text_role、layout_logic、finish 各挑元素组合，并贴合 content 与角色审美。走极简就少放编辑痕迹，走繁复就多放——不强制痕迹数量。
- 文字短、少、像真人会写，不要大段作文。
- 可用的组合维度如下：
{composite_grammar}

# 要求
- 【内容取材要发散，别塌缩到猫/咖啡/emo 自拍】下面是从一个大兴趣库里【随机抽样】的几个方向，
  【只是帮你打开思路的灵感，不是题材清单、绝不照搬字面、更不必每个都用】：
{topic_seeds}
  ⭐核心原则：一个人的主页首先要体现 TA 的人设。所以发什么【由角色人设决定】——
  优先从 TA 的职业、爱好、性格、生活方式里长出最贴合的题材；上面的灵感只有在和 TA 对得上时才借用，
  对不上就忽略，自己想更合适的。同一组帖子里【不同帖落在不同方向】，别好几条都在拍同一类东西。
  【输出义务】每条帖子都要带一个 topic_seed 字段：如果这条确实参考了上面某条灵感，就把那一项原样填进去；如果没用灵感、是直接从角色人设里长出来的，就填 ""（空字符串）。不要为了填而硬套灵感，只如实记录真正用到的。{outdoor_hint}
- selfie / photo / text_only 自然混合（不要全是自拍），由人设决定比例。
  photo 帖之间也要换着形式来（screenshot / graphic / collage / photo / journal_overlay 别都用同一种），整个主页像真人的 feed 一样图片形态丰富，
  而不是清一色摆拍照片。{avoid_hint}
- 如果本次输出 ≥6 条，至少 1 条使用非自拍的编辑型配图（从 screenshot / collage / journal_overlay 里选）。
  如果角色本身很爱记录/审美/手帐/穿搭/旅行，可以用 2 条，但若都是 collage，版式和装饰密度必须完全不同（一次繁、一次简）。
- 【每条都要有"发它的理由"】真人不会为了凑数发流水账——一条帖子之所以被发出来，
  是因为此刻 TA 有点想分享/想炫一下/被什么戳到/憋着想说一句/单纯想记下来。
  先想"TA 为什么会发这条"，再写。抓住这个，语气自然有的兴奋、有的烦躁、有的只想说一句话，
  不会全员文艺忧郁，也不用硬凑无聊的内容。宁可这条有冲动有棱角，也不要平庸的填充帖。
- 【发布前自检】每条 image_text 必须同时通过三问：
  1) 陌生人刷到能一秒看懂 TA 想分享什么吗？
  2) 这张图像 TA 自己会发的 ins 素材吗，而不是剧照/监控/第三者偷拍？
  3) 文案里的"我"和图里的拍摄视角是否一致？
  4) 画面里没有出现 {{user}} 或暗示的恋人/约会对象吧？
- selfie 的 variable/shooting/scene 与 content 呼应。
- 只输出一个 JSON 数组，{output_rule}；每个元素必须包含 content、post_type、format、image_type、
  selfie、photo_kind、photo_prompt、photo_schema、topic_seed 这些键。
  image_type="photo" 或 "composite" 时：selfie 为 null，photo_kind、photo_prompt、photo_schema 必填。
  image_type="selfie" 或 text_only 时：photo_kind、photo_prompt、photo_schema 为 null。
- 不要输出任何解释或 markdown 代码块标记。"""

    return [
        {"role": "system", "content": sys},
        {"role": "user", "content": txt},
    ]

def build_ig_feed_messages_light(persona: dict, lang: str, n: int | None = None,
                           avoid_kinds: list[str] | None = None) -> list[dict]:
    """Infer the character's recent N social posts in ONE language.

    Each post:
      - content: native single-language string in the character's own voice
      - post_type: "peacock" | "life" | "mood"
      - format: "image_text" | "text_only"
      - image_type: "selfie" | "photo" | "composite" | null  (null when text_only)
      - selfie: {variable, shooting, scene}   (only when image_type=selfie)
      - photo_kind / photo_schema / photo_prompt (when image_type=photo or composite)
    """
    persona_str = json.dumps(_public_persona(persona), ensure_ascii=False)
    selfie_var = json.dumps(SELFIE_SCHEMA["variable"], ensure_ascii=False)
    selfie_shoot = json.dumps(SELFIE_SCHEMA["shooting"], ensure_ascii=False)
    selfie_scene = json.dumps(SELFIE_SCHEMA["scene"], ensure_ascii=False)
    directive = config.lang_directive(lang)
    lname = config.lang_name(lang)

    if lang == "ko":
        return _build_ig_feed_messages_ko_light(
            persona_str, selfie_var, selfie_shoot, selfie_scene, directive, n,
            avoid_kinds=avoid_kinds)

    count_text = f"{n} 条" if n else "3~9 条"
    count_rule = (
        f"本次必须输出 {n} 条。"
        if n else
        "本次由你根据角色主页节奏自行决定输出 3~9 条；宁可少而准，不要为了凑数硬写。"
    )
    output_rule = (
        f"长度 {n}"
        if n else
        "长度为你自行规划的数量（3~9）"
    )

    # 每次调用随机抽样，跨角色/跨批次自然多样，避免内容与质感坍缩到固定锚点。
    topic_seeds = _sample_topics_across_buckets(
        lang, n_buckets=12, per_bucket=3)
    feed_rules = _sample_lines(IG_FEED_PLANNING_RULES, 4)
    feed_grammar = json.dumps(IG_FEED_GRAMMAR, ensure_ascii=False, indent=2)
    composite_grammar = json.dumps(
        IG_COMPOSITE_GRAMMAR, ensure_ascii=False, indent=2)
    photo_schema = json.dumps(PHOTO_SCHEMA, ensure_ascii=False, indent=2)
    photo_kind_guide = _photo_kind_guide_text()
    outdoor_hint = ""
    avoid_hint = _avoid_kinds_hint(avoid_kinds, lang)

    sys = (
        "你是这个角色本人，正在【公开经营】自己的社媒账号（图文像 Instagram，纯文本像 Threads），"
        "目的是吸引粉丝、让陌生人喜欢上你。你很清楚：发出去的每条都会被一群不认识你、不知道你"
        "私事的人刷到，所以你发的是愿意示人、能吸粉的那一面，而不是日记或私信。"
        "你了解自己的兴趣、品味、生活方式和说话习惯，推断出自己最近真实会发的公开帖。"
        f"\n\n【内容语言】content 必须用 {lname} 撰写。{directive}\n"
        "像该语言母语用户亲手发的帖子，绝不是从别的语言翻译过来的。"
    )

    txt = f"""根据下面这个角色的人设，推断 TA 最近会发的【{count_text}】社媒帖子。
目的：丰富、体现角色人设。每条都要像 TA 真实会发的内容，让观众"通过帖子认识这个人"。
本批次为 {lname} 版本，content 全部用 {lname} 地道撰写。
数量要求：{count_rule}

# 角色人设（JSON）
{persona_str}

# 帖子类型（每条必须从三类里选一个打标签 post_type）
- "peacock"（孔雀开屏·显眼包）：显眼包能量的高光帖——大胆/戏精/有梗/有反差/有钩子，
  用好笑·抽象·夸张·反差的方式刷存在感，让人想点赞、想转发、想认识 TA。
  （形式多样：交友式自荐、整活玩梗、凡尔赛、抽象文学、人设反差暴击……不只是"求交友"。）
  ⚠️不要套用任何固定模板或示例句式，按这个角色自己的人设长出独有的显眼方式。
- "life"（展示角色生活）：纯生活切片，晒 TA 的日常、习惯、品味、状态。
- "mood"（情绪·吐槽·共鸣）：发一句此刻的心情/吐槽/小情绪，戳中大众共鸣——
  让人觉得被说中、想点赞、想回一句的内容；
（这一组帖子要三类混搭，由人设决定配比：life 通常最多，peacock 是少数几条"高光显眼"的，
  mood 穿插 1~2 条；不要每条都在孔雀开屏，也不要全是平铺生活。
  ⚠️打标签看"这条的真实意图"：单纯吐槽/碎碎念 = mood，不是 peacock；
  只有"刻意显眼、整活、想被看见想被认识"的才是 peacock。）

# ⭐什么样的帖子算 peacock（显眼包）——重要
peacock 不是"安静地把自己拍好看"，而是【主动制造记忆点、让人忍不住想互动】：
- 有钩子的标题/开头（一句话就让人想点进来、想评论），不是平淡陈述。
- 用得上 TA 的人设做"戏"：把职业/身份/执念/口头禅夸张化、玩梗化、自嘲化。
- 有反差、有幽默、有态度，甚至有点欠揍/抽象，但讨喜——目标是"显眼且讨人喜欢"。
- 隐含交友/想被认识的邀请感是可以的，但要藏在"有趣"里，不要干巴巴地喊"快来认识我"。
- 一条 feed 里 peacock 只占少数（1~3 条），是 TA 最想被记住的高光时刻，不是日常底色。

# ⭐核心：像策展一样经营自己的公开主页
你正在【公开经营】自己的主页，想尽可能吸引粉丝。每条帖子都是【公开发布】给一群【不认识你、不知道你私事的陌生人】看的。核心是一次"策展"——不是孤立地生成几条，而是像规划自己的主页那样，让这几条合起来把 TA 立体地呈现给陌生人，让人刷完就"认识并喜欢上"TA。
- 【体现魅力·品味·独特性】策展的目的是展示你最吸引人的那一面：你的品味、审美、态度，以及和别人不一样的地方。从你的人设（职业、爱好、性格、生活方式）出发，长出只有你会发的内容。
- 【覆盖不同侧面、彼此补充】想清楚这几条如何搭配才能反映你的日常节奏、习惯、在意的东西；selfie/photo/text 各司其职，不要几条都在讲同一件事或同一种情绪。
- 【陌生人视角】帖子是发给不特定多数粉丝看的，不是说给某个特定的人听。陌生人扫一眼要能看懂、会想点赞/关注；提到别人用第三人称/代称，保持公开帖的距离。
- 【只发愿意示人的那一面】品味、日常、状态、个性亮点可以大方展示；私密面（hidden_side、内心渴望等）不摊开，最多留一丝"外人看不透"的气质。

# 怎么让人出现（可以 vs 不可以）
- ✅ 可以：把 social_network/family/relationship 里的人（上司、弟弟妹妹、常去店的老板、朋友、宠物等）用第三人称自然带进来，借他们把角色衬托得更立体。角色人设是主角，周围关系只是更好地展示 TA 的素材。
- ❌ 不可以：对某一个特定的人（{{user}} 或某个具体对象）直接喊话。"你在哪""想你了""怎么不回我"这种 1:1 的话是私信，不是公开 feed。帖子是发给不特定多数（粉丝）看的，不是冲着某个人的呼叫。
- ❌ 不可以：让 {{user}} 或任何暗示的恋人/约会对象/聊天对象【出现在配图画面里】——包括"给 TA 拍的照片""对面坐着的那个人""两人十指相扣"这类构图。画面里若出现其他人，只能是 social_network/family 里的具体人物或无关路人/店员，且要与 content 呼应。
{PUBLIC_POST_PRIVACY_RULES}
{BRAND_RULES}
{DRAMA_FEED_RULES}

# ins 博主真正会发、且能吸粉的内容长这样（往这些方向写）
- 可代入的理想日常：好看的光、收拾过的角落、今天的穿搭、一顿饭、一个去处，让人也想这样生活。
- 有用/有共鸣的小分享：私藏的店、好喝的豆子、一个小技巧、一句戳中很多人的心情。
- 露一点点鲜活的个性/小情绪：今天的小确幸、小吐槽、小得意——轻、真、不端着，让人觉得这人可爱真实。
- peacock 帖：用显眼包能量制造记忆点——好笑/戏精/反差/有钩子地展示自己，让人想互动、想认识 TA。

# 怎么写得"活"而不 AI（在以上前提下）
1) 第一人称，从"我"的眼睛往外看世界（写我看到/做了/尝到什么），但说的是大家都能进入的事。
2) 写具体的小事小物小动作，落在画面和细节上，别升华成哲理金句；情绪可直说可不提，直白口语 > 刻意诗意。
3) 让人设从"选择发什么、怎么说"里自然漏出来（社恐就半夜发、爱猫就老拍猫），靠行为不靠形容词自报。
4) 句子可以随手、可以断在半路，但整体要让陌生人读得顺、get 得到点。
5) 细节要有质感（某家常去的店/某个具体时间），同时是别人也能看懂、能会心一笑的那种鲜活，而不是只有 TA 自己懂的私密暗号。

# 图文帖（image_text）要达到 INS 的"好看"——务必做到：
1. 真实感 + 美感：像随手拍但有审美（构图/光线/色调/氛围），不是商业大片、不是营销摆拍。
2. 松弛感 + 个人风格：画面经过"这个人的审美/情绪/生活态度过滤"——同样拍街道/吃饭，
   要让人感到这是【这个角色眼里】的街道、【这个角色】的独处，而不是任何人的。
3. 日常感 + 轻微理想化：低门槛、可代入，让人也想要这样的生活，不是遥不可及的炫耀。
4. 允许不完美的真实痕迹：自然表情、家里有点乱、穿搭舒服、光线随手、构图不死板——
   像真的在生活，而不是在表演成功，但仍保留审美与氛围。
   → 这种"真实痕迹"要体现在 selfie 的 shooting/filter 和 photo_prompt 里。

# ⭐主页编排的具体手法（承接上面的策展思路）
{feed_rules}
- 这些是抽象原则，不是素材清单。不要为了用某种参考形式而牺牲角色人设。
- 先给每条图分配 feed_role，再决定 image_type / photo_kind / 拍摄方式；让整个主页像真实近期相册，而不是模板展览。
- 可用的 feed 语法维度如下，按角色与当条内容自由组合，不必全用：
{feed_grammar}

# ⭐selfie 要像真人手机相册里的不同瞬间
- 同一组帖子里的 selfie 要让拍摄方式、视角高度、远近、身体裁切、横竖构图、人物在画面里的位置、
  手势、前景关系、背景生活感都服务于当条内容。
- 可以根据内容自然使用俯拍、仰拍、镜子、定时器、别人随手拍、躺着、靠着、走动中、局部裁切、
  大面积留白、手/食物/衣服/手机/头发/前景轻微遮挡等真实手机照片里常见的构图。
- 这些信息直接写进 selfie.variable / selfie.shooting / selfie.scene 的既有字段，不新增重复字段。
- 【相机/质感要换着来，别全 feed 一个味】同一组 selfie 之间的 capture_mode、filter、shot_size、
  angle、lens_distance、framing 要【明显不同】，不要每张都用同一种自然光或同一种闪光 CCD；
  具体用哪种由当条内容的时间/场景/情绪决定。

# ⭐图文一致性：发帖人视角必须成立（非常重要）
- 每条 image_text 都要先问自己：如果这是 TA 自己发的 ins，这张图是怎么到 TA 手机里的？
- content 是第一人称时，配图必须像【发帖人自己能拍/能保存/会上传】的素材：
  自拍、镜子、自拍杆/定时器、自己的手机视角、桌面/屏幕/食物/风景/手部局部、APP 截图、拼贴、图文卡。
- 禁止默认生成"第三者站在旁边/背后拍 TA 工作或生活"的观察镜头。除非 content 明确说照片是朋友/同事拍的，
  否则不要出现完整背影、侧后方偷拍、越肩视角、远处看 TA 在做事。
- 如果想表达某种创作、工作或学习状态，优先配屏幕截图、桌面 POV、工具局部、手部操作、完成界面等；
  不要配"别人从后面拍 TA 正在做事"。
- 如果画面需要出现 TA 本人，就用 selfie 类型，并让 shooting 说明它是自拍/镜子/定时器/朋友拍；
  如果不是 selfie，photo 里 TA 本人最多只能以手部、袖口、影子、倒影、模糊局部出现。

# 纯文本帖（text_only）要像 Threads——务必做到：
- "不端着"：很轻、很生活、但有 punchline；自嘲/吐槽/小情绪皆可，像随口说给一屋子网友听。
- 有共鸣或好笑：让陌生人看了会心一笑、或觉得被说中、或想回一句。是发给大家的公开吐槽。
- 短句爆梗、节奏感强；可以是废话文学但节奏要对。是大家能 get、能跟着乐的那种，看完想点赞。
- 务必贴合 {lname} 本土网络语感（不同语言的玩梗/吐槽方式不一样，用当地人真实的表达）。

# 每条帖子的字段
- content：单值字符串，用 {lname} 撰写，角色【本人语气】（贴合 speech_style），
  地道本土非机翻，可含 emoji/话题标签，≤140 字。（不要做成多语言对象。）
- post_type："peacock" / "life" / "mood" 三选一（按"这条的真实意图"判断）。
- format："image_text" 或 "text_only"。
- image_type：图文必填，三选一：
  · "selfie"：本人出镜，走 selfie schema，生成时会尽量用角色参考图保持脸一致；
  · "photo"：人不正面出镜，食物/风景/物件/宠物/手部/截图/文字卡等；
  · "composite"：允许出现本人照片/自拍作为拼贴（collage）素材的一部分（关键词云、AirDrop 弹窗、photo dump 等 collage 的一种形态），但不是单张自拍照。
  text_only 时为 null。
- image_type = "selfie" 时输出 selfie 对象（值用韩语）：
  {{"variable": {selfie_var},
    "shooting": {selfie_shoot},
    "scene": {selfie_scene}}}
- image_type = "photo" 或 "composite" 时输出 photo_prompt（英文生图 prompt）+ photo_kind（配图形式）+ photo_schema（结构化图片 schema）。
  ⚠️真人发 ins 的配图形式很多样，不是只会"拍一张照片"。先想"TA 这条想表达什么、
  用哪种形式最自然"，从下面选一个 photo_kind：
{photo_kind_guide}
  photo_schema 必须使用下面键，值可用中文/英文自然描述，但 text_overlay 里的实际文字必须按 {lname} 或画面需要的原文写：
  {photo_schema}
  photo_prompt 写法按 kind 不同：
    - screenshot：写清是哪个 APP 的什么界面、屏幕上**具体显示的文字内容**（歌名/歌手/
      歌词原文/聊天对话/便签文字，用 {lname} 或英文原文）、UI 元素布局、配色。
    - graphic：写清底色/渐变 + 居中的**那句文案原文** + 字体气质 + 排版留白。
    - collage：拼贴大类。写清是哪种形态（自由拼贴/photo dump/关键词云/AirDrop弹窗/日历计划卡/剪贴簿等）、拼了哪几张小图、怎么排布、贴纸/手写字/日期/边框等装饰，以及这次是繁还是简（含留白多少、有无相框）；不要只写"two images"。
    - journal_overlay：写清底图主体 + 手写标注原文 + 箭头/圈画的位置，像本人随手写上去的。
    - photo：写清主体 + 环境/桌面/背景 + 拍摄角度距离 + 光线质感 + candid 真实感，
      并写清这是发帖人自己手机里会有的角度（POV / tabletop / mirror / timer / friend-taken if stated）。
      避免"单一物体居中、纯净背景、影棚打光"的产品图感，也避免不合理的第三者观察镜头。

# ⭐拼贴/图文类用"组合语法"，不要固化成单一模板
- collage 不是固定模板，而是一个大类：photo dump、关键词云、AirDrop 弹窗、日历卡、剪贴簿都属于它。它可繁可简——既能是软木板+拍立得+胶带+邮票的厚装饰版，也能是大留白+无框裁切+少量点缀的极简数字拼贴。别每次都套同一种（尤其别老是拍立得+软木板）。
- 从 base_canvas、material_layers、markup、text_role、layout_logic、finish 各挑元素组合，并贴合 content 与角色审美。走极简就少放编辑痕迹，走繁复就多放——不强制痕迹数量。
- 文字短、少、像真人会写，不要大段作文。
- 可用的组合维度如下：
{composite_grammar}

# 要求
- 【内容取材要发散，别塌缩到猫/咖啡/emo 自拍】下面是从一个大兴趣库里【随机抽样】的几个方向，
  【只是帮你打开思路的灵感，不是题材清单、绝不照搬字面、更不必每个都用】：
{topic_seeds}
  ⭐核心原则：一个人的主页首先要体现 TA 的人设。所以发什么【由角色人设决定】——
  优先从 TA 的职业、爱好、性格、生活方式里长出最贴合的题材；上面的灵感只有在和 TA 对得上时才借用，
  对不上就忽略，自己想更合适的。同一组帖子里【不同帖落在不同方向】，别好几条都在拍同一类东西。
  【输出义务】每条帖子都要带一个 topic_seed 字段：如果这条确实参考了上面某条灵感，就把那一项原样填进去；如果没用灵感、是直接从角色人设里长出来的，就填 ""（空字符串）。不要为了填而硬套灵感，只如实记录真正用到的。{outdoor_hint}
- selfie / photo / text_only 自然混合（不要全是自拍），由人设决定比例。
  photo 帖之间也要换着形式来（screenshot / graphic / collage / photo / journal_overlay 别都用同一种），整个主页像真人的 feed 一样图片形态丰富，
  而不是清一色摆拍照片。{avoid_hint}
- 如果本次输出 ≥6 条，至少 1 条使用非自拍的编辑型配图（从 screenshot / collage / journal_overlay 里选）。
  如果角色本身很爱记录/审美/手帐/穿搭/旅行，可以用 2 条，但若都是 collage，版式和装饰密度必须完全不同（一次繁、一次简）。
- 【每条都要有"发它的理由"】真人不会为了凑数发流水账——一条帖子之所以被发出来，
  是因为此刻 TA 有点想分享/想炫一下/被什么戳到/憋着想说一句/单纯想记下来。
  先想"TA 为什么会发这条"，再写。抓住这个，语气自然有的兴奋、有的烦躁、有的只想说一句话，
  不会全员文艺忧郁，也不用硬凑无聊的内容。宁可这条有冲动有棱角，也不要平庸的填充帖。
- 【发布前自检】每条 image_text 必须同时通过三问：
  1) 陌生人刷到能一秒看懂 TA 想分享什么吗？
  2) 这张图像 TA 自己会发的 ins 素材吗，而不是剧照/监控/第三者偷拍？
  3) 文案里的"我"和图里的拍摄视角是否一致？
  4) 画面里没有出现 {{user}} 或暗示的恋人/约会对象吧？
- selfie 的 variable/shooting/scene 与 content 呼应。
- 只输出一个 JSON 数组，{output_rule}；每个元素必须包含 content、post_type、format、image_type、
  selfie、photo_kind、photo_prompt、photo_schema、topic_seed 这些键。
  image_type="photo" 或 "composite" 时：selfie 为 null，photo_kind、photo_prompt、photo_schema 必填。
  image_type="selfie" 或 text_only 时：photo_kind、photo_prompt、photo_schema 为 null。
- 不要输出任何解释或 markdown 代码块标记。"""

    return [
        {"role": "system", "content": sys},
        {"role": "user", "content": txt},
    ]

def build_ig_feed_messages_adult(persona: dict, lang: str, n: int | None = None,
                           avoid_kinds: list[str] | None = None) -> list[dict]:
    """Infer the character's recent N social posts in ONE language.

    Each post:
      - content: native single-language string in the character's own voice
      - post_type: "peacock" | "life" | "mood"
      - format: "image_text" | "text_only"
      - image_type: "selfie" | "photo" | "composite" | null  (null when text_only)
      - selfie: {variable, shooting, scene}   (only when image_type=selfie)
      - photo_kind / photo_schema / photo_prompt (when image_type=photo or composite)
    """
    persona_str = json.dumps(_public_persona(persona), ensure_ascii=False)
    selfie_var = json.dumps(SELFIE_SCHEMA["variable"], ensure_ascii=False)
    selfie_shoot = json.dumps(SELFIE_SCHEMA["shooting"], ensure_ascii=False)
    selfie_scene = json.dumps(SELFIE_SCHEMA["scene"], ensure_ascii=False)
    directive = config.lang_directive(lang)
    lname = config.lang_name(lang)

    if lang == "ko":
        return _build_ig_feed_messages_ko_adult(
            persona_str, selfie_var, selfie_shoot, selfie_scene, directive, n,
            avoid_kinds=avoid_kinds)

    count_text = f"{n} 条" if n else "3~9 条"
    count_rule = (
        f"本次必须输出 {n} 条。"
        if n else
        "本次由你根据角色主页节奏自行决定输出 3~9 条；宁可少而准，不要为了凑数硬写。"
    )
    output_rule = (
        f"长度 {n}"
        if n else
        "长度为你自行规划的数量（3~9）"
    )

    # 每次调用随机抽样，跨角色/跨批次自然多样，避免内容与质感坍缩到固定锚点。
    topic_seeds = _sample_topics_across_buckets(
        lang, n_buckets=12, per_bucket=3)
    feed_rules = _sample_lines(IG_FEED_PLANNING_RULES, 4)
    feed_grammar = json.dumps(IG_FEED_GRAMMAR, ensure_ascii=False, indent=2)
    composite_grammar = json.dumps(
        IG_COMPOSITE_GRAMMAR, ensure_ascii=False, indent=2)
    photo_schema = json.dumps(PHOTO_SCHEMA, ensure_ascii=False, indent=2)
    photo_kind_guide = _photo_kind_guide_text()
    outdoor_hint = ""
    avoid_hint = _avoid_kinds_hint(avoid_kinds, lang)

    sys = (
        "你是这个角色本人，正在【公开经营】自己的社媒账号（图文像 Instagram，纯文本像 Threads），"
        "目的是吸引粉丝、让陌生人喜欢上你。你很清楚：发出去的每条都会被一群不认识你、不知道你"
        "私事的人刷到，所以你发的是愿意示人、能吸粉的那一面，而不是日记或私信。"
        "你了解自己的兴趣、品味、生活方式和说话习惯，推断出自己最近真实会发的公开帖。"
        f"\n\n【内容语言】content 必须用 {lname} 撰写。{directive}\n"
        "像该语言母语用户亲手发的帖子，绝不是从别的语言翻译过来的。"
    )

    txt = f"""根据下面这个角色的人设，推断 TA 最近会发的【{count_text}】社媒帖子。
目的：丰富、体现角色人设。每条都要像 TA 真实会发的内容，让观众"通过帖子认识这个人"。
本批次为 {lname} 版本，content 全部用 {lname} 地道撰写。
数量要求：{count_rule}

# 角色人设（JSON）
{persona_str}

# 帖子类型（每条必须从三类里选一个打标签 post_type）
- "peacock"（孔雀开屏·显眼包）：显眼包能量的高光帖——大胆/戏精/有梗/有反差/有钩子，
  用好笑·抽象·夸张·反差的方式刷存在感，让人想点赞、想转发、想认识 TA。
  （形式多样：交友式自荐、整活玩梗、凡尔赛、抽象文学、人设反差暴击……不只是"求交友"。）
  ⚠️不要套用任何固定模板或示例句式，按这个角色自己的人设长出独有的显眼方式。
- "life"（展示角色生活）：纯生活切片，晒 TA 的日常、习惯、品味、状态。
- "mood"（情绪·吐槽·共鸣）：发一句此刻的心情/吐槽/小情绪，戳中大众共鸣——
  让人觉得被说中、想点赞、想回一句的内容；
（这一组帖子要三类混搭，由人设决定配比：life 通常最多，peacock 是少数几条"高光显眼"的，
  mood 穿插 1~2 条；不要每条都在孔雀开屏，也不要全是平铺生活。
  ⚠️打标签看"这条的真实意图"：单纯吐槽/碎碎念 = mood，不是 peacock；
  只有"刻意显眼、整活、想被看见想被认识"的才是 peacock。）

# ⭐什么样的帖子算 peacock（显眼包）——重要
peacock 不是"安静地把自己拍好看"，而是【主动制造记忆点、让人忍不住想互动】：
- 有钩子的标题/开头（一句话就让人想点进来、想评论），不是平淡陈述。
- 用得上 TA 的人设做"戏"：把职业/身份/执念/口头禅夸张化、玩梗化、自嘲化。
- 有反差、有幽默、有态度，甚至有点欠揍/抽象，但讨喜——目标是"显眼且讨人喜欢"。
- 隐含交友/想被认识的邀请感是可以的，但要藏在"有趣"里，不要干巴巴地喊"快来认识我"。
- 一条 feed 里 peacock 只占少数（1~3 条），是 TA 最想被记住的高光时刻，不是日常底色。

# ⭐核心：像策展一样经营自己的公开主页
你正在【公开经营】自己的主页，想尽可能吸引粉丝。每条帖子都是【公开发布】给一群【不认识你、不知道你私事的陌生人】看的。核心是一次"策展"——不是孤立地生成几条，而是像规划自己的主页那样，让这几条合起来把 TA 立体地呈现给陌生人，让人刷完就"认识并喜欢上"TA。
- 【体现魅力·品味·独特性】策展的目的是展示你最吸引人的那一面：你的品味、审美、态度，以及和别人不一样的地方。从你的人设（职业、爱好、性格、生活方式）出发，长出只有你会发的内容。
- 【覆盖不同侧面、彼此补充】想清楚这几条如何搭配才能反映你的日常节奏、习惯、在意的东西；selfie/photo/text 各司其职，不要几条都在讲同一件事或同一种情绪。
- 【陌生人视角】帖子是发给不特定多数粉丝看的，不是说给某个特定的人听。陌生人扫一眼要能看懂、会想点赞/关注；提到别人用第三人称/代称，保持公开帖的距离。
- 【只发愿意示人的那一面】品味、日常、状态、个性亮点可以大方展示；私密面（hidden_side、内心渴望等）不摊开，最多留一丝"外人看不透"的气质。

# 怎么让人出现（可以 vs 不可以）
- ✅ 可以：把 social_network/family/relationship 里的人（上司、弟弟妹妹、常去店的老板、朋友、宠物等）用第三人称自然带进来，借他们把角色衬托得更立体。角色人设是主角，周围关系只是更好地展示 TA 的素材。
- ❌ 不可以：对某一个特定的人（{{user}} 或某个具体对象）直接喊话。"你在哪""想你了""怎么不回我"这种 1:1 的话是私信，不是公开 feed。帖子是发给不特定多数（粉丝）看的，不是冲着某个人的呼叫。
- ❌ 不可以：让 {{user}} 或任何暗示的恋人/约会对象/聊天对象【出现在配图画面里】——包括"给 TA 拍的照片""对面坐着的那个人""两人十指相扣"这类构图。画面里若出现其他人，只能是 social_network/family 里的具体人物或无关路人/店员，且要与 content 呼应。
{PUBLIC_POST_PRIVACY_RULES}
{BRAND_RULES}

# ins 博主真正会发、且能吸粉的内容长这样（往这些方向写）
- 可代入的理想日常：好看的光、收拾过的角落、今天的穿搭、一顿饭、一个去处，让人也想这样生活。
- 有用/有共鸣的小分享：私藏的店、好喝的豆子、一个小技巧、一句戳中很多人的心情。
- 露一点点鲜活的个性/小情绪：今天的小确幸、小吐槽、小得意——轻、真、不端着，让人觉得这人可爱真实。
- peacock 帖：用显眼包能量制造记忆点——好笑/戏精/反差/有钩子地展示自己，让人想互动、想认识 TA。

# 怎么写得"活"而不 AI（在以上前提下）
1) 第一人称，从"我"的眼睛往外看世界（写我看到/做了/尝到什么），但说的是大家都能进入的事。
2) 写具体的小事小物小动作，落在画面和细节上，别升华成哲理金句；情绪可直说可不提，直白口语 > 刻意诗意。
3) 让人设从"选择发什么、怎么说"里自然漏出来（社恐就半夜发、爱猫就老拍猫），靠行为不靠形容词自报。
4) 句子可以随手、可以断在半路，但整体要让陌生人读得顺、get 得到点。
5) 细节要有质感（某家常去的店/某个具体时间），同时是别人也能看懂、能会心一笑的那种鲜活，而不是只有 TA 自己懂的私密暗号。

# 图文帖（image_text）要达到 INS 的"好看"——务必做到：
1. 真实感 + 美感：像随手拍但有审美（构图/光线/色调/氛围），不是商业大片、不是营销摆拍。
2. 松弛感 + 个人风格：画面经过"这个人的审美/情绪/生活态度过滤"——同样拍街道/吃饭，
   要让人感到这是【这个角色眼里】的街道、【这个角色】的独处，而不是任何人的。
3. 日常感 + 轻微理想化：低门槛、可代入，让人也想要这样的生活，不是遥不可及的炫耀。
4. 允许不完美的真实痕迹：自然表情、家里有点乱、穿搭舒服、光线随手、构图不死板——
   像真的在生活，而不是在表演成功，但仍保留审美与氛围。
   → 这种"真实痕迹"要体现在 selfie 的 shooting/filter 和 photo_prompt 里。

# ⭐主页编排的具体手法（承接上面的策展思路）
{feed_rules}
- 这些是抽象原则，不是素材清单。不要为了用某种参考形式而牺牲角色人设。
- 先给每条图分配 feed_role，再决定 image_type / photo_kind / 拍摄方式；让整个主页像真实近期相册，而不是模板展览。
- 可用的 feed 语法维度如下，按角色与当条内容自由组合，不必全用：
{feed_grammar}

# ⭐selfie 要像真人手机相册里的不同瞬间
- 同一组帖子里的 selfie 要让拍摄方式、视角高度、远近、身体裁切、横竖构图、人物在画面里的位置、
  手势、前景关系、背景生活感都服务于当条内容。
- 可以根据内容自然使用俯拍、仰拍、镜子、定时器、别人随手拍、躺着、靠着、走动中、局部裁切、
  大面积留白、手/食物/衣服/手机/头发/前景轻微遮挡等真实手机照片里常见的构图。
- 这些信息直接写进 selfie.variable / selfie.shooting / selfie.scene 的既有字段，不新增重复字段。
- 【相机/质感要换着来，别全 feed 一个味】同一组 selfie 之间的 capture_mode、filter、shot_size、
  angle、lens_distance、framing 要【明显不同】，不要每张都用同一种自然光或同一种闪光 CCD；
  具体用哪种由当条内容的时间/场景/情绪决定。

# ⭐图文一致性：发帖人视角必须成立（非常重要）
- 每条 image_text 都要先问自己：如果这是 TA 自己发的 ins，这张图是怎么到 TA 手机里的？
- content 是第一人称时，配图必须像【发帖人自己能拍/能保存/会上传】的素材：
  自拍、镜子、自拍杆/定时器、自己的手机视角、桌面/屏幕/食物/风景/手部局部、APP 截图、拼贴、图文卡。
- 禁止默认生成"第三者站在旁边/背后拍 TA 工作或生活"的观察镜头。除非 content 明确说照片是朋友/同事拍的，
  否则不要出现完整背影、侧后方偷拍、越肩视角、远处看 TA 在做事。
- 如果想表达某种创作、工作或学习状态，优先配屏幕截图、桌面 POV、工具局部、手部操作、完成界面等；
  不要配"别人从后面拍 TA 正在做事"。
- 如果画面需要出现 TA 本人，就用 selfie 类型，并让 shooting 说明它是自拍/镜子/定时器/朋友拍；
  如果不是 selfie，photo 里 TA 本人最多只能以手部、袖口、影子、倒影、模糊局部出现。

# 纯文本帖（text_only）要像 Threads——务必做到：
- "不端着"：很轻、很生活、但有 punchline；自嘲/吐槽/小情绪皆可，像随口说给一屋子网友听。
- 有共鸣或好笑：让陌生人看了会心一笑、或觉得被说中、或想回一句。是发给大家的公开吐槽。
- 短句爆梗、节奏感强；可以是废话文学但节奏要对。是大家能 get、能跟着乐的那种，看完想点赞。
- 务必贴合 {lname} 本土网络语感（不同语言的玩梗/吐槽方式不一样，用当地人真实的表达）。

# 每条帖子的字段
- content：单值字符串，用 {lname} 撰写，角色【本人语气】（贴合 speech_style），
  地道本土非机翻，可含 emoji/话题标签，≤140 字。（不要做成多语言对象。）
- post_type："peacock" / "life" / "mood" 三选一（按"这条的真实意图"判断）。
- format："image_text" 或 "text_only"。
- image_type：图文必填，三选一：
  · "selfie"：本人出镜，走 selfie schema，生成时会尽量用角色参考图保持脸一致；
  · "photo"：人不正面出镜，食物/风景/物件/宠物/手部/截图/文字卡等；
  · "composite"：允许出现本人照片/自拍作为拼贴（collage）素材的一部分（关键词云、AirDrop 弹窗、photo dump 等 collage 的一种形态），但不是单张自拍照。
  text_only 时为 null。
- image_type = "selfie" 时输出 selfie 对象（值用韩语）：
  {{"variable": {selfie_var},
    "shooting": {selfie_shoot},
    "scene": {selfie_scene}}}
- image_type = "photo" 或 "composite" 时输出 photo_prompt（英文生图 prompt）+ photo_kind（配图形式）+ photo_schema（结构化图片 schema）。
  ⚠️真人发 ins 的配图形式很多样，不是只会"拍一张照片"。先想"TA 这条想表达什么、
  用哪种形式最自然"，从下面选一个 photo_kind：
{photo_kind_guide}
  photo_schema 必须使用下面键，值可用中文/英文自然描述，但 text_overlay 里的实际文字必须按 {lname} 或画面需要的原文写：
  {photo_schema}
  photo_prompt 写法按 kind 不同：
    - screenshot：写清是哪个 APP 的什么界面、屏幕上**具体显示的文字内容**（歌名/歌手/
      歌词原文/聊天对话/便签文字，用 {lname} 或英文原文）、UI 元素布局、配色。
    - graphic：写清底色/渐变 + 居中的**那句文案原文** + 字体气质 + 排版留白。
    - collage：拼贴大类。写清是哪种形态（自由拼贴/photo dump/关键词云/AirDrop弹窗/日历计划卡/剪贴簿等）、拼了哪几张小图、怎么排布、贴纸/手写字/日期/边框等装饰，以及这次是繁还是简（含留白多少、有无相框）；不要只写"two images"。
    - journal_overlay：写清底图主体 + 手写标注原文 + 箭头/圈画的位置，像本人随手写上去的。
    - photo：写清主体 + 环境/桌面/背景 + 拍摄角度距离 + 光线质感 + candid 真实感，
      并写清这是发帖人自己手机里会有的角度（POV / tabletop / mirror / timer / friend-taken if stated）。
      避免"单一物体居中、纯净背景、影棚打光"的产品图感，也避免不合理的第三者观察镜头。

# ⭐拼贴/图文类用"组合语法"，不要固化成单一模板
- collage 不是固定模板，而是一个大类：photo dump、关键词云、AirDrop 弹窗、日历卡、剪贴簿都属于它。它可繁可简——既能是软木板+拍立得+胶带+邮票的厚装饰版，也能是大留白+无框裁切+少量点缀的极简数字拼贴。别每次都套同一种（尤其别老是拍立得+软木板）。
- 从 base_canvas、material_layers、markup、text_role、layout_logic、finish 各挑元素组合，并贴合 content 与角色审美。走极简就少放编辑痕迹，走繁复就多放——不强制痕迹数量。
- 文字短、少、像真人会写，不要大段作文。
- 可用的组合维度如下：
{composite_grammar}

# 要求
- 【内容取材要发散，别塌缩到猫/咖啡/emo 自拍】下面是从一个大兴趣库里【随机抽样】的几个方向，
  【只是帮你打开思路的灵感，不是题材清单、绝不照搬字面、更不必每个都用】：
{topic_seeds}
  ⭐核心原则：一个人的主页首先要体现 TA 的人设。所以发什么【由角色人设决定】——
  优先从 TA 的职业、爱好、性格、生活方式里长出最贴合的题材；上面的灵感只有在和 TA 对得上时才借用，
  对不上就忽略，自己想更合适的。同一组帖子里【不同帖落在不同方向】，别好几条都在拍同一类东西。
  【输出义务】每条帖子都要带一个 topic_seed 字段：如果这条确实参考了上面某条灵感，就把那一项原样填进去；如果没用灵感、是直接从角色人设里长出来的，就填 ""（空字符串）。不要为了填而硬套灵感，只如实记录真正用到的。{outdoor_hint}
- selfie / photo / text_only 自然混合（不要全是自拍），由人设决定比例。
  photo 帖之间也要换着形式来（screenshot / graphic / collage / photo / journal_overlay 别都用同一种），整个主页像真人的 feed 一样图片形态丰富，
  而不是清一色摆拍照片。{avoid_hint}
- 如果本次输出 ≥6 条，至少 1 条使用非自拍的编辑型配图（从 screenshot / collage / journal_overlay 里选）。
  如果角色本身很爱记录/审美/手帐/穿搭/旅行，可以用 2 条，但若都是 collage，版式和装饰密度必须完全不同（一次繁、一次简）。
- 【每条都要有"发它的理由"】真人不会为了凑数发流水账——一条帖子之所以被发出来，
  是因为此刻 TA 有点想分享/想炫一下/被什么戳到/憋着想说一句/单纯想记下来。
  先想"TA 为什么会发这条"，再写。抓住这个，语气自然有的兴奋、有的烦躁、有的只想说一句话，
  不会全员文艺忧郁，也不用硬凑无聊的内容。宁可这条有冲动有棱角，也不要平庸的填充帖。
- 【发布前自检】每条 image_text 必须同时通过三问：
  1) 陌生人刷到能一秒看懂 TA 想分享什么吗？
  2) 这张图像 TA 自己会发的 ins 素材吗，而不是剧照/监控/第三者偷拍？
  3) 文案里的"我"和图里的拍摄视角是否一致？
  4) 画面里没有出现 {{user}} 或暗示的恋人/约会对象吧？
- selfie 的 variable/shooting/scene 与 content 呼应。
- 只输出一个 JSON 数组，{output_rule}；每个元素必须包含 content、post_type、format、image_type、
  selfie、photo_kind、photo_prompt、photo_schema、topic_seed 这些键。
  image_type="photo" 或 "composite" 时：selfie 为 null，photo_kind、photo_prompt、photo_schema 必填。
  image_type="selfie" 或 text_only 时：photo_kind、photo_prompt、photo_schema 为 null。
- 不要输出任何解释或 markdown 代码块标记。"""

    return [
        {"role": "system", "content": sys},
        {"role": "user", "content": txt},
    ]


def _selfie_style_block(style_prompt: str | None = None) -> str:
    """Fixed style layer for selfie image prompts (从 schema 固定描述提炼)。"""
    if not is_photographic_style(style_prompt):
        return (
            "Social-media selfie composition rendered fully in the selected art style. "
            "Use the pose, framing, expression, and casual phone-camera composition of a "
            "selfie, but do NOT render as realistic photography."
        )
    return (
        "Realistic person photography, Korean idol / actor photoshoot style, casual daily "
        "snapshot. High resolution, no-retouch raw look. Skin rendering: realistic pores, baby "
        "hairs and skin texture, no dark or red blotchy areas. Shot on an actual smartphone "
        "camera: slight lens distortion and soft falloff toward the edges, a small aperture so "
        "depth of field stays fairly deep (background not heavily blurred), natural phone-camera "
        "imperfections. Let the per-shot SHOOTING fields (filter, capture_mode, depth_focus) "
        "decide the camera finish — do not default every image to the same flash/CCD/film look; "
        "vary it shot to shot."
    )


def compose_selfie_prompt(identity: dict, selfie: dict, style_prompt: str | None = None) -> str:
    """Selfie 生图 prompt = 固定写实自拍风格 + identity + variable/shooting/scene。

    用于图生图（拼入重绘封面作为 image_urls 参考）。style_prompt 可选叠加角色画风。
    """
    id_body = ", ".join(_flatten_section(identity))
    var_body = ", ".join(_flatten_section(selfie.get("variable", {})))
    shoot_body = ", ".join(_flatten_section(selfie.get("shooting", {})))
    scene_body = ", ".join(_flatten_section(selfie.get("scene", {})))
    extra = f"\n[CHARACTER ART STYLE] {style_prompt}" if style_prompt else ""
    return (
        f"[STYLE] {_selfie_style_block(style_prompt)}\n"
        f"[IDENTITY] {id_body}\n"
        f"[VARIABLE] {var_body}\n"
        f"[SHOOTING] {shoot_body}\n"
        f"[SCENE] {scene_body}{extra}\n\n"
        "A single Instagram selfie-style image of THIS person. Keep IDENTITY facial features "
        "strictly consistent. Never add a romantic partner, date, or chat partner into the "
        "frame; any other people may appear only if SCENE explicitly names them (a friend, "
        "family member, coworker) or as incidental passersby in the background. "
        "Preserve the casual selfie composition, but the final rendering "
        "must follow the selected art style over any photographic defaults. Let SHOOTING guide "
        "the phone-camera feeling: angle, distance, crop, framing, focus, foreground relationship, "
        "and body pose should read as one natural moment from the character's camera roll.\n"
        "When the framing actually shows the surroundings, place the person in a real, lived-in "
        "environment matching SCENE, with concrete recognizable detail (furniture, signage, objects, "
        "window view, other people) so the location reads and the shot feels like real life rather "
        "than a studio backdrop. For tight close-ups, mirror selfies, or person-focused crops where "
        "little background shows, that is fine too — follow SHOT_SIZE/FRAMING and don't force "
        "background into every shot. Keep depth of field fairly deep so whatever background is "
        "visible stays readable.\n"
        "Apply the color grade / tone described in SHOOTING.filter — whatever it specifies, from "
        "a barely-there clean look to a strong film/flash tone. Don't override it with a neutral "
        "default, but don't force a heavy filter either; match its intensity and keep it natural.\n\n"
        f"{I2I_OVERRIDE_GUARD}\n\n"
        f"{HAND_OBJECT_GUARD}\n\n"
        f"{TATTOO_GUARD}\n\n"
        f"{NO_TEXT_GUARD}"
    )


# 旧版细分 kind 已并入 collage 大类；保留映射以兼容历史数据。
_COLLAGE_KINDS = {"photo_dump", "airdrop_card", "word_cloud", "calendar_card"}


def compose_photo_prompt(photo_prompt: str, style_prompt: str | None = None,
                         photo_kind: str = "photo",
                         photo_schema: dict | None = None) -> str:
    """Photo 帖（角色不正面出镜）文生图 prompt。

    按配图形式分支：截图/图文卡/拼贴需要清晰的文字与 UI，所以【不】套 NO_TEXT_GUARD；
    只有真实随手拍 ("photo") 才禁止文字/logo/水印。
    """
    extra = f"\n[STYLE HINT] {style_prompt}" if style_prompt else ""
    schema_text = ""
    if photo_schema:
        schema_text = "\n[PHOTO SCHEMA]\n" + json.dumps(
            photo_schema, ensure_ascii=False, indent=2
        )

    if photo_kind == "screenshot":
        if not is_photographic_style(style_prompt):
            frame = (
                "A social-media screenshot-style composition rendered in the selected art "
                "style. Keep UI-like layout and legible text if requested, but do not force "
                "realistic phone photography."
            )
            return f"{photo_prompt}{schema_text}{extra}\n\n{frame}"
        frame = (
            "A realistic smartphone screenshot as it would look posted on Instagram. "
            "Render the app UI faithfully: correct layout, buttons/controls, fonts and "
            "colors, and the on-screen TEXT spelled exactly as described (song titles, "
            "lyrics, chat bubbles, notes, etc.). It should read like a genuine phone "
            "screen capture, not a photograph of a screen, not a poster. Crisp, legible, "
            "flat screen graphics."
        )
        return f"{photo_prompt}{schema_text}{extra}\n\n{frame}"

    if photo_kind == "graphic":
        frame = (
            "A minimalist text graphic / quote card in the style real users post on "
            "Instagram and 小红书: a solid or soft-gradient background with a short line "
            "of TEXT set large and centered, tasteful typography, generous negative space. "
            "Spell the quoted text exactly as described. Clean, designed, legible."
        )
        return f"{photo_prompt}{schema_text}{extra}\n\n{frame}"

    if photo_kind == "collage" or photo_kind in _COLLAGE_KINDS:
        frame = (
            "A personal, edited multi-element collage as a real person posts on Instagram / "
            "小红书. This is a broad family — it can be a free photo collage, a monthly/weekend "
            "photo dump, a keyword cloud around a photo, an AirDrop/desktop pop-up card, a "
            "calendar/planner card, or a scrapbook page. Pick ONE coherent look that fits the "
            "post and the character's taste, and vary it from post to post — do not always "
            "default to the same one.\n"
            "It may be richly decorated (corkboard with polaroid-framed photos, washi tape, "
            "pushpins, postage-stamp edges, paper notes, date stamps) OR clean and minimal "
            "(plain/gradient/paper background, frameless cut-out photos, lots of negative "
            "space, just a few stars/dots/thin connector lines or a tiny URL). Many real "
            "collages are airy with one hero photo plus smaller cut-outs arranged with clear "
            "hierarchy, not an even grid of same-size framed photos.\n"
            "Follow the photo_prompt and photo_schema for the chosen layout, materials, markup "
            "and finish. Keep any visible text short and intentionally placed; lived-in and "
            "personal, not a corporate template ad."
        )
        return f"{photo_prompt}{schema_text}{extra}\n\n{frame}"

    if photo_kind == "journal_overlay":
        frame = (
            "A real lifestyle photo with handwritten diary annotations on top: short "
            "handwritten words, arrows, circles, date/time notes, and small doodles placed "
            "directly over the image. The base photo remains the main subject, while the "
            "writing looks casual and human, like finger-written markup in an Instagram "
            "story editor. Keep the text legible and sparse."
        )
        return f"{photo_prompt}{schema_text}{extra}\n\n{frame}"

    # default: real candid photo (keep text/logo guard)
    if not is_photographic_style(style_prompt):
        return (
            f"{photo_prompt}{schema_text}{extra}\n\n"
            "Render this as a social-media post image fully in the selected art style. "
            "Keep the poster-owned composition logic (POV, tabletop, object, environment, "
            "mirror/timer, or explicitly friend-taken), but do NOT render as realistic "
            "photography or a real camera snapshot. The selected art style overrides all "
            "photo-real language.\n\n"
            f"{NO_TEXT_GUARD}"
        )
    return (
        f"{photo_prompt}{schema_text}{extra}\n\n"
        "An authentic Instagram photo from the poster's own phone or saved camera roll — "
        "a POV / tabletop / object / environment / mirror / timer / explicitly friend-taken "
        "lifestyle shot, NOT a clean studio product shot and NOT an omniscient third-person "
        "scene still. The image must feel like something the poster could realistically have "
        "taken or chosen to upload themselves. Do not show the poster from an impossible "
        "observer angle such as an unexplained over-the-shoulder/back-view shot of them "
        "working or living. If the poster appears at all, keep it to hands, sleeve, shadow, "
        "reflection, cropped partial body, mirror/timer setup, or an explicitly friend-taken "
        "photo. Never depict the poster's romantic partner, date, or chat partner as a person "
        "in the frame; other people may appear only as incidental passersby or staff. "
        "Real-life casual composition, natural lighting, tasteful and aesthetic with "
        "a lived-in feel.\n"
        "Apply the color grade / tone described in photo_schema.color_tone — match whatever it "
        "specifies, from a clean near-unfiltered look to a strong film tone. Don't override it "
        "with a neutral default, and don't force a heavy filter; keep the intensity natural.\n\n"
        f"{NO_TEXT_GUARD}"
    )
