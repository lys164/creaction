[pytest]
# 保持测试输出 pristine：仅忽略外部库触发的、与本项目代码无关的弃用告警。
# 该告警由 fastapi/starlette 的 TestClient 导入触发（starlette 建议改用 httpx2），
# 非本项目代码可控。范围精确到该条消息，不会掩盖本项目自身的告警。
filterwarnings =
    ignore:Using .httpx. with .starlette.testclient. is deprecated
